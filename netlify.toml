[build]
  publish = "dist"
  # Skip submodule checkout by using a custom command
  command = "git config --global --add safe.directory '*' && npm run build"

[build.environment]
  NODE_VERSION = "18"
  
# Disable submodule processing
[build.processing]
  skip_processing = false
