[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"
  
# Disable submodule checkout
# Netlify will skip submodule checkout if .gitmodules is empty or missing
