[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# Disable submodule checkout
# This prevents Netlify from trying to checkout submodules
[build]
  ignore = "git diff --quiet HEAD^ HEAD ."
