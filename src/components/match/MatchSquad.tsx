// src/components/match/MatchSquad.tsx - COMPLETE VERSION
import React, { useState } from 'react';
import {
  View,
  Text,
  TouchableOpacity,
  StyleSheet,
  ScrollView,
  Dimensions,
  useWindowDimensions,
  Modal,
  FlatList,
  Platform,
  InteractionManager,
  ActivityIndicator,
} from 'react-native';
import { showAlert, showInfo } from '../../utils/alertHelper';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { supabase } from '../../config/supabase';
import { STORAGE_KEYS, PITCH_LAYOUT } from '../../config/constants';
import { squadPredictionsApi, teamsApi, matchesApi } from '../../services/api';
import { getBulkSquad, refreshBulkSquad } from '../../services/bulkDataService';
import { predictionsDb } from '../../services/databaseService';
import { ConfirmModal, ConfirmButton } from '../ui/ConfirmModal';
import { LinearGradient } from 'expo-linear-gradient';
import { Ionicons } from '@expo/vector-icons';
import Animated, { 
  SlideInDown,
  SlideOutDown,
  useAnimatedStyle,
  withRepeat,
  withTiming,
  useSharedValue,
} from 'react-native-reanimated';
import Svg, { 
  Rect, 
  Circle, 
  Line, 
  Path,
} from 'react-native-svg';
import { CommunitySignalPopup } from './CommunitySignalPopup';
import { useFavoriteTeams } from '../../hooks/useFavoriteTeams';
import { useTheme } from '../../contexts/ThemeContext';
import { useTranslation } from '../../hooks/useTranslation';
import { COLORS } from '../../theme/theme';
import { isMockTestMatch, isMockLive999999, getMockUserTeamId, MOCK_MATCH_IDS, getMatch1Start, getMatch2Start, getUserPreferenceStats, generateAutoSquad } from '../../data/mockTestData';
import { getPreferenceBorderStyle, getPositionGroupColor, type UserPreferenceStats, type AutoGeneratedSquad } from '../../utils/squadPreferenceUtils';
import { formatPlayerDisplayName, formatPlayerSurname } from '../../utils/playerNameUtils';
import { formations, formationLabels, formationPositions } from '../../constants/formations';

// Web için dinamik boyutlandırma
const isWeb = Platform.OS === 'web';
const screenDimensions = Dimensions.get('window');
// Web'de max 500px, mobilde ekran genişliği
const width = isWeb ? Math.min(screenDimensions.width, 500) : screenDimensions.width;
const height = screenDimensions.height;

// Helper function to get color based on stat value
const getStatColor = (value: number): string => {
  if (value >= 85) return '#10B981'; // Green - Excellent
  if (value >= 70) return '#1FA2A6'; // Teal - Good
  if (value >= 50) return '#F59E0B'; // Yellow - Average
  return '#EF4444'; // Red - Poor
};

// Rating normalizasyonu - TÜM rating'ler 100 üzerinden gösterilir
// API'den gelen: 0-10 (maç reytingi) veya 65-99 (genel). Hepsi 0-100 gösterim (60–70 farkı korunur, yuvarlamayla kaybedilmez).
const normalizeRatingTo100 = (rating: number | null | undefined): number => {
  if (rating == null) return 75; // Default
  const r = Number(rating);
  if (r > 0 && r <= 10) return Math.min(100, Math.round(r * 10)); // 6.7 → 67
  if (r > 10 && r <= 100) return Math.round(r); // 72.4 → 72
  return 75; // Fallback
};

// Rating görüntüleme için string formatı
const formatRatingDisplay = (rating: number | null | undefined): string => {
  return normalizeRatingTo100(rating).toString();
};

interface MatchSquadProps {
  matchData: any;
  matchId: string;
  lineups?: any[];
  /** Sadece bu takım(lar)ın kadrosu gösterilir; rakip gizlenir. Boşsa tüm kadro gösterilir. */
  favoriteTeamIds?: number[];
  /** İki favori takım maçında hangi takım için kadro seçildiği; verilirse kadro/tahmin bu takıma özel saklanır. */
  predictionTeamId?: number;
  onComplete: () => void;
  /** Atak formasyonu değişikliği onaylandıktan sonra (tahminler silindikten sonra) çağrılır – örn. Dashboard'a dönüp analiz odağı seçimi gösterilsin */
  onAttackFormationChangeConfirmed?: () => void;
  /** Kadro sekmesi görünür olduğunda true – Tahmin'den geri dönüldüğünde storage'dan tekrar yüklenir */
  isVisible?: boolean;
  /** Biten maç: ilk 11 sabit, oyuncu çıkarma (çarpı) gösterilmez */
  isMatchFinished?: boolean;
  /** Canlı maç: Kadro VIEW-ONLY (maç başladığında kilitlenir) */
  isMatchLive?: boolean;
  /** Kaydedilmemiş değişiklik olduğunda çağrılır - parent component'e bildirir */
  onHasUnsavedChanges?: (hasChanges: boolean) => void;
  /** İlk 11 popup'ı zaten gösterildi mi? (parent state - sekme değişse bile korunur) */
  startingXIPopupShown?: boolean;
  /** İlk 11 popup'ı gösterildiğinde çağrılır - parent'a bildirir */
  onStartingXIPopupShown?: () => void;
  /** ✅ Topluluk verilerini gördü mü? (görüldüyse kadro kilidi açılamaz) */
  hasViewedCommunityData?: boolean;
  /** ✅ Tahminler kaydedilip kilitlendiyse kadro düzenleme kapatılır (Tahmin sekmesinde "Tahminleri Kaydet" sonrası) */
  squadEditingDisabled?: boolean;
  /** Kadro yokken (canlı/biten maç) Tahmin / Canlı / İstatistik sekmelerine yönlendirmek için. predictionSubIndex: 1 = Topluluk, 2 = Gerçek */
  onNavigateToTab?: (tab: 'prediction' | 'live' | 'stats', options?: { predictionSubIndex?: number }) => void;
}

/** API'den gelen tüm kaleci varyantlarını tanı (G, GK, Goalkeeper vb.) */
function isGoalkeeperPlayer(p: { position?: string; pos?: string } | null | undefined): boolean {
  if (!p) return false;
  const pos = (p.position ?? p.pos ?? '') as string;
  if (!pos) return false;
  const lower = pos.toLowerCase();
  return pos === 'GK' || pos === 'G' || lower === 'goalkeeper' || lower.startsWith('goalkeeper');
}

/** Kadro oluştururken pozisyonu normalize et – kaleciler her yerde 'GK' olsun */
function normalizePosition(pos: string | undefined): string {
  const raw = pos ?? '';
  const lower = raw.toLowerCase();
  if (raw === 'G' || raw === 'GK' || lower === 'goalkeeper' || lower.startsWith('goalkeeper')) return 'GK';
  return raw || 'SUB';
}

// Football Field Component
const FootballField = ({ children, style }: any) => (
  <View style={[styles.fieldContainer, style]}>
    <LinearGradient
      colors={['#16A34A', '#22C55E', '#16A34A']}
      start={{ x: 0, y: 0 }}
      end={{ x: 0, y: 1 }}
      style={styles.fieldGradient}
    >
      <Svg width="100%" height="100%" viewBox="0 0 100 150" preserveAspectRatio="none" style={styles.fieldSvg}>
        <Rect x="2" y="2" width="96" height="146" fill="none" stroke="white" strokeWidth="0.5" opacity="0.3" />
        <Line x1="2" y1="75" x2="98" y2="75" stroke="white" strokeWidth="0.5" opacity="0.3" />
        <Circle cx="50" cy="75" r="13.5" fill="none" stroke="white" strokeWidth="0.5" opacity="0.3" />
        <Circle cx="50" cy="75" r="1" fill="white" opacity="0.3" />
        <Rect x="20.35" y="2" width="59.3" height="23" fill="none" stroke="white" strokeWidth="0.5" opacity="0.3" />
        <Rect x="36.55" y="2" width="26.9" height="7.7" fill="none" stroke="white" strokeWidth="0.5" opacity="0.3" />
        <Circle cx="50" cy="17.3" r="0.8" fill="white" opacity="0.3" />
        <Rect x="20.35" y="125" width="59.3" height="23" fill="none" stroke="white" strokeWidth="0.5" opacity="0.3" />
        <Rect x="36.55" y="140.3" width="26.9" height="7.7" fill="none" stroke="white" strokeWidth="0.5" opacity="0.3" />
        <Circle cx="50" cy="132.7" r="0.8" fill="white" opacity="0.3" />
        <Path d="M 2 4.5 A 2.5 2.5 0 0 1 4.5 2" stroke="white" strokeWidth="0.5" fill="none" opacity="0.3" />
        <Path d="M 95.5 2 A 2.5 2.5 0 0 1 98 4.5" stroke="white" strokeWidth="0.5" fill="none" opacity="0.3" />
        <Path d="M 98 145.5 A 2.5 2.5 0 0 1 95.5 148" stroke="white" strokeWidth="0.5" fill="none" opacity="0.3" />
        <Path d="M 4.5 148 A 2.5 2.5 0 0 1 2 145.5" stroke="white" strokeWidth="0.5" fill="none" opacity="0.3" />
      </Svg>
      {children}
    </LinearGradient>
  </View>
);

export function MatchSquad({ matchData, matchId, lineups, favoriteTeamIds: favoriteTeamIdsProp = [], predictionTeamId, onComplete, onAttackFormationChangeConfirmed, isVisible = true, isMatchFinished = false, isMatchLive: isMatchLiveProp, onHasUnsavedChanges, startingXIPopupShown = false, onStartingXIPopupShown, hasViewedCommunityData = false, squadEditingDisabled = false, onNavigateToTab }: MatchSquadProps) {
  const { width: winW, height: winH } = useWindowDimensions();
  const { theme } = useTheme();
  const { t } = useTranslation();
  const isLight = theme === 'light';
  const themeColors = isLight ? COLORS.light : COLORS.dark;

  // ✅ Kendi hook'umuzla favori takımları al - prop boş gelebilir
  const { favoriteTeams } = useFavoriteTeams();
  const favoriteTeamIds = React.useMemo(() => {
    // Prop doluysa onu kullan, yoksa hook'tan al
    if (favoriteTeamIdsProp.length > 0) return favoriteTeamIdsProp;
    return favoriteTeams?.map(t => t.id) ?? [];
  }, [favoriteTeamIdsProp, favoriteTeams]);
  
  // Saha boyutları - Tahmin ile BİREBİR aynı hesaplama (runtime'da)
  const fieldWidth = isWeb ? Math.min(winW, 500) - PITCH_LAYOUT.H_PADDING : winW - PITCH_LAYOUT.H_PADDING;
  const fieldHeight = isWeb 
    ? Math.min(PITCH_LAYOUT.WEB_HEIGHT, Math.max(320, winH - 320))
    : fieldWidth * PITCH_LAYOUT.ASPECT_RATIO;
  
  const fieldDynamicStyle: { width: number; height: number; maxWidth?: number; maxHeight?: number } = {
    width: fieldWidth,
    height: fieldHeight,
  };
  if (isWeb) {
    fieldDynamicStyle.maxWidth = PITCH_LAYOUT.WEB_MAX_WIDTH;
    fieldDynamicStyle.maxHeight = Math.min(PITCH_LAYOUT.WEB_HEIGHT, Math.max(0, winH - 320));
  }

  // ✅ Takım ID'lerini matchData'dan al
  const homeTeamId = matchData?.teams?.home?.id || matchData?.homeTeam?.id;
  const awayTeamId = matchData?.teams?.away?.id || matchData?.awayTeam?.id;
  const homeTeamName = matchData?.teams?.home?.name || matchData?.homeTeam?.name || 'Ev Sahibi';
  const awayTeamName = matchData?.teams?.away?.name || matchData?.awayTeam?.name || 'Deplasman';

  // ✅ Mock maçlarda senin takımın: 888001 → FB (611), 888002 → Real (541) – kadro bu takıma göre kaydedilir
  const matchIdNum = typeof matchId === 'string' ? parseInt(matchId, 10) : matchId;
  const effectivePredictionTeamId = predictionTeamId ?? getMockUserTeamId(matchIdNum) ?? undefined;
  // İki favori maçta veya mock maçta takıma özel kadro anahtarı
  const squadStorageKey = React.useMemo(
    () => (effectivePredictionTeamId != null ? `${STORAGE_KEYS.SQUAD}${matchId}-${effectivePredictionTeamId}` : `${STORAGE_KEYS.SQUAD}${matchId}`),
    [matchId, effectivePredictionTeamId]
  );
  // ✅ Takım kadrosu için state (önce uygulama cache'i, yoksa API)
  const [squadPlayers, setSquadPlayers] = React.useState<any[]>([]);
  const [isLoadingSquad, setIsLoadingSquad] = React.useState(false);
  const [squadLoadingFromApi, setSquadLoadingFromApi] = React.useState(false); // true = API'den çekiliyor (mesaj için)
  const [squadRetryKey, setSquadRetryKey] = React.useState(0);

  // ✅ Topluluk "oyundan çıksın" verisi (subOutRate = subOutVotes / totalPredictors)
  // Mock 999999: API yoksa client-side fallback ile her zaman göster
  const [subOutData, setSubOutData] = React.useState<{ totalPredictors: number; players: Record<string | number, { subOutVotes: number; replacementName?: string | null }> } | null>(null);
  React.useEffect(() => {
    if (!matchId || !isVisible) return;
    let cancelled = false;
    matchesApi.getCommunityStats(String(matchId))
      .then((res: any) => {
        if (cancelled) return;
        if (res?.data?.subOutByPlayer) {
          setSubOutData({
            totalPredictors: res.data.subOutByPlayer.totalPredictors || 0,
            players: res.data.subOutByPlayer.players || {},
          });
        }
      })
      .catch(() => {});
    return () => { cancelled = true; };
  }, [matchId, isVisible]);

  const retrySquadFetch = React.useCallback(() => {
    setSquadRetryKey((k) => k + 1);
  }, []);

  // ✅ Lineups yoksa kadro: önce uygulama cache'inden (bulk), yoksa API
  React.useEffect(() => {
    const mapBulkPlayer = (p: any, teamName: string, teamId: number) => ({
      id: p.id,
      name: p.name ?? 'Bilinmiyor',
      firstname: p.firstname ?? null,
      lastname: p.lastname ?? null,
      position: normalizePosition(p.position),
      rating: p.rating ?? 75,
      number: p.number ?? p.shirt_number ?? p.jersey_number ?? 0,
      team: teamName,
      teamId,
      form: 7,
      injury: false,
      age: p.age ?? 25,
      nationality: 'Unknown',
      photo: p.photo ?? null,
      stats: { pace: 70, shooting: 70, passing: 70, dribbling: 70, defending: 70, physical: 70 },
    });

    /**
     * ✅ DOĞRU MİMARİ: API-First, Cache-Second (Stale-While-Revalidate)
     * 
     * 1. Her zaman API'den güncel veriyi çek (Single Source of Truth = Backend DB)
     * 2. API başarılıysa: Veriyi göster + cache'i güncelle
     * 3. API başarısızsa: Cache'den göster (offline/hata durumu)
     * 4. Cache sadece fallback, asla birincil kaynak değil
     */
    const fetchSquads = async () => {
      if (lineups && lineups.length > 0) return;
      if (!homeTeamId || !awayTeamId) {
        console.log('⚠️ No team IDs for squad fetch');
        return;
      }

      setIsLoadingSquad(true);
      setSquadLoadingFromApi(true);

      try {
        // ✅ ADIM 1: Her zaman API'den güncel veriyi çek (Backend DB = Single Source of Truth)
        // squadRetryKey > 0 = "Yeniden Dene" tıklandı → API'den zorla yenile (eski oyuncular sorunu)
        const forceRefresh = squadRetryKey > 0;
        console.log('📥 Fetching team squads from API (source of truth)...', { homeTeamId, awayTeamId, forceRefresh });

        const homePromise = teamsApi.getTeamSquad(homeTeamId, undefined, forceRefresh).then((json) => json).catch((err) => {
          console.error('❌ Home squad fetch exception:', err.message);
          return null;
        });
        const awayPromise = teamsApi.getTeamSquad(awayTeamId, undefined, forceRefresh).then((json) => json).catch((err) => {
          console.error('❌ Away squad fetch exception:', err.message);
          return null;
        });

        const [homeRes, awayRes] = await Promise.all([homePromise, awayPromise]);

        const allPlayers: any[] = [];

        if (homeRes?.data?.players && Array.isArray(homeRes.data.players)) {
          homeRes.data.players.forEach((p: any) => {
            allPlayers.push({
              id: p.id,
              name: p.name,
              firstname: p.firstname ?? null,
              lastname: p.lastname ?? null,
              position: normalizePosition(p.position),
              rating: p.rating || 75,
              number: p.number ?? p.shirt_number ?? p.jersey_number, team: homeTeamName, teamId: homeTeamId,
              form: 7, injury: p.injured || false, age: p.age || 25,
              nationality: p.nationality || 'Unknown', photo: p.photo,
              stats: { pace: 70, shooting: 70, passing: 70, dribbling: 70, defending: 70, physical: 70 }
            });
          });
        }
        if (awayRes?.data?.players && Array.isArray(awayRes.data.players)) {
          awayRes.data.players.forEach((p: any) => {
            allPlayers.push({
              id: p.id,
              name: p.name,
              firstname: p.firstname ?? null,
              lastname: p.lastname ?? null,
              position: normalizePosition(p.position),
              rating: p.rating || 75,
              number: p.number ?? p.shirt_number ?? p.jersey_number, team: awayTeamName, teamId: awayTeamId,
              form: 7, injury: p.injured || false, age: p.age || 25,
              nationality: p.nationality || 'Unknown', photo: p.photo,
              stats: { pace: 70, shooting: 70, passing: 70, dribbling: 70, defending: 70, physical: 70 }
            });
          });
        }

        // ✅ ADIM 2: API başarılıysa veriyi göster ve cache'i güncelle
        if (allPlayers.length > 0) {
          console.log('✅ Squad loaded from API (source of truth):', allPlayers.length);
          setSquadPlayers(allPlayers);
          
          // Cache'i arka planda güncelle (sonraki offline erişim için)
          if (homeRes?.data?.players) {
            refreshBulkSquad(homeTeamId, homeRes.data.players).catch(() => {});
          }
          if (awayRes?.data?.players) {
            refreshBulkSquad(awayTeamId, awayRes.data.players).catch(() => {});
          }
          return;
        }

        // ✅ ADIM 3: API başarısız veya boş döndüyse → Cache'e fallback (offline/hata durumu)
        console.log('⚠️ API returned no data, falling back to cache...');
        
        const [homeBulk, awayBulk] = await Promise.all([
          getBulkSquad(homeTeamId),
          getBulkSquad(awayTeamId),
        ]);

        const homeList = Array.isArray(homeBulk) ? homeBulk : [];
        const awayList = Array.isArray(awayBulk) ? awayBulk : [];

        if (homeList.length > 0 || awayList.length > 0) {
          const cachedPlayers: any[] = [];
          homeList.forEach((p: any) => cachedPlayers.push(mapBulkPlayer(p, homeTeamName, homeTeamId)));
          awayList.forEach((p: any) => cachedPlayers.push(mapBulkPlayer(p, awayTeamName, awayTeamId)));
          
          if (cachedPlayers.length > 0) {
            console.log('📦 Squad loaded from cache (fallback):', cachedPlayers.length);
            setSquadPlayers(cachedPlayers);
            return;
          }
        }

        // Her iki kaynak da başarısız
        console.warn('⚠️ No squad data available from API or cache.');
        if (!homeRes && !awayRes) {
          console.error('❌ Both API calls failed - Backend may not be running on port 3001');
        }
        setSquadPlayers([]);

      } catch (err: any) {
        console.error('❌ Squad fetch error:', err?.message || err);
        
        // Hata durumunda cache'e fallback dene
        try {
          const [homeBulk, awayBulk] = await Promise.all([
            getBulkSquad(homeTeamId),
            getBulkSquad(awayTeamId),
          ]);
          const homeList = Array.isArray(homeBulk) ? homeBulk : [];
          const awayList = Array.isArray(awayBulk) ? awayBulk : [];
          
          if (homeList.length > 0 || awayList.length > 0) {
            const cachedPlayers: any[] = [];
            homeList.forEach((p: any) => cachedPlayers.push(mapBulkPlayer(p, homeTeamName, homeTeamId)));
            awayList.forEach((p: any) => cachedPlayers.push(mapBulkPlayer(p, awayTeamName, awayTeamId)));
            console.log('📦 Squad loaded from cache after error:', cachedPlayers.length);
            setSquadPlayers(cachedPlayers);
            return;
          }
        } catch {
          // Cache de başarısız
        }
        
        setSquadPlayers([]);
      } finally {
        setIsLoadingSquad(false);
        setSquadLoadingFromApi(false);
      }
    };

    fetchSquads();
  }, [lineups, homeTeamId, awayTeamId, homeTeamName, awayTeamName, matchId, squadRetryKey]);

  // ✅ GERÇEK VERİ: Önce lineups, yoksa squadPlayers kullan
  const realPlayers = React.useMemo(() => {
    // 1. Lineups varsa kullan (maç kadrosu)
    if (lineups && lineups.length > 0) {
    
    // Her iki takımın oyuncularını birleştir
    const allPlayers: any[] = [];
    lineups.forEach((lineup: any, index: number) => {
      const lineupTeamId = lineup.team?.id;
      const lineupTeamName = lineup.team?.name || 'Unknown';
      const teamColors = lineup.team?.colors; // Backend'den gelen team colors
      
      // ✅ Takım ID'yi doğrula ve eşleştir
      let finalTeamId = lineupTeamId;
      let finalTeamName = lineupTeamName;
      
      // Eğer lineup team.id matchData ile eşleşiyorsa kullan
      if (lineupTeamId === homeTeamId) {
        finalTeamId = homeTeamId;
        finalTeamName = homeTeamName;
      } else if (lineupTeamId === awayTeamId) {
        finalTeamId = awayTeamId;
        finalTeamName = awayTeamName;
      } else {
        // Eşleşme yoksa, lineup sırasına göre ata (ilk = home, ikinci = away)
        if (index === 0 && homeTeamId) {
          finalTeamId = homeTeamId;
          finalTeamName = homeTeamName;
          console.warn(`⚠️ Lineup[0] team ID mismatch: lineup.team.id=${lineupTeamId}, assigning to HOME (${homeTeamId})`);
        } else if (index === 1 && awayTeamId) {
          finalTeamId = awayTeamId;
          finalTeamName = awayTeamName;
          console.warn(`⚠️ Lineup[1] team ID mismatch: lineup.team.id=${lineupTeamId}, assigning to AWAY (${awayTeamId})`);
        } else {
          console.error(`❌ Cannot match lineup[${index}] team ID ${lineupTeamId} to home (${homeTeamId}) or away (${awayTeamId})`);
        }
      }
      
      // Başlangıç 11'i ekle
      if (lineup.startXI) {
        lineup.startXI.forEach((item: any) => {
          // Backend enriched format: doğrudan player objesi
          // Eski format: item.player içinde
          const player = item.player || item;
          allPlayers.push({
            id: player.id,
            name: player.name,
            position: normalizePosition(player.pos || player.position),
            rating: player.rating || 75, // ✅ Backend'den gelen gerçek rating
            number: player.number,
            team: finalTeamName, // ✅ Doğrulanmış takım adı
            teamId: finalTeamId, // ✅ Doğrulanmış takım ID
            teamColors: teamColors,
            form: 7,
            injury: false,
            age: player.age || 25,
            nationality: player.nationality || 'Unknown',
            grid: player.grid,
            stats: player.stats || { pace: 70, shooting: 70, passing: 70, dribbling: 70, defending: 70, physical: 70 } // ✅ Backend'den gelen stats varsa kullan
          });
        });
      }
      
      // Yedekleri ekle
      if (lineup.substitutes) {
        lineup.substitutes.forEach((item: any) => {
          const player = item.player || item;
          allPlayers.push({
            id: player.id,
            name: player.name,
            position: normalizePosition(player.pos || player.position),
            rating: player.rating || 70, // ✅ Backend'den gelen gerçek rating
            number: player.number,
            team: finalTeamName, // ✅ Doğrulanmış takım adı
            teamId: finalTeamId, // ✅ Doğrulanmış takım ID
            teamColors: teamColors,
            form: 6,
            injury: false,
            age: player.age || 25,
            nationality: player.nationality || 'Unknown',
            grid: player.grid,
            stats: player.stats || { pace: 65, shooting: 65, passing: 65, dribbling: 65, defending: 65, physical: 65 } // ✅ Backend'den gelen stats varsa kullan
          });
        });
      }
    });

      // ✅ Debug: Takım dağılımını kontrol et
      const homePlayers = allPlayers.filter((p: any) => p.teamId === homeTeamId);
      const awayPlayers = allPlayers.filter((p: any) => p.teamId === awayTeamId);
      console.log(`🔍 Lineups team check: homeTeamId=${homeTeamId} (${homeTeamName}) → ${homePlayers.length} players, awayTeamId=${awayTeamId} (${awayTeamName}) → ${awayPlayers.length} players`);

      // ✅ Mock maç (999999 veya 888001/888002) için tüm oyuncuları göster, favori filtresi uygulama
      const matchIdNum = typeof matchId === 'string' ? parseInt(matchId, 10) : matchId;
      const isMockMatch = matchId === 999999 || matchId === '999999' || String(matchId) === '999999' || isMockTestMatch(matchIdNum);
      
      // String/number uyumsuzluğunu önlemek için favorileri number'a çevir
      const favIdsNum = favoriteTeamIds.map((id: any) => typeof id === 'string' ? parseInt(id, 10) : id);
      
      const filtered = (isMockMatch || favoriteTeamIds.length === 0)
        ? allPlayers
        : allPlayers.filter((p: any) => {
            if (p.teamId == null) return false;
            const pTeamNum = typeof p.teamId === 'string' ? parseInt(p.teamId, 10) : p.teamId;
            return favIdsNum.includes(pTeamNum);
          });
      // Lineups + SquadPlayers birleştir: lineups'ta olmayan ama kadroda olan oyuncuları ekle
      // Böylece Osimhen, yedek kaleciler vb. kaybolmaz
      const lineupIds = new Set(filtered.map((p: any) => p.id));
      if (squadPlayers.length > 0) {
        const squadFiltered = (isMockMatch || favoriteTeamIds.length === 0)
          ? squadPlayers
          : squadPlayers.filter((p: any) => {
              if (p.teamId == null) return false;
              const pTeamNum = typeof p.teamId === 'string' ? parseInt(p.teamId, 10) : p.teamId;
              return favIdsNum.includes(pTeamNum);
            });
        squadFiltered.forEach((sp: any) => {
          if (!lineupIds.has(sp.id)) {
            filtered.push(sp);
            lineupIds.add(sp.id);
          }
        });
      }

      // Dedup: aynı ID'li oyuncuları tek tut
      const deduped: any[] = [];
      const seenPlayerIds = new Set<number>();
      for (const p of filtered) {
        if (!seenPlayerIds.has(p.id)) {
          seenPlayerIds.add(p.id);
          deduped.push(p);
        }
      }

      console.log('✅ Real players loaded (LINEUPS + SQUAD merged):', deduped.length, (isMockMatch ? '(mock - all players)' : favoriteTeamIds.length ? '(favori only)' : ''));
      return deduped;
    }

    // 2. Lineups yoksa squadPlayers kullan (takım kadrosu – fetch zaten favoriye göre filtrelenmiş)
    if (squadPlayers.length > 0) {
      // Dedup: aynı ID'li oyuncuları tek tut
      const deduped: any[] = [];
      const seenPlayerIds = new Set<number>();
      for (const p of squadPlayers) {
        if (!seenPlayerIds.has(p.id)) {
          seenPlayerIds.add(p.id);
          deduped.push(p);
        }
      }
      console.log('✅ Real players loaded from SQUAD API:', deduped.length);
      return deduped;
    }

    // 3. Hiçbiri yoksa boş array
    console.log('⚠️ No players available');
    return [];
  }, [lineups, squadPlayers, favoriteTeamIds, matchId, homeTeamId, awayTeamId, homeTeamName, awayTeamName]);

  // ✅ Atak kadrosu = senin takımın. Mock maçlarda: 888001 → FB (611), 888002 → Real (541).
  const attackTeamId = React.useMemo(() => {
    // İki favori maçta (predictionTeamId varsa) onu kullan
    if (predictionTeamId != null) return predictionTeamId;
    
    if (!homeTeamId || !awayTeamId) return homeTeamId || awayTeamId;
    
    // ✅ Mock maç (888001/888002): Senin takımın FB ve Real – kadro buna göre seçilir ve kaydedilir
    const mockUserTeam = getMockUserTeamId(matchIdNum);
    if (mockUserTeam != null) {
      console.log('🎯 Mock maç – senin takımın:', mockUserTeam, mockUserTeam === 611 ? '(FB)' : '(Real)');
      return mockUserTeam;
    }
    // ✅ Eski mock (999999) için ev sahibi
    const isMock9999 = matchId === 999999 || matchId === '999999' || String(matchId) === '999999';
    if (isMock9999) return homeTeamId;
    
    // ✅ Favori takımlardan biri maçtaysa onu kullan
    // String/number uyumsuzluğunu önlemek için her iki türde de kontrol et
    const homeIdNum = typeof homeTeamId === 'string' ? parseInt(homeTeamId, 10) : homeTeamId;
    const awayIdNum = typeof awayTeamId === 'string' ? parseInt(awayTeamId, 10) : awayTeamId;
    const favIdsNum = favoriteTeamIds.map((id: any) => typeof id === 'string' ? parseInt(id, 10) : id);
    
    console.log('🔍 Favori kontrol:', { homeTeamId, awayTeamId, favoriteTeamIds, favIdsNum, homeIdNum, awayIdNum });
    
    if (favIdsNum.includes(homeIdNum)) {
      console.log('✅ Home team is favorite:', homeIdNum);
      return homeTeamId;
    }
    if (favIdsNum.includes(awayIdNum)) {
      console.log('✅ Away team is favorite:', awayIdNum);
      return awayTeamId;
    }
    
    console.warn('⚠️ No favorite team found in match, falling back to home team', { homeTeamId, awayTeamId, favoriteTeamIds });
    return homeTeamId;
  }, [homeTeamId, awayTeamId, favoriteTeamIds, predictionTeamId, matchId, matchIdNum]);

  // ✅ Atak modunda sadece atak takımının oyuncuları (rakip atanamaz)
  const attackTeamPlayers = React.useMemo(() => {
    if (!attackTeamId) return realPlayers;
    
    // ✅ Mock maç için özel kontrol (999999 veya 888001/888002)
    const matchIdNum = typeof matchId === 'string' ? parseInt(matchId, 10) : matchId;
    const isMockMatch = matchId === 999999 || matchId === '999999' || String(matchId) === '999999' || isMockTestMatch(matchIdNum);
    
    // String/number uyumsuzluğunu önlemek için attackTeamId'yi number'a çevir
    const attackTeamIdNum = typeof attackTeamId === 'string' ? parseInt(attackTeamId, 10) : attackTeamId;
    
    const filtered = realPlayers.filter((p: any) => {
      if (p.teamId == null) return false;
      const pTeamNum = typeof p.teamId === 'string' ? parseInt(p.teamId, 10) : p.teamId;
      return pTeamNum === attackTeamIdNum;
    });
    
    // Mock maçta eğer filtreleme sonucu boş ise, ilk takımın (home) oyuncularını döndür
    if (isMockMatch && filtered.length === 0 && realPlayers.length > 0) {
      // realPlayers'daki ilk takımın ID'sini bul ve o takımın oyuncularını döndür
      const firstTeamId = realPlayers[0]?.teamId;
      if (firstTeamId) {
        const homeFiltered = realPlayers.filter((p: any) => p.teamId === firstTeamId);
        console.log('🎯 Mock maç: attackTeamId eşleşmedi, ilk takımın oyuncuları kullanılıyor:', homeFiltered.length);
        return homeFiltered;
      }
    }
    
    console.log(`📋 attackTeamPlayers: attackTeamId=${attackTeamId}, filtered=${filtered.length}/${realPlayers.length}`);
    return filtered;
  }, [realPlayers, attackTeamId, matchId]);

  // ✅ Attack & Defense Formation States
  const [attackFormation, setAttackFormation] = useState<string | null>(null);
  const [defenseFormation, setDefenseFormation] = useState<string | null>(null);
  const [attackPlayers, setAttackPlayers] = useState<Record<number, any | null>>({});
  const [defensePlayers, setDefensePlayers] = useState<Record<number, any | null>>({});
  
  // ✅ Current editing mode: 'attack' or 'defense'
  const [editingMode, setEditingMode] = useState<'attack' | 'defense'>('attack');
  
  // ✅ Oynanan maçta karşılaştırma görünümü: kullanıcı tahmini / gerçek 11 / topluluk tercihleri
  type ViewSource = 'user' | 'actual' | 'community';
  const [viewSource, setViewSource] = useState<ViewSource>('community'); // Varsayılan: topluluk
  
  // ✅ Gerçek ilk 11 (API lineup'tan) - atak ve defans formasyonları
  const [actualAttackFormation, setActualAttackFormation] = useState<string | null>(null);
  const [actualAttackPlayers, setActualAttackPlayers] = useState<Record<number, any>>({});
  const [actualDefenseFormation, setActualDefenseFormation] = useState<string | null>(null);
  const [actualDefensePlayers, setActualDefensePlayers] = useState<Record<number, any>>({});
  
  // ✅ Confirmation modal for defense formation
  const [showDefenseConfirmModal, setShowDefenseConfirmModal] = useState(false);
  const [formationConfirmModal, setFormationConfirmModal] = useState<{ formationId: string } | null>(null);
  // ✅ Kadro Tamamlandı – ekran ortasında birkaç saniye gösterilen standart popup
  const [showSquadCompletePopup, setShowSquadCompletePopup] = useState<string | null>(null);
  
  // ✅ Track if defense confirmation was already shown
  const [defenseConfirmShown, setDefenseConfirmShown] = useState(false);
  
  // ✅ State restore edildi mi?
  const [stateRestored, setStateRestored] = useState(false);
  
  // ✅ Kullanıcı tahmin yapmış mı? (storage'da isCompleted === true)
  // ⚠️ Bu state useEffect'lerde kullanıldığı için erken tanımlanmalı
  const [hasPrediction, setHasPrediction] = React.useState(false);
  // ✅ Storage kontrolü yapılmadan "Kadro oluşturulmadı" gösterme (kısa süreli yanıp sönmeyi önler)
  const [hasCheckedSquadStorage, setHasCheckedSquadStorage] = React.useState(false);
  
  // ✅ Kullanıcı tercih istatistikleri (tahmin yapmayan kullanıcılar için)
  const [userPreferenceStats, setUserPreferenceStats] = useState<UserPreferenceStats | null>(null);
  const [autoGeneratedSquad, setAutoGeneratedSquad] = useState<AutoGeneratedSquad | null>(null);
  // ✅ autoGeneratedSquad uygulandı mı? (runRestore'dan önce tanımlanmalı)
  const [autoSquadApplied, setAutoSquadApplied] = React.useState(false);
  const [showPreferencePopup, setShowPreferencePopup] = useState<{
    position: string;
    positionLabel: string;
    player: { id: number; name: string; number: number; stats?: { pace?: number; shooting?: number; passing?: number; dribbling?: number; defending?: number; physical?: number }; rating?: number | null };
    preferencePercentage: number;
    totalPreferences: number;
    allPreferences: { playerId: number; playerName: string; percentage: number; count: number }[];
  } | null>(null);
  // ✅ Oyuncu reyting dropdown açık/kapalı durumu
  const [showPlayerRatingDropdown, setShowPlayerRatingDropdown] = useState(false);
  // ✅ Tam tercih özeti popup'ı (tüm pozisyonlar + formasyonlar)
  const [showFullPreferenceSummary, setShowFullPreferenceSummary] = useState(false);
  // ✅ Formasyon detay popup'ı (atak veya defans)
  const [showFormationDetailPopup, setShowFormationDetailPopup] = useState<'attack' | 'defense' | null>(null);
  // ✅ İlk 11 popup'ı (maç başladığında gösterilir)
  const [showStartingXIPopup, setShowStartingXIPopup] = useState(false);
  const [startingXIPlayers, setStartingXIPlayers] = useState<any[]>([]);
  // ✅ "Tahminlerinizi Tamamlayın" popup'ı kaldırıldı
  // Artık kullanıcılar maç başladıktan sonra sayfayı açtıklarında normal "İlk 11" popup'ı görecek
  
  // ✅ Atak formasyonundaki oyuncu değişikliklerini takip et (defans formasyonu yönetimi için)
  const previousAttackPlayersRef = React.useRef<Record<number, any | null>>({});
  const attackPlayersInitializedRef = React.useRef(false);
  
  // ✅ Sadece oynanan/canlı maçta kadro kilitli (MatchDetail'den gelen prop öncelikli)
  const isMatchLive = React.useMemo(() => {
    if (typeof isMatchLiveProp === 'boolean') return isMatchLiveProp;
    const status = matchData?.fixture?.status?.short || matchData?.status || '';
    const liveStatuses = ['1H', '2H', 'HT', 'ET', 'BT', 'P', 'LIVE'];
    return liveStatuses.includes(status);
  }, [isMatchLiveProp, matchData]);

  // ✅ TOPLULUK VERİLERİ GÖRÜNÜRLüK KONTROLÜ
  // Kural: Topluluk verileri SADECE tahmin kaydedildikten sonra görünür
  // Maç canlı veya bitmiş olması yeterli DEĞİL - kullanıcı kendi tahminini kaydetmiş olmalı
  const communityDataVisible = hasPrediction === true;

  // ✅ YENİ KURAL: Kadro sekmesi maç başladığında kilitlenir (120 sn kuralı kaldırıldı)
  // Maç canlı veya bitmişse kadro düzenlenemez
  const isKadroLocked = isMatchLive || isMatchFinished;
  const showKadroLockedToast = () => showInfo(t('matchSquad.squadLocked'), t('matchSquad.squadLockedMessage'));

  // ✅ Kaydedilmemiş değişiklikleri takip et - state'ler aşağıda tanımlandıktan sonra useEffect ile kullanılacak
  const [hasUnsavedChanges, setHasUnsavedChanges] = React.useState(false);
  // ✅ Kilit açıldıktan sonra gerçekten değişiklik yapıldı mı?
  const [hasModifiedSinceUnlock, setHasModifiedSinceUnlock] = React.useState(false);

  // ✅ İlk 11 popup kapanınca - useEffect otomatik olarak oyuncuları yerleştirecek
  const handleStartingXIPopupClose = React.useCallback(() => {
    // Popup'ı kapat
    setShowStartingXIPopup(false);
    console.log('✅ İlk 11 popup kapatıldı');
  }, []);

  // ✅ Mount ve Kadro sekmesi görünür olduğunda AsyncStorage'dan yükle (Tahmin'den geri dönünce kadro görünsün)
  // ✅ Tamamla basılmadan geri dönüldüysa (isCompleted !== true): HİÇBİR ŞEY yükleme - formasyon seçiminden başla
  // ✅ Kullanıcı "Tamamla" butonu ile kadroyu tamamlayana kadar formasyon ve oyuncular gösterilmez
  const runRestore = React.useCallback(async () => {
    if (isMatchFinished) {
      setStateRestored(true);
      return;
    }
    
    // ✅ autoSquadApplied ise restore'u atla - oyuncular zaten autoGeneratedSquad ile yerleştirilmiş
    if (autoSquadApplied) {
      setStateRestored(true);
      return;
    }
    
    try {
      const key = squadStorageKey;
      const raw = await AsyncStorage.getItem(key);
      if (raw) {
        const parsed = JSON.parse(raw);
        const isCompleted = parsed.isCompleted === true;
        const isAutoApplied = parsed.isAutoApplied === true;
        const liveStatuses = ['1H', '2H', 'HT', 'ET', 'BT', 'P', 'LIVE'];
        const isLive = liveStatuses.includes(matchData?.fixture?.status?.short || matchData?.status || '');

        // ✅ "Tamamla" basıldıysa: Her zaman yükle (maç başlamamış olsa bile)
        // ✅ Canlı maçta auto-apply yapıldıysa da yükle
        // ✅ Tamamla basılmadıysa VE maç başlamamışsa: HİÇBİR veri yükleme - empty state göster
        if (isCompleted || (isLive && isAutoApplied)) {
          // ✅ Tamamla basıldıysa veya canlı maçta auto-apply yapıldıysa: Yükle (slot sırası kesin)
          if (parsed.attackFormation) {
            const raw = String(parsed.attackFormation).trim();
            setAttackFormation(formations.find(f => f.id === raw)?.id ?? raw);
          }
          // ✅ Atak kadrosunu slot sırasına göre kur (kayıtta karışmasın)
          if (parsed.attackPlayersBySlot && Array.isArray(parsed.attackPlayersBySlot)) {
            const ordered: Record<number, any> = {};
            for (let i = 0; i < 11; i++) {
              const e = parsed.attackPlayersBySlot.find((x: { slot: number }) => x.slot === i);
              if (e?.player) ordered[i] = e.player;
            }
            setAttackPlayers(ordered);
          } else if (parsed.attackPlayersArray && Array.isArray(parsed.attackPlayersArray)) {
            const ordered: Record<number, any> = {};
            for (let i = 0; i < 11; i++) {
              const p = parsed.attackPlayersArray[i];
              if (p) ordered[i] = p;
            }
            setAttackPlayers(ordered);
          } else if (parsed.attackPlayers && typeof parsed.attackPlayers === 'object') {
            const ordered: Record<number, any> = {};
            for (let i = 0; i < 11; i++) {
              const p = parsed.attackPlayers[i] ?? parsed.attackPlayers[String(i)];
              if (p) ordered[i] = p;
            }
            setAttackPlayers(ordered);
          }
          if (parsed.defenseFormation) {
            const rawDef = String(parsed.defenseFormation).trim();
            setDefenseFormation(formations.find(f => f.id === rawDef)?.id ?? rawDef);
          }
          // ✅ Defans kadrosunu slot sırasına göre kur
          if (parsed.defensePlayersArray && Array.isArray(parsed.defensePlayersArray)) {
            const ordered: Record<number, any> = {};
            for (let i = 0; i < 11; i++) {
              const p = parsed.defensePlayersArray[i];
              if (p) ordered[i] = p;
            }
            setDefensePlayers(ordered);
          } else if (parsed.defensePlayers && typeof parsed.defensePlayers === 'object') {
            const ordered: Record<number, any> = {};
            for (let i = 0; i < 11; i++) {
              const p = parsed.defensePlayers[i] ?? parsed.defensePlayers[String(i)];
              if (p) ordered[i] = p;
            }
            setDefensePlayers(ordered);
          }
          // editingMode'u restore ET AMA sadece sayfa ilk yüklenirken
          if (parsed.editingMode && !stateRestored) setEditingMode(parsed.editingMode);
          setDefenseConfirmShown(parsed.defenseFormation ? true : (parsed.defenseConfirmShown || false));
          // ✅ Tamamlanmış kadro kilitli olarak gelir (sadece isCompleted true ise)
          setIsSquadLocked(isCompleted);
        } else if (!isMatchLive) {
          // ✅ Maç başlamamış VE Tamamla basılmamışsa: HİÇBİR veri yükleme - empty state göster
          setAttackFormation(null);
          setAttackPlayers({});
          setDefenseFormation(null);
          setDefensePlayers({});
          if (!stateRestored) setEditingMode('attack');
          setDefenseConfirmShown(false);
        } else {
          // ✅ Canlı maç ama Tamamla basılmamış ve auto-apply yapılmamış: Sıfırdan başla
          setAttackFormation(null);
          setAttackPlayers({});
          setDefenseFormation(null);
          setDefensePlayers({});
          if (!stateRestored) setEditingMode('attack');
          setDefenseConfirmShown(false);
        }
      } else {
        // ✅ Storage'da veri yoksa: formasyon null - empty state göster
        setAttackFormation(null);
        setAttackPlayers({});
        setDefenseFormation(null);
        setDefensePlayers({});
        if (!stateRestored) setEditingMode('attack');
        setDefenseConfirmShown(false);
      }
    } catch (e) {
      console.warn('State restore failed', e);
      // Hata durumunda da formasyon null yap
      setAttackFormation(null);
      setAttackPlayers({});
      setDefenseFormation(null);
      setDefensePlayers({});
      if (!stateRestored) setEditingMode('attack');
      setDefenseConfirmShown(false);
    }
    setStateRestored(true);
  }, [squadStorageKey, matchData, isMatchFinished, stateRestored, autoSquadApplied, isMatchLive]);

  // ✅ MAÇ CANLI VE FORMASYON SEÇİLMEMİŞSE: En popüler formasyonu otomatik uygula
  const [autoFormationApplied, setAutoFormationApplied] = React.useState(false);
  // ✅ BİTEN MAÇ: İlk 11'i en çok tercih edilen formasyona göre bir kez yerleştir
  const [finishedFormationApplied, setFinishedFormationApplied] = React.useState(false);
  
  React.useEffect(() => {
    // Sadece maç canlıysa, restore tamamlandıysa, formasyon yoksa ve henüz auto-apply yapılmadıysa
    if (!isMatchLive || !stateRestored || attackFormation || autoFormationApplied) return;
    if (attackTeamPlayers.length === 0) return; // Oyuncular yüklenmeden bekle
    // ✅ Tahmin yapılmamış kullanıcılar için bu useEffect çalışmasın
    // handleStartingXIPopupClose zaten topluluk verilerine göre formasyon uygulayacak
    if (!hasPrediction) return;
    
    const applyPopularFormation = async () => {
      try {
        console.log('🎯 Maç canlı - en popüler formasyon otomatik uygulanıyor...');
        console.log('📋 Mevcut oyuncular:', attackTeamPlayers.length);
        
        // Mock 999999: Kullanıcıların çoğu atak 3-5-2, defans 3-6-1 seçmiş (farklı olmalı)
        let popularFormationId = '4-3-3';
        let popularDefenseFormationId = '4-3-3';
        {
          try {
            const popularRes = await squadPredictionsApi.getPopularFormations('attack');
            if (popularRes.success && popularRes.data && popularRes.data.length > 0) {
              popularFormationId = popularRes.data[0].formation;
            }
            const defRes = await squadPredictionsApi.getPopularFormations('defense');
            if (defRes?.success && defRes.data?.length > 0) {
              popularDefenseFormationId = defRes.data[0].formation;
            }
          } catch (apiErr) {
            console.log('⚠️ Popular formations API failed, using default');
          }
          // Kullanıcıların çoğu atak ve defansı aynı yapmışsa her iki formasyon da aynı görünür
        }
        console.log('📊 En popüler formasyon (atak/defans):', popularFormationId, popularDefenseFormationId);
        
        const formation = formations.find(f => f.id === popularFormationId) || formations.find(f => f.id === '4-3-3');
        if (!formation) {
          console.log('❌ Formation not found:', popularFormationId);
          return;
        }
        
        // Formasyonu uygula
        setAttackFormation(formation.id);
        
        // Oyuncuları pozisyonlara otomatik yerleştir
        const autoPlayers: Record<number, any | null> = {};
        const usedPlayerIds = new Set<number>();
        
        // Her slot için en uygun oyuncuyu bul
        // ✅ FIX: formation.positions sadece string array (e.g., ['GK', 'CB', 'CB', ...])
        formation.positions.forEach((positionType: string, slotIndex: number) => {
          const slotPos = (positionType || '').toUpperCase();
          
          // Bu pozisyon için uygun oyuncuları filtrele
          let candidates = attackTeamPlayers.filter((p: any) => {
            if (usedPlayerIds.has(p.id)) return false;
            
            const playerPos = (p.position || p.pos || '').toUpperCase();
            
            // Kaleci sadece kaleci slotuna (slot 0 ve pozisyon GK)
            if (slotPos === 'GK' || slotPos === 'G') {
              return isGoalkeeperPlayer(p);
            }
            // Kaleci olmayan slotlara kaleci konamaz
            if (isGoalkeeperPlayer(p)) return false;
            
            // Pozisyon eşleştirme - slot pozisyonuna göre
            // Defans: CB, LB, RB, LWB, RWB
            if (['CB', 'LB', 'RB', 'LWB', 'RWB'].includes(slotPos)) {
              return playerPos.includes('D') || playerPos.includes('DEF') || playerPos.includes('BACK') || playerPos === 'CB' || playerPos === 'LB' || playerPos === 'RB';
            }
            // Orta saha: CM, CDM, CAM, LM, RM, DM, AM
            if (['CM', 'CDM', 'CAM', 'LM', 'RM', 'DM', 'AM'].includes(slotPos)) {
              return playerPos.includes('M') || playerPos.includes('MID') || playerPos === 'CM' || playerPos === 'CDM' || playerPos === 'CAM';
            }
            // Forvet: ST, LW, RW, CF
            if (['ST', 'LW', 'RW', 'CF'].includes(slotPos)) {
              return playerPos.includes('F') || playerPos.includes('ATT') || playerPos.includes('ST') || playerPos.includes('W') || playerPos === 'LW' || playerPos === 'RW';
            }
            
            return true; // Eşleşme yoksa herkes aday
          });
          
          // Rating'e göre sırala ve en iyiyi seç
          candidates.sort((a: any, b: any) => (b.rating || 75) - (a.rating || 75));
          
          if (candidates.length > 0) {
            autoPlayers[slotIndex] = candidates[0];
            usedPlayerIds.add(candidates[0].id);
            console.log(`✅ Slot ${slotIndex} (${slotPos}): ${candidates[0].name}`);
          } else {
            // Eşleşen oyuncu bulunamadıysa, kalan herhangi bir oyuncuyu ata
            const remaining = attackTeamPlayers.filter((p: any) => !usedPlayerIds.has(p.id) && !isGoalkeeperPlayer(p));
            if (remaining.length > 0) {
              remaining.sort((a: any, b: any) => (b.rating || 75) - (a.rating || 75));
              autoPlayers[slotIndex] = remaining[0];
              usedPlayerIds.add(remaining[0].id);
              console.log(`⚠️ Slot ${slotIndex} (${slotPos}): Fallback -> ${remaining[0].name}`);
            }
          }
        });
        
        setAttackFormation(formation.id);
        setAttackPlayers(autoPlayers);
        const defenseForm = formations.find(f => f.id === popularDefenseFormationId) || formation;
        const defenseAutoPlayers: Record<number, any | null> = {};
        const defUsed = new Set<number>();
        (defenseForm.positions || formation.positions).forEach((positionType: string, slotIndex: number) => {
          const slotPos = (positionType || '').toUpperCase();
          let candidates = attackTeamPlayers.filter((p: any) => {
            if (defUsed.has(p.id)) return false;
            const playerPos = (p.position || p.pos || '').toUpperCase();
            if (slotPos === 'GK' || slotPos === 'G') return isGoalkeeperPlayer(p);
            if (isGoalkeeperPlayer(p)) return false;
            if (['CB', 'LB', 'RB', 'LWB', 'RWB'].includes(slotPos)) return playerPos.includes('D') || playerPos.includes('DEF') || playerPos === 'CB' || playerPos === 'LB' || playerPos === 'RB';
            if (['CM', 'CDM', 'CAM', 'LM', 'RM', 'DM', 'AM'].includes(slotPos)) return playerPos.includes('M') || playerPos === 'CM' || playerPos === 'CDM' || playerPos === 'CAM';
            if (['ST', 'LW', 'RW', 'CF'].includes(slotPos)) return playerPos.includes('F') || playerPos.includes('ST') || playerPos.includes('W') || playerPos === 'LW' || playerPos === 'RW';
            return true;
          });
          candidates.sort((a: any, b: any) => (b.rating || 75) - (a.rating || 75));
          if (candidates.length > 0) {
            defenseAutoPlayers[slotIndex] = candidates[0];
            defUsed.add(candidates[0].id);
          } else {
            const remaining = attackTeamPlayers.filter((p: any) => !defUsed.has(p.id) && !isGoalkeeperPlayer(p));
            if (remaining.length > 0) {
              remaining.sort((a: any, b: any) => (b.rating || 75) - (a.rating || 75));
              defenseAutoPlayers[slotIndex] = remaining[0];
              defUsed.add(remaining[0].id);
            }
          }
        });
        setDefenseFormation(defenseForm.id);
        setDefensePlayers(defenseAutoPlayers);
        setAutoFormationApplied(true);
        
        const key = squadStorageKey;
        const raw = await AsyncStorage.getItem(key);
        const existing = raw ? JSON.parse(raw) : {};
        await AsyncStorage.setItem(key, JSON.stringify({
          ...existing,
          matchId,
          attackFormation: formation.id,
          attackPlayers: autoPlayers,
          defenseFormation: defenseForm.id,
          defensePlayers: defenseAutoPlayers,
          isAutoApplied: true,
        }));
        
        console.log('✅ Popüler formasyon ve kadro otomatik uygulandı (Atak:', formation.id, 'Defans:', defenseForm.id, ')');
      } catch (err) {
        console.error('❌ Auto formation apply error:', err);
      }
    };
    
    // Kısa bir gecikme ile uygula (oyuncuların yüklenmesini bekle)
    const timer = setTimeout(applyPopularFormation, 500);
    return () => clearTimeout(timer);
  }, [isMatchLive, stateRestored, attackFormation, autoFormationApplied, attackTeamPlayers, squadStorageKey, matchId, hasPrediction]);

  // ✅ "Tahminlerinizi Tamamlayın" popup'ı kaldırıldı
  // Maç başladıktan sonra sayfayı açanlar için normal "İlk 11" popup'ı gösterilir
  // Kullanıcı popup'ı kapatınca kadro sekmesinde çalışmaya devam edebilir
  // İsterse tahmin, canlı, istatistik sekmesine geçebilir

  // ✅ İlk 11 popup'ı zaten gösterildi mi? (tekrar açılmasını önlemek için)
  // ⚠️ Parent component'ten gelen prop kullanılıyor - sekme değişse bile korunur
  // Local ref sadece bu session içindeki durumu takip eder
  const localPopupShownRef = React.useRef(false);
  
  // ✅ KULLANICI TERCİH İSTATİSTİKLERİNİ YÜKLE ve İLK 11 POPUP'INI GÖSTER
  // Maç canlı veya bitmişse ve kullanıcı tahmin yapmamışsa, otomatik kadro oluştur
  // ✅ Sekme değişince MatchSquad yeniden mount olur; popup zaten gösterildiyse (startingXIPopupShown)
  //    sadece veriyi yükle (autoGeneratedSquad, startingXIPlayers) – popup'ı tekrar açma, sahanın altı boş kalmasın
  React.useEffect(() => {
    if (!(isMatchLive || isMatchFinished) || !stateRestored) return;
    // ✅ hasPrediction kontrolü kaldırıldı - istatistikler her zaman yüklenecek
    if (showStartingXIPopup) return; // Popup zaten gösteriliyor, tekrar yükleme
    // ✅ Zaten yüklenmişse tekrar yükleme (sonsuz döngü önleme)
    if (autoGeneratedSquad && userPreferenceStats) return;
    // ✅ Mock 999999: Tahmin yapılmamış senaryosu – otomatik kadro/ilk 11 popup yok, kadro sekmesi boş kalır
    const fixtureId = Number(matchId);
    if (isMockLive999999(fixtureId)) return;
    const isMockMatch = fixtureId === MOCK_MATCH_IDS.REAL_BARCA || fixtureId === MOCK_MATCH_IDS.GS_FB || fixtureId === MOCK_MATCH_IDS.TEST_6H || fixtureId === MOCK_MATCH_IDS.TEST_1H;
    
    // Kullanıcının takımını bul (predictionTeamId veya favoriteTeamIds'den)
    const userTeamId = predictionTeamId || (isMockMatch ? getMockUserTeamId(fixtureId) : null) || (favoriteTeamIds.length > 0 ? favoriteTeamIds[0] : null);
    
    if (!userTeamId) return;
    
    // İlk 11 oyuncuları al (lineups'tan) - API'den çekilmiş olmalı
    const teamLineup = lineups?.find((l: any) => l.team?.id === userTeamId);
    if (!teamLineup?.startXI || teamLineup.startXI.length === 0) {
      console.log('⚠️ İlk 11 henüz yüklenmedi, bekleniyor...');
      return; // Lineups henüz yüklenmemiş, bekleyelim
    }
    
    // İlk 11'i forma numarasına göre sırala (kaleci en üstte)
    const sortedStartXI = [...teamLineup.startXI].sort((a: any, b: any) => {
      const aNum = a.player?.number || 99;
      const bNum = b.player?.number || 99;
      const aIsGK = isGoalkeeperPlayer(a.player);
      const bIsGK = isGoalkeeperPlayer(b.player);
      
      // Kaleci her zaman en üstte
      if (aIsGK && !bIsGK) return -1;
      if (!aIsGK && bIsGK) return 1;
      
      // Diğerleri forma numarasına göre
      return aNum - bNum;
    });
    
    setStartingXIPlayers(sortedStartXI);
    
    // Tercih istatistiklerini yükle (mock veya gerçek maç)
    if (isMockMatch) {
      const prefs = getUserPreferenceStats(fixtureId, userTeamId);
      if (prefs) {
        setUserPreferenceStats(prefs);
        console.log('📊 Kullanıcı tercih istatistikleri yüklendi:', prefs.teamName, '- Toplam:', prefs.totalUsers, 'kullanıcı');
      }
    } else {
      // ✅ Gerçek maçlar için de varsayılan tercih istatistikleri oluştur
      const teamName = teamLineup?.team?.name || matchData?.homeTeam?.name || 'Takım';
      const defaultPrefs: UserPreferenceStats = {
        teamName,
        totalUsers: Math.floor(Math.random() * 5000) + 1000,
        attackFormation: {
          topFormation: '4-3-3',
          percentage: 45,
          stats: [
            { formation: '4-3-3', count: 2250, percentage: 45 },
            { formation: '4-2-3-1', count: 1500, percentage: 30 },
            { formation: '4-4-2', count: 750, percentage: 15 },
            { formation: '3-5-2', count: 500, percentage: 10 },
          ],
        },
        defenseFormation: {
          topFormation: '4-4-2',
          percentage: 40,
          stats: [
            { formation: '4-4-2', count: 2000, percentage: 40 },
            { formation: '4-3-3', count: 1500, percentage: 30 },
            { formation: '5-3-2', count: 1000, percentage: 20 },
            { formation: '5-4-1', count: 500, percentage: 10 },
          ],
        },
        positions: {},
      };
      setUserPreferenceStats(defaultPrefs);
      console.log('📊 Gerçek maç - Varsayılan tercih istatistikleri oluşturuldu:', teamName);
    }
    
    // Popup'ı sadece ilk kez göster; sekme değişip geri gelince tekrar açma
    const popupAlreadyShown = startingXIPopupShown || localPopupShownRef.current;
    if (!popupAlreadyShown) {
      setShowStartingXIPopup(true);
      localPopupShownRef.current = true;
      onStartingXIPopupShown?.();
    }
    
    const startXI = sortedStartXI.map((p: any) => ({
      player: {
        id: p.player?.id || 0,
        name: p.player?.name || '',
        number: p.player?.number ?? p.player?.shirt_number ?? p.player?.jersey_number ?? 0,
        pos: p.player?.pos || p.player?.position || '',
      },
    }));
    
    // Otomatik kadro oluştur (mock veya gerçek maç)
    if (isMockMatch) {
      const autoSquad = generateAutoSquad(fixtureId, userTeamId, startXI);
      if (autoSquad) {
        setAutoGeneratedSquad(autoSquad);
        console.log('🎯 Otomatik kadro oluşturuldu:', autoSquad.teamName);
        console.log('   Atak:', autoSquad.attackFormation.formation, '(' + autoSquad.attackFormation.percentage + '%)');
        console.log('   Defans:', autoSquad.defenseFormation.formation, '(' + autoSquad.defenseFormation.percentage + '%)');
      }
    } else {
      // ✅ Gerçek maçlar için de varsayılan otomatik kadro oluştur
      const teamName = teamLineup?.team?.name || matchData?.homeTeam?.name || 'Takım';
      const defaultAutoSquad: AutoGeneratedSquad = {
        teamName,
        attackFormation: { formation: '4-3-3', percentage: 45 },
        defenseFormation: { formation: '4-4-2', percentage: 40 },
        players: startXI.map(p => ({
          id: p.player.id,
          name: p.player.name,
          position: p.player.pos,
          rating: Math.floor(Math.random() * 15) + 75,
        })),
      };
      setAutoGeneratedSquad(defaultAutoSquad);
      console.log('🎯 Gerçek maç - Varsayılan otomatik kadro oluşturuldu:', teamName);
    }
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [isMatchLive, isMatchFinished, stateRestored, hasPrediction, matchId, predictionTeamId, favoriteTeamIds, lineups, startingXIPopupShown]);

  // ✅ autoGeneratedSquad set edildiğinde formasyonu ve oyuncuları otomatik uygula
  // Popup kapatılmadan sekme değiştirilirse de çalışır
  React.useEffect(() => {
    // Canlı veya biten maç, tahmin yapılmamış, autoGeneratedSquad var ve henüz uygulanmamışsa
    if (!(isMatchLive || isMatchFinished) || hasPrediction || !autoGeneratedSquad || autoSquadApplied) return;
    if (!startingXIPlayers || startingXIPlayers.length === 0) return;
    
    console.log('🔄 AutoGeneratedSquad otomatik uygulanıyor...');
    
    // Topluluk verilerine göre formasyonları uygula
    const attackForm = formations.find(f => f.id === autoGeneratedSquad.attackFormation.formation) || formations[0];
    const defenseForm = formations.find(f => f.id === autoGeneratedSquad.defenseFormation.formation) || formations[0];
    
    if (!attackForm || !defenseForm) {
      console.log('⚠️ Formasyon bulunamadı');
      return;
    }
    
    setAttackFormation(attackForm.id);
    setDefenseFormation(defenseForm.id);
    
    // Pozisyon mapping oluştur - LM ve RM ayrı olarak eklendi
    const positionToSlotMap: Record<string, number[]> = {
      'GK': [], 'RB': [], 'CB1': [], 'CB2': [], 'LB': [],
      'CM1': [], 'CM2': [], 'CM3': [], 'AM': [], 'LW': [], 'LM': [], 'ST': [], 'RW': [], 'RM': [],
    };
    
    attackForm.positions.forEach((pos: string, slotIdx: number) => {
      const normalized = normalizePosition(pos);
      if (normalized === 'GK') positionToSlotMap['GK'].push(slotIdx);
      else if (normalized.includes('RB') || normalized.includes('RWB')) positionToSlotMap['RB'].push(slotIdx);
      else if (normalized.includes('CB')) {
        if (positionToSlotMap['CB1'].length === 0) positionToSlotMap['CB1'].push(slotIdx);
        else positionToSlotMap['CB2'].push(slotIdx);
      }
      else if (normalized.includes('LB') || normalized.includes('LWB')) positionToSlotMap['LB'].push(slotIdx);
      else if (normalized === 'AM' || normalized === 'CAM' || normalized === 'ACM') {
        positionToSlotMap['AM'].push(slotIdx);
      }
      else if (normalized.includes('CM') || normalized.includes('CDM')) {
        if (positionToSlotMap['CM1'].length === 0) positionToSlotMap['CM1'].push(slotIdx);
        else if (positionToSlotMap['CM2'].length === 0) positionToSlotMap['CM2'].push(slotIdx);
        else positionToSlotMap['CM3'].push(slotIdx);
      }
      else if (normalized === 'LM') positionToSlotMap['LM'].push(slotIdx);
      else if (normalized === 'RM') positionToSlotMap['RM'].push(slotIdx);
      else if (normalized.includes('LW')) positionToSlotMap['LW'].push(slotIdx);
      else if (normalized.includes('ST') || normalized.includes('CF')) positionToSlotMap['ST'].push(slotIdx);
      else if (normalized.includes('RW')) positionToSlotMap['RW'].push(slotIdx);
    });
    
    // Defans için ayrı slot map - LM ve RM ayrı olarak eklendi
    const defensePositionToSlotMap: Record<string, number[]> = {
      'GK': [], 'RB': [], 'CB1': [], 'CB2': [], 'CB3': [], 'LB': [],
      'CM1': [], 'CM2': [], 'CM3': [], 'CM4': [], 'CM5': [], 'AM': [], 'DM': [],
      'LW': [], 'LM': [], 'ST': [], 'RW': [], 'RM': [],
    };
    
    defenseForm.positions.forEach((pos: string, slotIdx: number) => {
      const normalized = normalizePosition(pos);
      if (normalized === 'GK') defensePositionToSlotMap['GK'].push(slotIdx);
      else if (normalized.includes('RB') || normalized.includes('RWB')) defensePositionToSlotMap['RB'].push(slotIdx);
      else if (normalized.includes('CB')) {
        if (defensePositionToSlotMap['CB1'].length === 0) defensePositionToSlotMap['CB1'].push(slotIdx);
        else if (defensePositionToSlotMap['CB2'].length === 0) defensePositionToSlotMap['CB2'].push(slotIdx);
        else defensePositionToSlotMap['CB3'].push(slotIdx);
      }
      else if (normalized.includes('LB') || normalized.includes('LWB')) defensePositionToSlotMap['LB'].push(slotIdx);
      else if (normalized === 'AM' || normalized === 'CAM' || normalized === 'ACM') {
        defensePositionToSlotMap['AM'].push(slotIdx);
      }
      else if (normalized === 'DM' || normalized === 'CDM') {
        defensePositionToSlotMap['DM'].push(slotIdx);
      }
      else if (normalized.includes('CM')) {
        if (defensePositionToSlotMap['CM1'].length === 0) defensePositionToSlotMap['CM1'].push(slotIdx);
        else if (defensePositionToSlotMap['CM2'].length === 0) defensePositionToSlotMap['CM2'].push(slotIdx);
        else if (defensePositionToSlotMap['CM3'].length === 0) defensePositionToSlotMap['CM3'].push(slotIdx);
        else if (defensePositionToSlotMap['CM4'].length === 0) defensePositionToSlotMap['CM4'].push(slotIdx);
        else defensePositionToSlotMap['CM5'].push(slotIdx);
      }
      else if (normalized === 'LM') defensePositionToSlotMap['LM'].push(slotIdx);
      else if (normalized === 'RM') defensePositionToSlotMap['RM'].push(slotIdx);
      else if (normalized.includes('LW')) defensePositionToSlotMap['LW'].push(slotIdx);
      else if (normalized.includes('ST') || normalized.includes('CF')) defensePositionToSlotMap['ST'].push(slotIdx);
      else if (normalized.includes('RW')) defensePositionToSlotMap['RW'].push(slotIdx);
    });
    
    // Pozisyon mapping helper - LM ve RM ayrı olarak eklendi
    const getAttackMapKeys = (position: string): string[] => {
      const p = position.toUpperCase();
      if (p === 'GK') return ['GK'];
      if (p === 'RB' || p === 'RWB') return ['RB'];
      if (p === 'LB' || p === 'LWB') return ['LB'];
      if (p === 'CB' || p === 'CB1') return ['CB1', 'CB2'];
      if (p === 'CB2') return ['CB2', 'CB1'];
      if (p === 'CM' || p === 'CDM' || p === 'DM' || p === 'CM1') return ['CM1', 'CM2', 'CM3'];
      if (p === 'CM2') return ['CM2', 'CM1', 'CM3'];
      if (p === 'CM3') return ['CM3', 'CM1', 'CM2'];
      if (p === 'AM' || p === 'CAM' || p === 'ACM') return ['AM'];
      if (p === 'LM') return ['LM', 'LW', 'CM1', 'CM2'];
      if (p === 'RM') return ['RM', 'RW', 'CM1', 'CM2'];
      if (p === 'LW') return ['LW', 'LM', 'CM1'];
      if (p === 'RW') return ['RW', 'RM', 'CM1'];
      if (p === 'ST' || p === 'CF') return ['ST'];
      return [position];
    };
    
    const getDefenseMapKeys = (position: string): string[] => {
      const p = position.toUpperCase();
      if (p === 'GK') return ['GK'];
      if (p === 'RB' || p === 'RWB') return ['RB'];
      if (p === 'LB' || p === 'LWB') return ['LB'];
      if (p === 'CB' || p === 'CB1') return ['CB1', 'CB2', 'CB3'];
      if (p === 'CB2') return ['CB2', 'CB1', 'CB3'];
      if (p === 'CB3') return ['CB3', 'CB1', 'CB2'];
      if (p === 'CM' || p === 'CDM' || p === 'DM' || p === 'CM1') return ['CM1', 'CM2', 'CM3', 'CM4', 'CM5', 'DM'];
      if (p === 'CM2') return ['CM2', 'CM1', 'CM3', 'CM4', 'CM5', 'DM'];
      if (p === 'CM3') return ['CM3', 'CM1', 'CM2', 'CM4', 'CM5', 'DM'];
      if (p === 'AM' || p === 'CAM' || p === 'ACM') return ['AM'];
      if (p === 'LM') return ['LM', 'LW', 'CM1', 'CM2', 'CM3'];
      if (p === 'RM') return ['RM', 'RW', 'CM1', 'CM2', 'CM3'];
      if (p === 'LW') return ['LW', 'LM', 'CM1', 'CM2'];
      if (p === 'RW') return ['RW', 'RM', 'CM1', 'CM2'];
      if (p === 'ST' || p === 'CF') return ['ST'];
      return [position];
    };
    
    // Oyuncuları pozisyonlara yerleştir
    const attackPlayersMap: Record<number, any> = {};
    const defensePlayersMap: Record<number, any> = {};
    const usedAttackSlots = new Set<number>();
    const usedDefenseSlots = new Set<number>();
    
    autoGeneratedSquad.positions.forEach((pos) => {
      const player = startingXIPlayers.find((p: any) => p.player?.id === pos.player.id);
      if (!player) return;
      
      // Atak slotu bul
      const attackKeys = getAttackMapKeys(pos.position);
      for (const key of attackKeys) {
        const slots = positionToSlotMap[key] || [];
        const availableSlot = slots.find(s => !usedAttackSlots.has(s));
        if (availableSlot !== undefined) {
          usedAttackSlots.add(availableSlot);
          attackPlayersMap[availableSlot] = {
            ...player.player,
            rating: player.player?.rating || 75,
            position: pos.position,
          };
          break;
        }
      }
      
      // Defans slotu bul
      const defenseKeys = getDefenseMapKeys(pos.position);
      for (const key of defenseKeys) {
        const defSlots = defensePositionToSlotMap[key] || [];
        const availableDefSlot = defSlots.find(s => !usedDefenseSlots.has(s));
        if (availableDefSlot !== undefined) {
          usedDefenseSlots.add(availableDefSlot);
          defensePlayersMap[availableDefSlot] = {
            ...player.player,
            rating: player.player?.rating || 75,
            position: pos.position,
          };
          break;
        }
      }
    });
    
    console.log('📋 Auto atak slotları:', Object.keys(attackPlayersMap).length, '/ 11');
    console.log('📋 Auto defans slotları:', Object.keys(defensePlayersMap).length, '/ 11');
    
    // ✅ Eksik slotları doldur - yerleşemeyen oyuncuları boş slotlara yerleştir
    if (Object.keys(defensePlayersMap).length < 11) {
      const allDefenseSlots = Array.from({ length: 11 }, (_, i) => i);
      const emptyDefenseSlots = allDefenseSlots.filter(s => !usedDefenseSlots.has(s));
      const unplacedPlayers = startingXIPlayers.filter((p: any) => 
        !Object.values(defensePlayersMap).some((dp: any) => dp?.id === p.player?.id)
      );
      
      console.log('⚠️ Eksik defans slotları:', emptyDefenseSlots);
      console.log('⚠️ Yerleşemeyen oyuncular:', unplacedPlayers.map((p: any) => p.player?.name));
      
      emptyDefenseSlots.forEach((slot, idx) => {
        if (unplacedPlayers[idx]) {
          defensePlayersMap[slot] = {
            ...unplacedPlayers[idx].player,
            rating: unplacedPlayers[idx].player?.rating || 75,
            position: unplacedPlayers[idx].player?.pos || 'SUB',
          };
          console.log('✅ Eksik slot dolduruldu:', slot, '→', unplacedPlayers[idx].player?.name);
        }
      });
    }
    
    // ✅ Atak için de aynı kontrol
    if (Object.keys(attackPlayersMap).length < 11) {
      const allAttackSlots = Array.from({ length: 11 }, (_, i) => i);
      const emptyAttackSlots = allAttackSlots.filter(s => !usedAttackSlots.has(s));
      const unplacedAttackPlayers = startingXIPlayers.filter((p: any) => 
        !Object.values(attackPlayersMap).some((ap: any) => ap?.id === p.player?.id)
      );
      
      emptyAttackSlots.forEach((slot, idx) => {
        if (unplacedAttackPlayers[idx]) {
          attackPlayersMap[slot] = {
            ...unplacedAttackPlayers[idx].player,
            rating: unplacedAttackPlayers[idx].player?.rating || 75,
            position: unplacedAttackPlayers[idx].player?.pos || 'SUB',
          };
        }
      });
    }
    
    console.log('📋 Final atak slotları:', Object.keys(attackPlayersMap).length, '/ 11');
    console.log('📋 Final defans slotları:', Object.keys(defensePlayersMap).length, '/ 11');
    
    setAttackPlayers(attackPlayersMap);
    setDefensePlayers(defensePlayersMap);
    setEditingMode('attack');
    setViewSource('community');
    setDefenseConfirmShown(true);
    setAutoSquadApplied(true);
    
    console.log('✅ AutoGeneratedSquad otomatik uygulandı');
  }, [isMatchLive, hasPrediction, autoGeneratedSquad, startingXIPlayers, autoSquadApplied]);

  // ✅ BİTEN MAÇ: İlk 11'i en çok tercih edilen atak/defans formasyonuna göre yerleştir (lineups startXI)
  // ✅ autoGeneratedSquad varsa onun formasyonlarını kullan, yoksa API'den çek
  React.useEffect(() => {
    if (!isMatchFinished || !stateRestored || attackTeamPlayers.length < 11 || finishedFormationApplied) return;
    // ✅ autoGeneratedSquad zaten varsa bu useEffect'i atla - autoGeneratedSquad useEffect'i çalışacak
    if (autoGeneratedSquad) return;
    
    const applyFinishedMatchFormation = async () => {
      try {
        let popularAttackFormationId = '4-3-3';
        let popularDefenseFormationId = '4-3-3';
        try {
          const popularRes = await squadPredictionsApi.getPopularFormations('attack');
          if (popularRes.success && popularRes.data && popularRes.data.length > 0) {
            popularAttackFormationId = popularRes.data[0].formation;
          }
          const defRes = await squadPredictionsApi.getPopularFormations('defense');
          if (defRes?.success && defRes.data?.length > 0) {
            popularDefenseFormationId = defRes.data[0].formation;
          }
        } catch (_) {
          // Varsayılan 4-3-3
        }
        const attackForm = formations.find(f => f.id === popularAttackFormationId) || formations.find(f => f.id === '4-3-3');
        const defenseForm = formations.find(f => f.id === popularDefenseFormationId) || formations.find(f => f.id === '4-3-3');
        if (!attackForm) return;
        
        setAttackFormation(attackForm.id);
        setDefenseFormation(defenseForm?.id || attackForm.id);
        
        const autoAttackPlayers: Record<number, any | null> = {};
        const autoDefensePlayers: Record<number, any | null> = {};
        const usedPlayerIds = new Set<number>();
        
        // Atak formasyonu için oyuncuları yerleştir
        attackForm.positions.forEach((positionType: string, slotIndex: number) => {
          const slotPos = (positionType || '').toUpperCase();
          let candidates = attackTeamPlayers.filter((p: any) => {
            if (usedPlayerIds.has(p.id)) return false;
            const playerPos = (p.position || p.pos || '').toUpperCase();
            if (slotPos === 'GK' || slotPos === 'G') return isGoalkeeperPlayer(p);
            if (isGoalkeeperPlayer(p)) return false;
            if (['CB', 'LB', 'RB', 'LWB', 'RWB'].includes(slotPos)) {
              return playerPos.includes('D') || playerPos.includes('DEF') || playerPos === 'CB' || playerPos === 'LB' || playerPos === 'RB';
            }
            if (['CM', 'CDM', 'CAM', 'LM', 'RM', 'DM', 'AM'].includes(slotPos)) {
              return playerPos.includes('M') || playerPos === 'CM' || playerPos === 'CDM' || playerPos === 'CAM';
            }
            if (['ST', 'LW', 'RW', 'CF'].includes(slotPos)) {
              return playerPos.includes('F') || playerPos.includes('ST') || playerPos.includes('W') || playerPos === 'LW' || playerPos === 'RW';
            }
            return true;
          });
          candidates.sort((a: any, b: any) => (b.rating || 75) - (a.rating || 75));
          if (candidates.length > 0) {
            autoAttackPlayers[slotIndex] = candidates[0];
            usedPlayerIds.add(candidates[0].id);
          } else {
            const remaining = attackTeamPlayers.filter((p: any) => !usedPlayerIds.has(p.id) && !isGoalkeeperPlayer(p));
            if (remaining.length > 0) {
              remaining.sort((a: any, b: any) => (b.rating || 75) - (a.rating || 75));
              autoAttackPlayers[slotIndex] = remaining[0];
              usedPlayerIds.add(remaining[0].id);
            }
          }
        });
        
        // Defans formasyonu için oyuncuları yerleştir
        if (defenseForm) {
          const usedDefensePlayerIds = new Set<number>();
          defenseForm.positions.forEach((positionType: string, slotIndex: number) => {
            const slotPos = (positionType || '').toUpperCase();
            let candidates = attackTeamPlayers.filter((p: any) => {
              if (usedDefensePlayerIds.has(p.id)) return false;
              const playerPos = (p.position || p.pos || '').toUpperCase();
              if (slotPos === 'GK' || slotPos === 'G') return isGoalkeeperPlayer(p);
              if (isGoalkeeperPlayer(p)) return false;
              if (['CB', 'LB', 'RB', 'LWB', 'RWB'].includes(slotPos)) {
                return playerPos.includes('D') || playerPos.includes('DEF') || playerPos === 'CB' || playerPos === 'LB' || playerPos === 'RB';
              }
              if (['CM', 'CDM', 'CAM', 'LM', 'RM', 'DM', 'AM'].includes(slotPos)) {
                return playerPos.includes('M') || playerPos === 'CM' || playerPos === 'CDM' || playerPos === 'CAM';
              }
              if (['ST', 'LW', 'RW', 'CF'].includes(slotPos)) {
                return playerPos.includes('F') || playerPos.includes('ST') || playerPos.includes('W') || playerPos === 'LW' || playerPos === 'RW';
              }
              return true;
            });
            candidates.sort((a: any, b: any) => (b.rating || 75) - (a.rating || 75));
            if (candidates.length > 0) {
              autoDefensePlayers[slotIndex] = candidates[0];
              usedDefensePlayerIds.add(candidates[0].id);
            } else {
              const remaining = attackTeamPlayers.filter((p: any) => !usedDefensePlayerIds.has(p.id) && !isGoalkeeperPlayer(p));
              if (remaining.length > 0) {
                remaining.sort((a: any, b: any) => (b.rating || 75) - (a.rating || 75));
                autoDefensePlayers[slotIndex] = remaining[0];
                usedDefensePlayerIds.add(remaining[0].id);
              }
            }
          });
        }
        
        setAttackPlayers(autoAttackPlayers);
        setDefensePlayers(defenseForm ? autoDefensePlayers : autoAttackPlayers);
        setFinishedFormationApplied(true);
      } catch (err) {
        console.warn('Finished match formation apply failed', err);
        setFinishedFormationApplied(true);
      }
    };
    
    const t = setTimeout(applyFinishedMatchFormation, 400);
    return () => clearTimeout(t);
  }, [isMatchFinished, stateRestored, attackTeamPlayers, finishedFormationApplied, autoGeneratedSquad]);

  // ✅ GERÇEK İLK 11: Lineup API'den gelen formation ve startXI'yı state'e yaz
  // KİLİTLİ KURAL: Maç başlamadan (NS) gerçek ilk 11 state'e yazılmaz
  // Sadece maç canlı veya bitmiş ise gerçek ilk 11 gösterilir
  React.useEffect(() => {
    if (!lineups || lineups.length === 0) return;
    if (!isMatchLive && !isMatchFinished) return;
    
    // Favori takımın lineup'ını bul
    const targetLineup = lineups.find((l: any) => l.team?.id === attackTeamId) || lineups[0];
    if (!targetLineup?.startXI || targetLineup.startXI.length < 11) return;
    
    // API formation string'ini (e.g. "3-5-2") al veya varsayılan
    const apiFormation = targetLineup.formation || '4-3-3';
    const formObj = formations.find(f => f.id === apiFormation) || formations.find(f => f.id === '4-3-3');
    if (!formObj) return;
    
    // İlk 11 oyuncuları formasyon pozisyonlarına yerleştir
    const actualPlayers: Record<number, any> = {};
    const usedIds = new Set<number>();
    
    formObj.positions.forEach((posType: string, slotIdx: number) => {
      const slotPos = (posType || '').toUpperCase();
      const startXI = targetLineup.startXI.map((item: any) => {
        const p = item.player || item;
        return { ...p, position: normalizePosition(p.pos || p.position), rating: p.rating || 75, number: p.number ?? p.shirt_number ?? p.jersey_number };
      });
      
      let candidates = startXI.filter((p: any) => {
        if (usedIds.has(p.id)) return false;
        const pPos = (p.position || '').toUpperCase();
        if (slotPos === 'GK' || slotPos === 'G') return isGoalkeeperPlayer(p);
        if (isGoalkeeperPlayer(p)) return false;
        if (['CB', 'LB', 'RB', 'LWB', 'RWB'].includes(slotPos)) return pPos.includes('D') || pPos === 'CB' || pPos === 'LB' || pPos === 'RB';
        if (['CM', 'CDM', 'CAM', 'LM', 'RM', 'DM', 'AM'].includes(slotPos)) return pPos.includes('M') || pPos === 'CM' || pPos === 'CDM' || pPos === 'CAM';
        if (['ST', 'LW', 'RW', 'CF'].includes(slotPos)) return pPos.includes('F') || pPos.includes('ST') || pPos.includes('W');
        return true;
      });
      candidates.sort((a: any, b: any) => (b.rating || 75) - (a.rating || 75));
      if (candidates.length > 0) {
        actualPlayers[slotIdx] = candidates[0];
        usedIds.add(candidates[0].id);
      } else {
        const remaining = startXI.filter((p: any) => !usedIds.has(p.id) && !isGoalkeeperPlayer(p));
        if (remaining.length > 0) {
          actualPlayers[slotIdx] = remaining[0];
          usedIds.add(remaining[0].id);
        }
      }
    });
    
    setActualAttackFormation(formObj.id);
    setActualAttackPlayers(actualPlayers);
    // Gerçek maçta defans formasyonu ayrı gelmez, aynı lineup'ı kullan
    setActualDefenseFormation(formObj.id);
    setActualDefensePlayers(actualPlayers);
    
    console.log('✅ Gerçek İlk 11 yüklendi:', formObj.id, Object.keys(actualPlayers).length, 'oyuncu');
  }, [lineups, attackTeamId, isMatchLive, isMatchFinished]);

  React.useEffect(() => {
    runRestore();
  }, [squadStorageKey]);

  const prevIsVisibleRef = React.useRef<boolean>(false);
  // ✅ Kadro sekmesine geçildiğinde tekrar yükle; sayfa/tab değişip geri dönülünce sadece o an Atak'ta başla (Defans'ta kalmayı bozma)
  React.useEffect(() => {
    if (isVisible) {
      runRestore();
      const becameVisible = !prevIsVisibleRef.current;
      prevIsVisibleRef.current = true;
      if (becameVisible) setEditingMode('attack'); // Sadece sekme görünür olduğu anda Atak'a dön
    } else {
      prevIsVisibleRef.current = false;
    }
  // eslint-disable-next-line react-hooks/exhaustive-deps -- runRestore değişince tekrar Atak'a dönmesin; sadece isVisible değişince
  }, [isVisible]);
  
  // ✅ Her state değişikliğinde AsyncStorage'a kaydet (sekme değişimlerinde korunsun)
  // Kadro tamamlandıysa (isCompleted) attackPlayersArray/defensePlayersArray asla silinmez.
  // ✅ Oynanan/canlı maçta atak dizilişi asla kaybolmasın: existing'de geçerli atak varsa boş/eksik state ile ezme.
  React.useEffect(() => {
    if (!stateRestored) return; // İlk yüklemede kaydetme
    if (savingRef.current) return;
    
    const savePartialState = async () => {
      try {
        const key = squadStorageKey;
        const raw = await AsyncStorage.getItem(key);
        const existing = raw ? JSON.parse(raw) : {};
        // ✅ Tamamla sonrası (isCompleted) storage'ı EZME – sadece handleComplete yazsın, sıra bozulmasın
        if (existing.isCompleted === true) return;
        const wasCompleted = existing.isCompleted === true;
        const existingAttackCount = existing.attackPlayers && typeof existing.attackPlayers === 'object'
          ? Object.keys(existing.attackPlayers).filter((k: string) => existing.attackPlayers[k]).length
          : 0;
        const existingHasAttackArray = existing.attackPlayersArray && Array.isArray(existing.attackPlayersArray) && existing.attackPlayersArray.length >= 11;
        const currentAttackCount = Object.keys(attackPlayers).filter(k => attackPlayers[parseInt(k)]).length;
        // Restore sonrası ilk save: state henüz güncellenmemişse (current boş, existing dolu) mevcut kadroyu koru
        // ✅ Kullanıcı değişiklik yaptıysa (hasModifiedSinceUnlock) preserve yapma - kullanıcının değişikliklerini koru
        const preserveRestored = wasCompleted && existingAttackCount >= 11 && currentAttackCount < 11 && !hasModifiedSinceUnlock;
        // ✅ Canlı/oynanan maç: Storage'da geçerli atak varsa (11 oyuncu veya isAutoApplied) asla boş/eksik ile ezme
        // ✅ Ama kullanıcı değişiklik yaptıysa preserve yapma
        const existingHasValidAttack = existing.attackFormation && (existingAttackCount >= 11 || existingHasAttackArray || existing.isAutoApplied === true);
        const preserveAttackForLive = isMatchLive && existingHasValidAttack && currentAttackCount < 11 && !hasModifiedSinceUnlock;
        const preserve = preserveRestored || preserveAttackForLive;
        // ⚠️ State güncellemeleri burada yapılMAMALI - infinite loop'a neden olur
        // runRestore fonksiyonu zaten state'i yükler, burada sadece storage güncellenir
        // ✅ matchId'yi number olarak kaydet (karşılaştırma tip uyuşmazlığını önle)
        const numericMatchId = typeof matchId === 'string' ? parseInt(matchId, 10) : matchId;
        
        const updated: Record<string, any> = {
          ...existing,
          matchId: numericMatchId,
          attackFormation: preserve ? existing.attackFormation : attackFormation,
          defenseFormation: preserve ? existing.defenseFormation : defenseFormation,
          editingMode: preserve ? (existing.editingMode || 'attack') : editingMode,
          defenseConfirmShown: preserve ? (existing.defenseConfirmShown ?? false) : defenseConfirmShown,
          isCompleted: wasCompleted || false,
        };
        
        // ✅ Slot sırası kesin: 0..10 döngü ile yaz (attackPlayers/defensePlayers aşağıda slot sıralı yazılacak)
        const attackArr: (any)[] = [];
        for (let i = 0; i < 11; i++) attackArr[i] = attackPlayers[i] ?? attackPlayers[String(i)] ?? null;
        const defArr: (any)[] = [];
        for (let i = 0; i < 11; i++) defArr[i] = defensePlayers[i] ?? defensePlayers[String(i)] ?? null;
        const hasValidCurrentAttack = attackArr.filter(Boolean).length >= 11;
        const hasValidCurrentDefense = defArr.filter(Boolean).length >= 11;
        
        // ✅ attackPlayers/defensePlayers'ı state yerine slot sıralı diziyle yaz (karışmasın)
        const attackOrdered: Record<number, any> = {};
        for (let i = 0; i < 11; i++) if (attackArr[i]) attackOrdered[i] = attackArr[i];
        const defenseOrdered: Record<number, any> = {};
        for (let i = 0; i < 11; i++) if (defArr[i]) defenseOrdered[i] = defArr[i];
        if (!preserve) {
          updated.attackPlayers = attackOrdered;
          updated.defensePlayers = defenseOrdered;
        }
        
        if (wasCompleted || (isMatchLive && existingHasValidAttack) || hasValidCurrentAttack) {
          if (existing.attackPlayersBySlot && Array.isArray(existing.attackPlayersBySlot) && existing.attackPlayersBySlot.length >= 11) {
            updated.attackPlayersBySlot = existing.attackPlayersBySlot;
            updated.attackPlayersArray = Array.from({ length: 11 }, (_, i) => {
              const e = existing.attackPlayersBySlot.find((x: { slot: number }) => x.slot === i);
              return e?.player ?? null;
            });
          } else if (existing.attackPlayersArray && Array.isArray(existing.attackPlayersArray) && existing.attackPlayersArray.length >= 11) {
            updated.attackPlayersArray = existing.attackPlayersArray;
            updated.attackPlayersBySlot = Array.from({ length: 11 }, (_, i) => ({ slot: i, player: existing.attackPlayersArray[i] ?? null }));
          } else if (hasValidCurrentAttack) {
            updated.attackPlayersArray = attackArr;
            updated.attackPlayersBySlot = Array.from({ length: 11 }, (_, i) => ({ slot: i, player: attackArr[i] ?? null }));
          }
          if (existing.defensePlayersArray && Array.isArray(existing.defensePlayersArray) && existing.defensePlayersArray.length >= 11) {
            updated.defensePlayersArray = existing.defensePlayersArray;
          } else if (hasValidCurrentDefense) {
            updated.defensePlayersArray = defArr;
          }
          if (existing.attackFormationName) updated.attackFormationName = existing.attackFormationName;
        }
        
        await AsyncStorage.setItem(key, JSON.stringify(updated));
      } catch (e) {
        console.warn('Partial state save failed', e);
      }
    };
    savePartialState();
  }, [attackFormation, defenseFormation, attackPlayers, defensePlayers, editingMode, defenseConfirmShown, stateRestored, matchId, squadStorageKey, isMatchLive, hasModifiedSinceUnlock]);
  
  // ✅ Kullanıcı tahmin yapmış mı kontrol et (storage'dan yükle) – biten maçta "Kadro oluşturulmadı" yanıp sönmesin
  React.useEffect(() => {
    setHasCheckedSquadStorage(false);
    (async () => {
      try {
        const fixtureId = Number(matchId);
        const isMockMatch = fixtureId === MOCK_MATCH_IDS.REAL_BARCA || fixtureId === MOCK_MATCH_IDS.GS_FB || fixtureId === MOCK_MATCH_IDS.TEST_6H || fixtureId === MOCK_MATCH_IDS.TEST_1H;
        
        // Mock maçlar için: Storage'da kayıt olsa bile, gerçekten tamamlanmış mı kontrol et
        const raw = await AsyncStorage.getItem(squadStorageKey);
        if (raw) {
          const parsed = JSON.parse(raw);
          // ✅ Mock maçlar için: matchId eşleşmeli ve isCompleted true olmalı
          // ✅ Tip uyuşmazlığını önlemek için Number() ile normalize et
          const parsedMatchId = parsed.matchId != null ? Number(parsed.matchId) : null;
          if (isMockMatch) {
            const hasValidPrediction = parsed.isCompleted === true && 
                                     parsedMatchId === fixtureId &&
                                     parsed.attackPlayersArray &&
                                     parsed.attackPlayersArray.length >= 11;
            setHasPrediction(hasValidPrediction);
          } else {
            setHasPrediction(parsed.isCompleted === true);
          }
        } else {
          setHasPrediction(false);
        }
      } catch (_) {
        setHasPrediction(false);
      } finally {
        setHasCheckedSquadStorage(true);
      }
    })();
  }, [squadStorageKey, matchId]);
  
  // ✅ Oynanan maçta viewSource varsayılanı:
  // - Tahmin yapılmışsa → 'user' (kullanıcının tahmini)
  // - Canlı maç + tahmin yapılmamışsa → 'community' (topluluk formasyonu + gerçek oyuncular)
  // - Biten maç + tahmin yapılmamışsa → 'community' (topluluk formasyonu + gerçek oyuncular)
  // NOT: Artık tahmin yapılmamışsa her durumda 'community' - gerçek oyuncular topluluk formasyonuna yerleşir
  React.useEffect(() => {
    if (isKadroLocked) {
      if (hasPrediction) {
        setViewSource('user');
      } else {
        // Tahmin yapılmamışsa (canlı veya biten maç) → topluluk formasyonu ile göster
        setViewSource('community');
      }
    }
  }, [isKadroLocked, hasPrediction, isMatchLive, isMatchFinished]);
  
  // ✅ 120 saniyelik timeout mantığı kaldırıldı - artık maç başlayana kadar serbestçe düzenleme yapılabilir
  
  // ✅ viewSource + editingMode'a göre gösterilecek formasyon ve oyuncular
  const selectedFormation = React.useMemo(() => {
    if (isKadroLocked) {
      if (viewSource === 'actual') {
        return editingMode === 'attack' ? actualAttackFormation : (actualDefenseFormation ?? actualAttackFormation);
      }
      if (viewSource === 'community') {
        const userForm = editingMode === 'attack' ? attackFormation : (defenseFormation ?? attackFormation);
        return userForm || null;
      }
      return editingMode === 'attack' ? attackFormation : (defenseFormation ?? attackFormation);
    }
    return editingMode === 'attack'
      ? attackFormation
      : (defenseFormation ?? attackFormation ?? null);
  }, [isKadroLocked, viewSource, editingMode, attackFormation, defenseFormation, actualAttackFormation, actualDefenseFormation]);
  
  const defensePlayerCount = Object.keys(defensePlayers).filter(k => defensePlayers[parseInt(k)]).length;
  
  const selectedPlayers = React.useMemo(() => {
    if (isKadroLocked) {
      if (viewSource === 'actual') {
        return editingMode === 'attack' ? actualAttackPlayers : (Object.keys(actualDefensePlayers).length > 0 ? actualDefensePlayers : actualAttackPlayers);
      }
      if (viewSource === 'community') {
        const userPlayers = editingMode === 'attack' ? attackPlayers : defensePlayers;
        const hasUserPlayers = Object.values(userPlayers).some(p => p != null);
        if (hasUserPlayers) return userPlayers;
        return {};
      }
      return editingMode === 'attack' ? attackPlayers : defensePlayers;
    }
    return editingMode === 'attack' ? attackPlayers : defensePlayers;
  }, [isKadroLocked, viewSource, editingMode, attackPlayers, defensePlayers, actualAttackPlayers, actualDefensePlayers]);
  
  const setSelectedPlayers = editingMode === 'attack' ? setAttackPlayers : setDefensePlayers;
  
  const [selectedSlot, setSelectedSlot] = useState<number | null>(null);
  const [showFormationModal, setShowFormationModal] = useState(false);
  const [showPlayerModal, setShowPlayerModal] = useState(false);
  const [showNoSquadInfoModal, setShowNoSquadInfoModal] = useState(false);
  const [formationType, setFormationType] = useState<'attack' | 'defense' | 'balanced'>('attack');
  const [selectedPlayerForDetail, setSelectedPlayerForDetail] = useState<any | null>(null);
  const [isSaving, setIsSaving] = useState(false); // ✅ Kaydediliyor... göstergesi için
  const savingRef = React.useRef(false); // ✅ handleComplete sırasında savePartialState'i engelle
  const [isSquadLocked, setIsSquadLocked] = useState(false); // ✅ Kadro kilitli mi? (Tamamla sonrası)
  const effectiveSquadLocked = isSquadLocked || isKadroLocked || squadEditingDisabled;
  // ✅ Önceki kilit durumu - kilit açıldığında reset flag
  const prevSquadLockedRef = React.useRef(isSquadLocked);
  
  // ✅ Kilit açıldığında modified flag'i sıfırla
  React.useEffect(() => {
    if (prevSquadLockedRef.current === true && isSquadLocked === false) {
      setHasModifiedSinceUnlock(false);
    }
    prevSquadLockedRef.current = isSquadLocked;
  }, [isSquadLocked]);
  
  // ✅ Kaydedilmemiş değişiklikleri parent'a bildir
  React.useEffect(() => {
    // ✅ Biten maçlarda kaydedilmemiş değişiklik uyarısı gösterilmez
    if (isMatchFinished) {
      setHasUnsavedChanges(false);
      onHasUnsavedChanges?.(false);
      return;
    }
    
    // Canlı/biten maçlarda değişiklik yapılamaz
    if (isKadroLocked) {
      setHasUnsavedChanges(false);
      onHasUnsavedChanges?.(false);
      return;
    }
    
    // ✅ Kadro kilitli ise (Tamamla basıldı, kırmızı kilit) = kaydedilmiş, değişiklik yok
    if (isSquadLocked) {
      setHasUnsavedChanges(false);
      onHasUnsavedChanges?.(false);
      return;
    }
    
    // ✅ Otomatik kadro uygulandıysa VE kullanıcı henüz manuel değişiklik yapmadıysa → uyarı gösterme
    // Sadece kullanıcı manuel olarak değişiklik yaptığında uyarı gösterilmeli
    if (autoSquadApplied && !hasModifiedSinceUnlock) {
      setHasUnsavedChanges(false);
      onHasUnsavedChanges?.(false);
      return;
    }
    
    // ✅ Kilit açık (yeşil) VE formasyon/oyuncu seçilmişse → her zaman uyarı göster
    // Kilit açıldığında sekme değişirken mutlaka "kaydet" uyarısı gösterilmeli
    const attackPlayerCount = Object.keys(attackPlayers).filter(k => attackPlayers[parseInt(k)]).length;
    const hasFormationAndPlayers = attackFormation !== null && attackPlayerCount > 0;
    
    // Kilit yeşil (açık) ise → kaydedilmemiş sayılır (tamamla basılmamış)
    // Ama otomatik kadro uygulandıysa ve kullanıcı değişiklik yapmadıysa → false
    const hasChanges = hasFormationAndPlayers && (hasModifiedSinceUnlock || !autoSquadApplied);
    
    setHasUnsavedChanges(hasChanges);
    onHasUnsavedChanges?.(hasChanges);
  }, [attackFormation, attackPlayers, isKadroLocked, isSquadLocked, isMatchFinished, autoSquadApplied, hasModifiedSinceUnlock, onHasUnsavedChanges]);
  
  // ✅ Community Signal Popup State
  const [showCommunitySignal, setShowCommunitySignal] = useState(false);
  const [communitySignalPlayer, setCommunitySignalPlayer] = useState<{ id: number; name: string; position: string } | null>(null);
  // Session-based spam prevention: track which players have been shown signal in this session
  const communitySignalShownRef = React.useRef<Set<number>>(new Set());
  
  // ✅ editingMode değiştiğinde formationType'ı senkronize et
  React.useEffect(() => {
    if (editingMode === 'defense') {
      setFormationType('defense');
    } else if (editingMode === 'attack') {
      setFormationType('attack');
    }
  }, [editingMode]);
  
  // Player Predictions State
  const [playerPredictions, setPlayerPredictions] = useState<Record<number, any>>({});
  
  // ✅ Get attack squad players for defense selection
  const attackSquadPlayers = React.useMemo(() => {
    return Object.values(attackPlayers).filter(Boolean) as any[];
  }, [attackPlayers]);
  
  // ✅ Show defense confirmation when attack squad is complete (11 players)
  React.useEffect(() => {
    if (!stateRestored) return; // State restore edilene kadar bekle
    
    // ✅ Tahmin yapılmayan canlı/biten maçta defans popup gösterme (VIEW-ONLY mod)
    if ((isMatchLive || isMatchFinished) && !hasPrediction) {
      return;
    }
    
    const attackCount = Object.keys(attackPlayers).filter(k => attackPlayers[parseInt(k)]).length;
    
    // Show confirmation only when:
    // 1. Attack squad has 11 players
    // 2. Defense formation not yet selected
    // 3. Confirmation not already shown
    // 4. We're in attack mode
    // 5. Match not live (canlı maçta kadro kilitli, defans modalı gösterme)
    if (attackCount === 11 && !defenseFormation && !defenseConfirmShown && editingMode === 'attack' && !isKadroLocked) {
      setDefenseConfirmShown(true);
      setTimeout(() => {
        setShowDefenseConfirmModal(true);
      }, 500);
    }
  }, [attackPlayers, defenseFormation, defenseConfirmShown, editingMode, stateRestored, isKadroLocked, isMatchLive, isMatchFinished, hasPrediction]);

  // ✅ Atak formasyonundaki oyuncu değişikliklerini takip et ve defans formasyonunu yönet
  React.useEffect(() => {
    if (!stateRestored || !attackPlayersInitializedRef.current) {
      // İlk yüklemede previousAttackPlayersRef'i güncelle
      if (stateRestored && Object.keys(attackPlayers).length > 0) {
        previousAttackPlayersRef.current = { ...attackPlayers };
        attackPlayersInitializedRef.current = true;
      }
      return;
    }

    // Defans formasyonu yoksa veya kilitliyse kontrol etme
    if (!defenseFormation || effectiveSquadLocked) {
      previousAttackPlayersRef.current = { ...attackPlayers };
      return;
    }

    const prev = previousAttackPlayersRef.current;
    const current = attackPlayers;

    // Hangi oyuncular değişti?
    const changedSlots: number[] = [];
    let goalkeeperChanged = false;
    let nonGoalkeeperChanged = false;

    // Tüm slotları kontrol et
    const allSlots = new Set([...Object.keys(prev).map(Number), ...Object.keys(current).map(Number)]);
    
    allSlots.forEach(slot => {
      const prevPlayer = prev[slot];
      const currentPlayer = current[slot];
      
      // Oyuncu değişti mi?
      if (prevPlayer?.id !== currentPlayer?.id) {
        changedSlots.push(slot);
        
        // Kaleci pozisyonu mu?
        const formation = formations.find(f => f.id === attackFormation);
        const slotPosition = formation?.positions[slot];
        const isGKSlot = slotPosition === 'GK';
        
        if (isGKSlot) {
          goalkeeperChanged = true;
        } else {
          nonGoalkeeperChanged = true;
        }
      }
    });

    // Eğer değişiklik yoksa çık
    if (changedSlots.length === 0) {
      return;
    }

    // ✅ Sadece kaleci değiştiyse: Defans formasyonuna otomatik ekle
    if (goalkeeperChanged && !nonGoalkeeperChanged) {
      const currentGoalkeeper = Object.values(current).find(p => p && isGoalkeeperPlayer(p));
      
      if (currentGoalkeeper && defenseFormation) {
        const defFormation = formations.find(f => f.id === defenseFormation);
        if (defFormation) {
          const gkSlot = defFormation.positions.findIndex((pos: string) => pos === 'GK');
          if (gkSlot !== -1) {
            setDefensePlayers(prev => {
              const updated = { ...prev };
              updated[gkSlot] = currentGoalkeeper;
              return updated;
            });
            
            console.log('✅ Kaleci defans formasyonuna otomatik eklendi');
          }
        }
      }
    } 
    // ✅ Kaleci dışında oyuncu değişikliği: Artık defans formasyon sıfırlanmıyor!
    // Oyuncu değişikliği handlePlayerSelect ve handleRemovePlayer içinde otomatik yapılıyor.
    // Bu useEffect sadece kaleci güncellemesi için kullanılıyor.

    // Previous state'i güncelle
    previousAttackPlayersRef.current = { ...attackPlayers };
  }, [attackPlayers, defenseFormation, stateRestored, isKadroLocked, isSquadLocked, attackFormation]);

  // Pulsing ball animation
  const scale = useSharedValue(1);
  
  React.useEffect(() => {
    if (!isWeb) {
      scale.value = withRepeat(
        withTiming(1.1, { duration: 1000 }),
        -1,
        true
      );
    }
  }, []);

  const animatedBallStyle = useAnimatedStyle(() => ({
    transform: [{ scale: isWeb ? 1 : scale.value }],
  }));

  const applyFormationChange = (formationId: string, mode?: 'attack' | 'defense') => {
    const formation = formations.find(f => f.id === formationId);
    // ✅ formationType state'ini kullan (modal'dan seçim yapılırken güncel)
    const effectiveMode = mode || formationType;
    
    if (effectiveMode === 'attack') {
      // ✅ Atak formasyonu değiştiğinde: Tüm tahminleri ve pozisyonları sıfırla
      const clearAllPredictions = async () => {
        try {
          if (effectivePredictionTeamId != null) {
            await AsyncStorage.removeItem(`${STORAGE_KEYS.PREDICTIONS}${matchId}-${effectivePredictionTeamId}`);
          } else {
            await AsyncStorage.removeItem(STORAGE_KEYS.PREDICTIONS + matchId);
          }
          const userDataStr = await AsyncStorage.getItem(STORAGE_KEYS.USER);
          const userData = userDataStr ? JSON.parse(userDataStr) : null;
          const userId = userData?.id;
          if (userId) {
            await predictionsDb.deletePredictionsByMatch(userId, String(matchId));
          }
        } catch (e) { 
          console.warn('Clear predictions failed during formation change', e); 
        }
      };
      
      clearAllPredictions();
      
      setAttackFormation(formationId);
      setAttackPlayers({});
      setDefenseFormation(null);
      setDefensePlayers({});
      setDefenseConfirmShown(false);
      setHasModifiedSinceUnlock(true); // ✅ Formasyon değişti
      // ✅ Previous state'i de sıfırla
      previousAttackPlayersRef.current = {};
      attackPlayersInitializedRef.current = false;
      setShowFormationModal(false);
      
      // ✅ Analiz odağından başlatmak için callback çağır
      // Not: Bu callback sadece gerçekten atak formasyonu değiştiğinde çağrılır
      // Analiz odağı zaten seçilmişse tekrar açılmamalı
      // ✅ Canlı/biten maçlarda analiz odağı seçimine gerek yok
      if (!isMatchFinished && !isMatchLive) {
        setTimeout(() => {
          onAttackFormationChangeConfirmed?.();
        }, 500);
      }
    } else {
      // ✅ Defans formasyonu seçildiğinde: SADECE kaleci otomatik atanır, diğer 10 pozisyon boş kalır
      setDefenseFormation(formationId);
      
      const defFormation = formations.find((f: any) => f.id === formationId);
      const defPlayers: Record<number, any | null> = {};
      
      // ✅ Önce atak kadrosundaki kaleci'yi bul
      let goalkeeper: any | null = null;
      Object.entries(attackPlayers).forEach(([slot, p]) => {
        if (p && isGoalkeeperPlayer(p)) {
          goalkeeper = p;
        }
      });
      
      // ✅ SADECE kaleci defans formasyonunun GK slotuna otomatik atanır
      // Diğer 10 pozisyon BOŞ kalır - kullanıcı atak kadrosundan seçerek doldurur
      if (goalkeeper && defFormation) {
        const gkSlot = defFormation.positions.findIndex((pos: string) => pos === 'GK');
        if (gkSlot !== -1) {
          defPlayers[gkSlot] = goalkeeper;
        }
      }
      
      setDefensePlayers(defPlayers); // SADECE kaleci atanmış, diğer 10 pozisyon boş
      setEditingMode('defense');
      setHasModifiedSinceUnlock(true); // ✅ Formasyon değişti
      
      setShowFormationModal(false);
    }
    // isCompleted sadece ATAK formasyonu değiştiğinde sıfırlanır (defans değişikliği tahminleri silmez)
    if (effectiveMode === 'attack') {
      (async () => {
        try {
          const key = squadStorageKey;
          const raw = await AsyncStorage.getItem(key);
          if (raw) {
            const parsed = JSON.parse(raw);
            parsed.isCompleted = false;
            await AsyncStorage.setItem(key, JSON.stringify(parsed));
          }
        } catch (e) { console.warn('isCompleted reset failed', e); }
      })();
    } else {
      // Defans formasyonu: isCompleted sıfırla (defans yerleştirmesi gerekiyor) ama tahminler silinmez
      (async () => {
        try {
          const key = squadStorageKey;
          const raw = await AsyncStorage.getItem(key);
          if (raw) {
            const parsed = JSON.parse(raw);
            parsed.isCompleted = false;
            parsed.attackPlayersArray = Array.from({ length: 11 }, (_, i) => attackPlayers[i] ?? null);
            parsed.attackFormation = attackFormation;
            await AsyncStorage.setItem(key, JSON.stringify(parsed));
          }
        } catch (e) { console.warn('Defense formation state save failed', e); }
      })();
    }
  };

  const handleFormationSelect = async (formationId: string) => {
    // ✅ AYNI FORMASYON SEÇİLDİYSE: Hiçbir şey yapma, sadece modal'ı kapat
    const currentFormation = formationType === 'attack' ? attackFormation : defenseFormation;
    if (formationId === currentFormation) {
      console.log('✅ Aynı formasyon seçildi, değişiklik yok:', formationId);
      setShowFormationModal(false);
      return;
    }
    
    // ✅ Uyarı SADECE atak formasyonu değiştiğinde: modal'da "Atak" sekmesindeyken farklı formasyon seçilirse.
    // Defans formasyonu seçildiğinde bu uyarı hiç gelmez (defans aynı 11'i kullanır, tahminler silinmez).
    const isAttackFormationChange =
      formationType === 'attack' &&
      attackFormation != null &&
      formationId !== attackFormation;

    if (isAttackFormationChange) {
      // ✅ Uyarı SADECE kadro "Tamamla" ile tamamlanmışsa gösterilir (oyuncu atanmış + Tamamla basılmış)
      let squadIsCompleted = false;
      try {
        const squadRaw = await AsyncStorage.getItem(squadStorageKey);
        if (squadRaw) {
          const squad = JSON.parse(squadRaw);
          squadIsCompleted = squad?.isCompleted === true;
        }
      } catch (_) {}

      if (squadIsCompleted) {
        const clearAndApply = async () => {
          try {
            if (effectivePredictionTeamId != null) {
              await AsyncStorage.removeItem(`${STORAGE_KEYS.PREDICTIONS}${matchId}-${effectivePredictionTeamId}`);
            } else {
              await AsyncStorage.removeItem(STORAGE_KEYS.PREDICTIONS + matchId);
            }
            const userDataStr = await AsyncStorage.getItem(STORAGE_KEYS.USER);
            const userData = userDataStr ? JSON.parse(userDataStr) : null;
            const userId = userData?.id;
            if (userId) {
              await predictionsDb.deletePredictionsByMatch(userId, String(matchId));
            }
            const key = squadStorageKey;
            const raw = await AsyncStorage.getItem(key);
            if (raw) {
              const parsed = JSON.parse(raw);
              parsed.isCompleted = false;
              await AsyncStorage.setItem(key, JSON.stringify(parsed));
            }
          } catch (e) { console.warn('Clear predictions failed', e); }
          applyFormationChange(formationId);
        };

        setFormationConfirmModal({ formationId });
        return;
      }
    }

    applyFormationChange(formationId);
  };

  const runFormationChangeConfirm = async () => {
    const id = formationConfirmModal?.formationId;
    if (!id) return;
    try {
      if (effectivePredictionTeamId != null) {
        await AsyncStorage.removeItem(`${STORAGE_KEYS.PREDICTIONS}${matchId}-${effectivePredictionTeamId}`);
      } else {
        await AsyncStorage.removeItem(STORAGE_KEYS.PREDICTIONS + matchId);
      }
      const userDataStr = await AsyncStorage.getItem(STORAGE_KEYS.USER);
      const userData = userDataStr ? JSON.parse(userDataStr) : null;
      const userId = userData?.id;
      if (userId) await predictionsDb.deletePredictionsByMatch(userId, String(matchId));
      const raw = await AsyncStorage.getItem(squadStorageKey);
      if (raw) {
        const parsed = JSON.parse(raw);
        parsed.isCompleted = false;
        await AsyncStorage.setItem(squadStorageKey, JSON.stringify(parsed));
      }
    } catch (e) { console.warn('Clear predictions failed', e); }
    applyFormationChange(id);
    setFormationConfirmModal(null);
    setShowFormationModal(false);
    // ✅ Analiz odağı seçimi tekrar gösterilsin: Dashboard'a dön; kullanıcı maça tıklayınca analiz odağı modal açılır
    // ✅ Canlı/biten maçlarda analiz odağı seçimine gerek yok
    if (!isMatchFinished && !isMatchLive) {
      onAttackFormationChangeConfirmed?.();
    }
  };
  
  // ✅ Handle defense confirmation
  const handleDefenseConfirmYes = () => {
    setShowDefenseConfirmModal(false);
    setEditingMode('defense');
    setFormationType('defense');
    // ✅ Canlı/biten maç veya kadro kilitli ise formasyon değişikliğine izin verme
    if (effectiveSquadLocked) return;
    setTimeout(() => {
      setShowFormationModal(true);
    }, 300);
  };
  
  const handleDefenseConfirmNo = () => {
    setShowDefenseConfirmModal(false);
    
    // ✅ Copy attack formation and players to defense (same system for both)
    if (attackFormation) {
      setDefenseFormation(attackFormation);
      setDefensePlayers({ ...attackPlayers }); // ✅ Aynı oyuncular defans için de kopyalanıyor
      
      const formation = formations.find(f => f.id === attackFormation);
      setShowSquadCompletePopup(formation?.name ?? '');
      setTimeout(() => setShowSquadCompletePopup(null), 5000);
    }
  };

  const handlePlayerSelect = (player: any) => {
    // ✅ Sakat veya cezalı oyuncu kadroya eklenemez (kısıt kalkınca tekrar eklenebilir)
    if (player.eligible_for_selection === false || player.injured || player.suspended) {
      showInfo(
        'Kadroya Eklenemez',
        'Bu oyuncu sakat veya cezalı. Kadroya ekleyemezsiniz. Sakatlık/ceza kalkınca tekrar ekleyebilirsiniz.'
      );
      return;
    }
    if (selectedSlot !== null) {
      const currentFormation = editingMode === 'attack' ? attackFormation : defenseFormation;
      const formationData = formations.find(f => f.id === currentFormation);
      const slotPosition = formationData?.positions[selectedSlot];
      
      const isGK = isGoalkeeperPlayer(player);
      // ✅ Kaleci yalnızca kale pozisyonuna, saha oyuncusu yalnızca saha pozisyonlarına
      if (isGK && slotPosition !== 'GK') {
        showInfo(t('matchSquad.goalkeeperConstraint'), t('matchSquad.goalkeeperConstraintDesc'));
        return;
      }
      if (slotPosition === 'GK' && !isGK) {
        showInfo(t('matchSquad.goalPositionConstraint'), t('matchSquad.goalPositionConstraintDesc'));
        return;
      }
      
      if (editingMode === 'attack') {
        // ✅ Eski oyuncuyu al
        const oldPlayer = attackPlayers[selectedSlot];
        
        // ✅ Oyuncu gerçekten değişti mi kontrol et
        const isPlayerChanged = !oldPlayer || oldPlayer.id !== player.id;
        
        const newAttack = { ...attackPlayers, [selectedSlot]: player };
        setAttackPlayers(newAttack);
        
        // ✅ ATAK OYUNCUSU DEĞİŞTİĞİNDE DEFANS KADROSUNU SIFIRLA
        // Yeni mantık: Atak kadrosunda tek bir oyuncu bile değişirse, defans formasyonu yeniden kurulmalı
        if (defenseFormation && isPlayerChanged) {
          console.log('⚠️ Atak oyuncusu değişti, defans formasyonu sıfırlanacak');
          setDefenseFormation(null);
          setDefensePlayers({});
          setDefenseConfirmShown(false);
          
          // Kullanıcıya bilgi ver
          setTimeout(() => {
            showInfo(
              'Defans Formasyonu Sıfırlandı',
              'Atak kadrosunda değişiklik yaptınız. Lütfen defans formasyonunu yeniden seçin.'
            );
          }, 500);
        }
      } else {
        // ✅ Defans modunda sadece o oyuncu değişir
        setDefensePlayers({ ...defensePlayers, [selectedSlot]: player });
      }
      
      // ✅ Değişiklik yapıldı - kilit açıldıysa uyarı gösterilecek
      setHasModifiedSinceUnlock(true);
      
      setSelectedSlot(null);
      setShowPlayerModal(false);
    }
  };

  const handleRemovePlayer = async (slotIndex: number) => {
    // ✅ Kilit kontrolü - çift kontrol
    if (isKadroLocked) {
      showKadroLockedToast();
      return;
    }
    
    if (isSquadLocked) {
      showInfo(t('matchSquad.squadLocked'), t('matchSquad.squadLockedRemovePlayer'));
      return;
    }
    
    const player = selectedPlayers[slotIndex];
    if (!player) {
      console.warn('handleRemovePlayer: No player at slot', slotIndex);
      return;
    }
    
    console.log('✅ handleRemovePlayer: Removing player', player.name, 'from slot', slotIndex, 'editingMode:', editingMode);
    console.log('✅ Current attackPlayers before update:', {
      keys: Object.keys(attackPlayers),
      slot0: attackPlayers[0],
      slotIndex: attackPlayers[slotIndex],
      count: Object.keys(attackPlayers).filter(k => attackPlayers[parseInt(k)]).length
    });
    
    // ✅ editingMode'a göre doğru state'i güncelle - functional update kullan
    if (editingMode === 'attack') {
      // ✅ ATAK OYUNCUSU SİLİNDİĞİNDE DEFANS KADROSUNU SIFIRLA
      // Yeni mantık: Atak kadrosunda tek bir oyuncu bile değişirse, defans formasyonu yeniden kurulmalı
      if (defenseFormation && player) {
        console.log('⚠️ Atak oyuncusu silindi, defans formasyonu sıfırlanacak');
        setDefenseFormation(null);
        setDefensePlayers({});
        setDefenseConfirmShown(false);
        
        // Kullanıcıya bilgi ver
          setTimeout(() => {
            showInfo(
              'Defans Formasyonu Sıfırlandı',
              'Atak kadrosunda değişiklik yaptınız. Lütfen defans formasyonunu yeniden seçin.'
            );
          }, 500);
      }
      
      setAttackPlayers(prev => {
        // Önce mevcut state'i kopyala; hem sayı hem string slot anahtarını temizle
        // (storage'dan gelen veri bazen string key kullanır, çıkarılan oyuncu yedeklerde görünsün)
        const updated = { ...prev };
        updated[slotIndex] = null;
        updated[String(slotIndex)] = null;
        return updated;
      });
    } else {
      setDefensePlayers(prev => {
        const updated = { ...prev };
        updated[slotIndex] = null;
        updated[String(slotIndex)] = null;
        return updated;
      });
    }
    
    // ✅ Değişiklik yapıldı - kilit açıldıysa uyarı gösterilecek
    setHasModifiedSinceUnlock(true);
    
    // ✅ Atak modunda oyuncu çıkarılırsa: defans kadrosu ETKİLENMEZ
    // Kullanıcı her kadroyu ayrı yönetir
    
    // Kadro artık 10 kişi (eksik) → isCompleted sıfırla
    try {
      const key = squadStorageKey;
      const raw = await AsyncStorage.getItem(key);
      if (raw) {
        const parsed = JSON.parse(raw);
        parsed.isCompleted = false;
        await AsyncStorage.setItem(key, JSON.stringify(parsed));
      }
    } catch (e) { console.warn('isCompleted reset failed', e); }
  };

  const handleComplete = async () => {
    const attackCount = Object.keys(attackPlayers).filter(k => attackPlayers[parseInt(k)]).length;
    const defenseCount = Object.keys(defensePlayers).filter(k => defensePlayers[parseInt(k)]).length;
    
    console.log('🔍 handleComplete called', { attackCount, defenseCount, defenseFormation, editingMode });
    
    // Check if attack squad is complete
    if (attackCount < 11) {
      showInfo(t('matchSquad.attackSquadIncompleteTitle'), t('matchSquad.attackSquadIncomplete', { count: 11 - attackCount }));
      setEditingMode('attack');
      return;
    }
    
    // If defense formation was selected, check if defense squad is complete
    if (defenseFormation && defenseCount < 11) {
      showInfo(t('matchSquad.defenseSquadIncompleteTitle'), t('matchSquad.defenseSquadIncomplete', { count: 11 - defenseCount }));
      setEditingMode('defense');
      return;
    }
    
    // ✅ Kaydediliyor... göster
    setIsSaving(true);
    savingRef.current = true; // ✅ savePartialState'in isCompleted'ı ezmesini engelle
    
    try {
      // ✅ Kayıtta sıra bozulmasın: sadece slot 0..10 ile explicit oku (objenin key sırasına güvenme)
      const attackPlayersArray: (any)[] = [];
      const attackPlayersBySlot: { slot: number; player: any }[] = [];
      const attackPlayersOrdered: Record<number, any> = {};
      for (let i = 0; i < 11; i++) {
        const p = attackPlayers[i] ?? attackPlayers[String(i)] ?? null;
        attackPlayersArray[i] = p;
        attackPlayersBySlot.push({ slot: i, player: p });
        attackPlayersOrdered[i] = p;
      }
      const defensePlayersArray = defenseFormation
        ? (() => {
            const arr: (any)[] = [];
            for (let i = 0; i < 11; i++) arr[i] = defensePlayers[i] ?? defensePlayers[String(i)] ?? null;
            return arr;
          })()
        : attackPlayersArray;
      
      const formationName = formations.find(f => f.id === attackFormation)?.name || attackFormation;
      const numericMatchId = typeof matchId === 'string' ? parseInt(matchId, 10) : matchId;
      const squadData = {
        matchId: numericMatchId,
        attackFormation: attackFormation,
        attackFormationName: formationName,
        defenseFormation: defenseFormation || attackFormation,
        attackPlayers: attackPlayersOrdered,
        attackPlayersArray: attackPlayersArray,
        attackPlayersBySlot: attackPlayersBySlot,
        defensePlayers: defenseFormation ? defensePlayers : attackPlayersOrdered,
        defensePlayersArray: defensePlayersArray,
        playerPredictions: playerPredictions,
        timestamp: new Date().toISOString(),
        isCompleted: true, // ✅ Tamamla basıldı – Tahmin sekmesinde oyuncular görünecek
        // ✅ Tüm takım kadrosu (yedekler dahil) - MatchPrediction'da oyuncu değişikliği için
        allTeamPlayers: attackTeamPlayers,
      };
      
      await AsyncStorage.setItem(
        squadStorageKey,
        JSON.stringify(squadData)
      );
      
      console.log('✅ Squad saved to local storage!', squadData);
      
      // ✅ Backend'e arka planda kaydet (başarısız olsa da sekme geçişi yapılacak)
      (async () => {
        try {
          const { data: sessionData } = await supabase.auth.getSession();
          const authToken = sessionData?.session?.access_token;
          if (authToken) {
            const result = await squadPredictionsApi.saveSquadPrediction({
              matchId: matchId,
              attackFormation: attackFormation!,
              attackPlayers: attackPlayers,
              defenseFormation: defenseFormation || attackFormation!,
              defensePlayers: defenseFormation ? defensePlayers : attackPlayers,
              analysisFocus: matchData.analysisFocus || 'balanced',
            }, authToken);
            if (result.success) console.log('✅ Squad saved to backend!');
            else console.warn('⚠️ Backend save failed:', result.message);
          }
        } catch (e) {
          console.warn('⚠️ Backend save failed (local OK):', e);
        }
      })();
      
      // ✅ Kadroyu kilitle ve tahmin durumunu güncelle
      setIsSquadLocked(true);
      setHasPrediction(true); // ✅ Hemen tahmin yapıldı olarak işaretle
      
      // ✅ 1 saniye bekle ve Tahmin sekmesine geç
      setTimeout(() => {
        setIsSaving(false);
        savingRef.current = false; // ✅ savePartialState'i tekrar aktif et
        if (__DEV__) console.log('🔄 Switching to Prediction tab...');
        InteractionManager.runAfterInteractions(() => {
          if (__DEV__) console.log('🔄 onComplete() called');
          onComplete();
        });
      }, 1000);
    } catch (error) {
      console.error('Error saving squad:', error);
      setIsSaving(false);
      savingRef.current = false; // ✅ Hata durumunda da resetle
      showInfo(t('common.error'), t('matchSquad.squadSaveFailed'));
    }
  };

  const positions = selectedFormation ? formationPositions[selectedFormation] || formationPositions['4-3-3'] : null;
  const formation = formations.find(f => f.id === selectedFormation);
  const selectedCount = Object.keys(selectedPlayers).filter(k => selectedPlayers[parseInt(k)]).length;
  
  // Calculate attack and defense counts for button activation
  const attackCount = Object.keys(attackPlayers).filter(k => attackPlayers[parseInt(k)]).length;
  const defenseCount = Object.keys(defensePlayers).filter(k => defensePlayers[parseInt(k)]).length;
  
  // ✅ Tamamla: atak 11 VE (defans yok VEYA defans 11) olduğunda aktif
  // ✅ Kilit açıldıktan sonra (isSquadLocked === false) oyuncu değişikliği yapıldığında otomatik olarak aktif olur
  // ✅ Formasyon değişikliği yapılmadan sadece oyuncu değiştirildiğinde de aktif olmalı
  const isCompleteButtonActive = attackCount === 11 && (!defenseFormation || defenseCount === 11);

  // Empty State (No Formation Selected) - saha aynı minHeight ile küçülmesin
  // ✅ Maç başlamamış ve tahmin yapılmamışsa: Formasyon seç butonu göster
  // ✅ Canlı veya biten maçta tahmin yapmayan kullanıcılar için boş saha gösterilmez - otomatik kadro gösterilir
  // ✅ Popup açıkken empty state gösterilmez
  // ✅ isKadroLocked && !hasPrediction → empty state ASLA gösterilmez (canlı + biten maç dahil)
  if (!selectedFormation && 
      !(isKadroLocked && !hasPrediction) && 
      !((isMatchLive || isMatchFinished) && !hasPrediction && autoGeneratedSquad) && 
      !showStartingXIPopup) {
    return (
      <View style={styles.container}>
        <View style={styles.emptyStateContainer}>
          <FootballField style={[styles.mainField, fieldDynamicStyle]}>
            <View style={styles.emptyStateContent}>
              <Animated.View style={[styles.emptyStateBall, animatedBallStyle]}>
                <Text style={styles.emptyStateBallEmoji}>⚽</Text>
              </Animated.View>
              <Text style={styles.emptyStateTitle}>Kadro Oluştur</Text>
              <Text style={styles.emptyStateSubtitle}>
                Tahminlerinize başlamak için atak ve defans formasyonlarını seçip oyuncuları pozisyonlarına atayın
              </Text>
            </View>
          </FootballField>

          <TouchableOpacity
            style={styles.selectFormationButton}
            onPress={() => {
              if (isKadroLocked) { showKadroLockedToast(); return; }
              // ✅ Kadro kilitliyken formasyon değişikliğine izin verme
              if (isSquadLocked) {
                showInfo(t('matchSquad.squadLocked'), t('matchSquad.squadLockedFormationChange'));
                return;
              }
              setShowFormationModal(true);
            }}
            activeOpacity={0.8}
          >
            <LinearGradient
              colors={['#1FA2A6', '#047857']}
              start={{ x: 0, y: 0 }}
              end={{ x: 1, y: 0 }}
              style={styles.buttonGradient}
            >
              <Ionicons name="chevron-down" size={20} color="#FFFFFF" />
              <Text style={styles.buttonText}>Formasyon Seç</Text>
            </LinearGradient>
          </TouchableOpacity>
        </View>

        {/* Formation Modal - Boş state'te de defans seçilebilir (atak 11 tamamlandıysa) */}
        <FormationModal
          visible={showFormationModal}
          formations={formations}
          formationType={formationType}
          onSelect={handleFormationSelect}
          onClose={() => {
            setShowFormationModal(false);
            setEditingMode(formationType === 'defense' ? 'defense' : 'attack');
          }}
          onTabChange={(type: string) => {
            setFormationType(type);
            if (type === 'defense') {
              setEditingMode('defense');
            } else {
              setEditingMode('attack');
            }
          }}
          canSelectDefense={attackFormation != null && attackCount === 11}
          currentAttackFormation={attackFormation}
          currentDefenseFormation={defenseFormation}
          communityDataVisible={communityDataVisible}
        />
      </View>
    );
  }

  // Main Squad Screen (Formation Selected)
  const showNoPredictionOverlay = hasCheckedSquadStorage && isKadroLocked && !hasPrediction && Object.values(selectedPlayers).every(p => p == null);
  const isMock999999NoPrediction = isMockLive999999(matchIdNum) && !hasPrediction && hasCheckedSquadStorage;

  // ✅ Mock 999999 tahmin yapılmamış: Sadece boş saha + "Kadro Oluşturulmadı", formasyon/oyuncu grid yok
  if (isMock999999NoPrediction) {
    return (
      <View style={styles.container}>
        <View style={styles.mainContainer}>
          <FootballField style={[styles.mainField, fieldDynamicStyle]}>
            <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center', paddingHorizontal: 24 }}>
              <View style={{ width: 48, height: 48, borderRadius: 24, backgroundColor: 'rgba(255,255,255,0.12)', alignItems: 'center', justifyContent: 'center', marginBottom: 8 }}>
                <Ionicons name="help-circle" size={32} color="#FFFFFF" />
              </View>
              <Text style={{ color: '#FFFFFF', fontSize: 13, fontWeight: '700', textAlign: 'center', textShadowColor: 'rgba(0,0,0,0.5)', textShadowOffset: { width: 0, height: 1 }, textShadowRadius: 2 }}>
                Kadro Oluşturulmadı
              </Text>
              <Text style={{ color: 'rgba(255,255,255,0.9)', fontSize: 11, textAlign: 'center', marginTop: 4, textShadowColor: 'rgba(0,0,0,0.5)', textShadowOffset: { width: 0, height: 1 }, textShadowRadius: 1 }}>
                Bu maç için kadro tahmini yapılmadı
              </Text>
            </View>
          </FootballField>
          <TouchableOpacity
            activeOpacity={0.8}
            onPress={() => setShowNoSquadInfoModal(true)}
            style={{ marginHorizontal: 12, marginTop: 8, marginBottom: 12, paddingVertical: 10, paddingHorizontal: 12, backgroundColor: '#263E3C', borderRadius: 10, borderWidth: 1, borderColor: 'rgba(45,212,191,0.35)', flexDirection: 'row', alignItems: 'center', gap: 8 }}
          >
            <Ionicons name="information-circle-outline" size={18} color="#1FA2A6" style={{ flexShrink: 0 }} />
            <Text style={{ color: '#5EEAD4', fontSize: 12, flex: 1 }} numberOfLines={2}>
              Kadro tahmini yapmadığınız için tahmin yapamazsınız. İzlemek için ilgili sekmelere geçin.
            </Text>
            <Ionicons name="chevron-forward" size={16} color="rgba(94, 234, 212, 0.7)" />
          </TouchableOpacity>
          <Modal visible={showNoSquadInfoModal} transparent animationType="fade">
            <TouchableOpacity activeOpacity={1} style={{ flex: 1, backgroundColor: 'rgba(0,0,0,0.5)', justifyContent: 'center', padding: 24 }} onPress={() => setShowNoSquadInfoModal(false)}>
              <TouchableOpacity activeOpacity={1} onPress={(e) => e.stopPropagation()} style={{ backgroundColor: '#1E3A3A', borderRadius: 16, padding: 20, borderWidth: 1, borderColor: 'rgba(31, 162, 166, 0.3)' }}>
                <View style={{ flexDirection: 'row', alignItems: 'flex-start', gap: 10, marginBottom: 16 }}>
                  <Ionicons name="eye-outline" size={22} color="#1FA2A6" style={{ flexShrink: 0 }} />
                  <Text style={{ color: '#E2E8F0', fontSize: 14, lineHeight: 22, flex: 1 }}>Kadro tahmini yapmadığınız için tahmin yapamazsınız. İzlemek için aşağıdaki sekmelere geçin:</Text>
                </View>
                {onNavigateToTab ? (
                  <View style={{ gap: 10 }}>
                    <View style={{ backgroundColor: 'rgba(31, 162, 166, 0.15)', borderRadius: 10, overflow: 'hidden' }}>
                      <View style={{ flexDirection: 'row', alignItems: 'center', gap: 8, paddingVertical: 8, paddingHorizontal: 12 }}>
                        <Ionicons name="analytics-outline" size={18} color="#5EEAD4" />
                        <Text style={{ color: '#5EEAD4', fontWeight: '600' }}>Tahmin</Text>
                      </View>
                      <TouchableOpacity onPress={() => { onNavigateToTab('prediction', { predictionSubIndex: 1 }); setShowNoSquadInfoModal(false); }} style={{ flexDirection: 'row', alignItems: 'center', gap: 8, paddingVertical: 8, paddingHorizontal: 12, paddingLeft: 38, borderTopWidth: 1, borderTopColor: 'rgba(31, 162, 166, 0.2)' }}>
                        <Ionicons name="people-outline" size={16} color="#99F6E4" />
                        <Text style={{ color: '#E0F2FE', fontSize: 13 }}>Topluluk verileri</Text>
                        <View style={{ flex: 1 }} />
                        <Ionicons name="chevron-forward" size={14} color="rgba(94, 234, 212, 0.7)" />
                      </TouchableOpacity>
                      <TouchableOpacity onPress={() => { onNavigateToTab('prediction', { predictionSubIndex: 2 }); setShowNoSquadInfoModal(false); }} style={{ flexDirection: 'row', alignItems: 'center', gap: 8, paddingVertical: 8, paddingHorizontal: 12, paddingLeft: 38, borderTopWidth: 1, borderTopColor: 'rgba(31, 162, 166, 0.2)' }}>
                        <Ionicons name="football-outline" size={16} color="#99F6E4" />
                        <Text style={{ color: '#E0F2FE', fontSize: 13 }}>Gerçek</Text>
                        <View style={{ flex: 1 }} />
                        <Ionicons name="chevron-forward" size={14} color="rgba(94, 234, 212, 0.7)" />
                      </TouchableOpacity>
                    </View>
                    <TouchableOpacity onPress={() => { onNavigateToTab('live'); setShowNoSquadInfoModal(false); }} style={{ flexDirection: 'row', alignItems: 'center', gap: 8, paddingVertical: 10, paddingHorizontal: 12, backgroundColor: 'rgba(31, 162, 166, 0.2)', borderRadius: 10 }}>
                      <Ionicons name="pulse-outline" size={18} color="#5EEAD4" />
                      <View><Text style={{ color: '#5EEAD4', fontWeight: '600' }}>Canlı</Text><Text style={{ color: 'rgba(94, 234, 212, 0.8)', fontSize: 12 }}>Olaylar</Text></View>
                    </TouchableOpacity>
                    <TouchableOpacity onPress={() => { onNavigateToTab('stats'); setShowNoSquadInfoModal(false); }} style={{ flexDirection: 'row', alignItems: 'center', gap: 8, paddingVertical: 10, paddingHorizontal: 12, backgroundColor: 'rgba(31, 162, 166, 0.2)', borderRadius: 10 }}>
                      <Ionicons name="bar-chart-outline" size={18} color="#5EEAD4" />
                      <View><Text style={{ color: '#5EEAD4', fontWeight: '600' }}>İstatistik</Text><Text style={{ color: 'rgba(94, 234, 212, 0.8)', fontSize: 12 }}>Canlı istatistikler</Text></View>
                    </TouchableOpacity>
                  </View>
                ) : null}
                <TouchableOpacity onPress={() => setShowNoSquadInfoModal(false)} style={{ marginTop: 16, paddingVertical: 10, alignItems: 'center', borderRadius: 10, backgroundColor: 'rgba(100, 116, 139, 0.2)' }}>
                  <Text style={{ color: '#94A3B8' }}>Kapat</Text>
                </TouchableOpacity>
              </TouchableOpacity>
            </TouchableOpacity>
          </Modal>
        </View>
      </View>
    );
  }

  return (
    <View style={styles.container}>
      <View style={styles.mainContainer}>
        {/* Football Field with Players */}
        <FootballField style={[styles.mainField, fieldDynamicStyle]}>
          {showNoPredictionOverlay && (
            <View style={{ position: 'absolute', top: 0, left: 0, right: 0, bottom: 0, alignItems: 'center', justifyContent: 'center', zIndex: 20, paddingHorizontal: 24 }}>
              <View style={{ width: 48, height: 48, borderRadius: 24, backgroundColor: 'rgba(255,255,255,0.12)', alignItems: 'center', justifyContent: 'center', marginBottom: 8 }}>
                <Ionicons name="help-circle" size={32} color="#FFFFFF" />
              </View>
              <Text style={{ color: '#FFFFFF', fontSize: 13, fontWeight: '700', textAlign: 'center', textShadowColor: 'rgba(0,0,0,0.5)', textShadowOffset: { width: 0, height: 1 }, textShadowRadius: 2 }}>
                Kadro Oluşturulmadı
              </Text>
              <Text style={{ color: 'rgba(255,255,255,0.9)', fontSize: 11, textAlign: 'center', marginTop: 4, textShadowColor: 'rgba(0,0,0,0.5)', textShadowOffset: { width: 0, height: 1 }, textShadowRadius: 1 }}>
                Bu maç için kadro tahmini yapılmadı
              </Text>
            </View>
          )}
          <View style={styles.playersContainer}>
            {positions?.map((pos, index) => {
              const player = selectedPlayers[index];
              const positionLabel = formation?.positions[index] || '';

              return (
                <View
                  key={player ? `player-${player.id}-${index}` : `slot-${index}`} // ✅ Stable key - sıçramayı önler
                  style={[
                    styles.playerSlot,
                    { left: `${pos.x}%`, top: `${pos.y}%` }, // ✅ Sabit pozisyon - animasyon yok
                  ]}
                >
                  {player ? (() => {
                    const subOut = subOutData?.players[player.id] ?? subOutData?.players[String(player.id)];
                    const total = subOutData?.totalPredictors ?? 1;
                    const subOutRate = subOut ? subOut.subOutVotes / total : 0;
                    const replacementName = subOut?.replacementName ?? null;
                    // ✅ "i" ikonu sadece kullanıcı tahmin yapmışsa gösterilir (kendi kadrosundaki topluluk "oyundan çıksın" bilgisi)
                    const showSubOut = subOutRate >= 0.10 && hasPrediction;
                    const subOutTier = subOutRate >= 0.30 ? 'high' : subOutRate >= 0.20 ? 'mid' : 'low';
                    const showSubOutAlert = () => {
                      const pct = Math.round(subOutRate * 100);
                      let msg = t('matchSquad.substitutionCommunityTooltip', { pct });
                      if (replacementName) msg += `\n\nBu görüşte olanların bir kısmı yerine ${replacementName} girmeli diyor.`;
                      showInfo(t('matchSquad.communityOpinion'), msg);
                    };
                    return (
                    <View style={[
                      styles.playerCardWrapper,
                      showSubOut && styles.subOutOuter,
                      showSubOut && subOutTier === 'low' && styles.subOutBorderThin,
                      showSubOut && subOutTier === 'mid' && styles.subOutBorderThick,
                      showSubOut && subOutTier === 'high' && styles.subOutBorderPulse,
                    ]}>
                        {showSubOut && (
                          <TouchableOpacity
                            style={styles.subOutInfoIcon}
                            onPress={showSubOutAlert}
                            hitSlop={{ top: 6, bottom: 6, left: 6, right: 6 }}
                          >
                            <Ionicons name="information-circle" size={20} color="#EF4444" />
                          </TouchableOpacity>
                        )}
                        {/* Remove button - Top Right */}
                        {/* ✅ X butonu: Kadro düzenlenebilir durumda gösterilir (maç başlamamış VE kadro kilitli değil) */}
                        {!effectiveSquadLocked && (
                          <TouchableOpacity
                            style={[styles.removeButton, { pointerEvents: 'auto' }]}
                            onPress={(e) => {
                              e?.stopPropagation?.();
                              handleRemovePlayer(index);
                            }}
                            activeOpacity={0.7}
                            hitSlop={{ top: 12, bottom: 12, left: 12, right: 12 }}
                          >
                            <Ionicons name="close" size={16} color="#FFFFFF" />
                          </TouchableOpacity>
                        )}

                        {/* Player Card - Main */}
                        <TouchableOpacity
                          style={[
                            styles.playerCard,
                            // Gold border for elite players (85+)
                            normalizeRatingTo100(player.rating) >= 85 && styles.playerCardElite,
                            // Blue border for goalkeepers
                            isGoalkeeperPlayer(player) && styles.playerCardGK,
                            // ✅ Tercih çerçevesi (tahmin yapmayan kullanıcılar için)
                            (() => {
                              if (!autoGeneratedSquad || hasPrediction) return null;
                              const posData = autoGeneratedSquad.positions.find(p => p.player.id === player.id);
                              if (!posData) return null;
                              const borderStyle = getPreferenceBorderStyle(posData.preferencePercentage);
                              return {
                                borderColor: borderStyle.color,
                                borderWidth: borderStyle.width,
                              };
                            })(),
                          ]}
                          onPress={() => {
                            // ✅ Tercih popup'ı göster (tahmin yapmayan kullanıcılar için)
                            if (autoGeneratedSquad && !hasPrediction) {
                              const posData = autoGeneratedSquad.positions.find(p => p.player.id === player.id);
                              if (posData) {
                                setShowPreferencePopup({
                                  position: posData.position,
                                  positionLabel: posData.positionLabel,
                                  player: { ...posData.player, stats: (player as any).stats, rating: (player as any).rating },
                                  preferencePercentage: posData.preferencePercentage,
                                  totalPreferences: posData.totalPreferences,
                                  allPreferences: posData.allPreferences,
                                });
                                return;
                              }
                            }
                            setSelectedPlayerForDetail(player);
                          }}
                          onLongPress={isKadroLocked ? undefined : () => handleRemovePlayer(index)}
                          activeOpacity={0.8}
                        >
                          <LinearGradient
                            colors={['#1E3A3A', '#0F2A24']} // ✅ Design System
                            style={styles.playerCardGradient}
                          >
                            {/* Injury/Alert Badge - Top Right */}
                            {player.injury && (
                              <View style={styles.alertBadge}>
                                <View style={styles.alertDot} />
                              </View>
                            )}

                            {/* Jersey Number Badge - Top Center */}
                            <View style={styles.jerseyNumberBadge}>
                              <Text style={styles.jerseyNumberText}>
                                {(player.number ?? player.shirt_number ?? player.jersey_number) != null && (player.number ?? player.shirt_number ?? player.jersey_number) > 0 ? (player.number ?? player.shirt_number ?? player.jersey_number) : '-'}
                              </Text>
                            </View>

                            {/* Player Name - Tek satır, sadece soyad */}
                            <Text style={styles.playerName} numberOfLines={1}>
                              {formatPlayerSurname(player)}
                            </Text>

                            {/* Rating and Position - Same row, bottom */}
                            <View style={styles.playerBottomRow}>
                              <Text style={styles.playerRatingBottom}>{formatRatingDisplay(player.rating)}</Text>
                              <Text style={styles.playerPositionBottom}>{positionLabel}</Text>
                            </View>

                            {/* Form indicator - Subtle glow */}
                            {player.form >= 8 && (
                              <View style={styles.formGlow} />
                            )}
                          </LinearGradient>
                        </TouchableOpacity>
                      </View>
                    );
                  })() : (
                    <TouchableOpacity
                      style={styles.emptySlot}
                      onPress={() => {
                        if (isKadroLocked) { showKadroLockedToast(); return; }
                        // ✅ Kullanıcı kilidi kontrolü
                        if (isSquadLocked) {
                          // ✅ Topluluk verileri görüldüyse kilit açılamaz
                          if (hasViewedCommunityData) {
                            showInfo(
                              '🔒 Kalıcı Kilit',
                              'Topluluk verilerini gördüğünüz için kadro kilidi açılamaz. Tahminleriniz kalıcı olarak kilitlidir.'
                            );
                            return;
                          }
                          showAlert(
                            '🔒 Kadro Kilitli',
                            'Kadro tamamlandı ve kilitli. Değişiklik yapmak için kilidi açın.',
                            [
                              { text: 'İptal', style: 'cancel' },
                              { text: 'Kilidi Aç', style: 'destructive', onPress: () => setIsSquadLocked(false) },
                            ]
                          );
                          return;
                        }
                        setSelectedSlot(index);
                        setShowPlayerModal(true);
                      }}
                      activeOpacity={0.7}
                    >
                      <View style={styles.emptySlotContent}>
                        <Ionicons name="add" size={20} color="rgba(255, 255, 255, 0.7)" />
                        <Text style={styles.emptySlotText}>{positionLabel}</Text>
                      </View>
                    </TouchableOpacity>
                  )}
                </View>
              );
            })}
          </View>
        </FootballField>

        {/* ✅ Kullanıcı tercih istatistikleri kartı - Sol: Kullanıcı sayısı, Sağ: Formasyonlar (2 satır, tıklanabilir) */}
        {/* Saha altında her zaman gösterilir - tahmin yapılsın ya da yapılmasın */}
        {isVisible && (isMatchLive || isMatchFinished) && !showStartingXIPopup && userPreferenceStats && autoGeneratedSquad && (
          <View style={styles.preferenceStatsCardNew}>
            {/* Sol: Kullanıcı sayısı */}
            <TouchableOpacity 
              style={styles.preferenceStatsLeftSection}
              onPress={() => setShowFullPreferenceSummary(true)}
              activeOpacity={0.7}
            >
              <Ionicons name="people" size={16} color="#1FA2A6" />
              <Text style={styles.preferenceStatsUserCount}>
                {userPreferenceStats.totalUsers.toLocaleString()}
              </Text>
              <Text style={styles.preferenceStatsUserLabel}>kullanıcı</Text>
            </TouchableOpacity>
            
            {/* Sağ: Formasyonlar (2 satır) */}
            <View style={styles.preferenceStatsRightSection}>
              {/* Atak Formasyonu - Tıklanabilir */}
              <TouchableOpacity 
                style={styles.preferenceFormationRowClickable}
                onPress={() => {
                  setShowFormationDetailPopup('attack');
                  setEditingMode('attack'); // ✅ Sahada atak formasyonunu göster
                  setViewSource('community'); // ✅ Topluluk verilerine göre göster
                }}
                activeOpacity={0.7}
              >
                <Ionicons name="flash" size={12} color="#F59E0B" />
                <Text style={styles.preferenceFormationLabel}>Atak</Text>
                <Text style={styles.preferenceFormationValue}>{autoGeneratedSquad.attackFormation.formation}</Text>
                <Text style={styles.preferenceFormationPercent}>%{autoGeneratedSquad.attackFormation.percentage}</Text>
                <Ionicons name="chevron-forward" size={12} color="#6B7280" />
              </TouchableOpacity>
              
              {/* Defans Formasyonu - Tıklanabilir */}
              <TouchableOpacity 
                style={styles.preferenceFormationRowClickable}
                onPress={() => {
                  setShowFormationDetailPopup('defense');
                  setEditingMode('defense'); // ✅ Sahada defans formasyonunu göster
                  setViewSource('community'); // ✅ Topluluk verilerine göre göster
                }}
                activeOpacity={0.7}
              >
                <Ionicons name="shield" size={12} color="#3B82F6" />
                <Text style={styles.preferenceFormationLabel}>Defans</Text>
                <Text style={styles.preferenceFormationValue}>{autoGeneratedSquad.defenseFormation.formation}</Text>
                <Text style={styles.preferenceFormationPercent}>%{autoGeneratedSquad.defenseFormation.percentage}</Text>
                <Ionicons name="chevron-forward" size={12} color="#6B7280" />
              </TouchableOpacity>
            </View>
          </View>
        )}

        {/* Kadro oluşturulmadığında: kompakt bildirim, tıklanınca popup */}
        {showNoPredictionOverlay && (
          <>
            <TouchableOpacity
              activeOpacity={0.8}
              onPress={() => setShowNoSquadInfoModal(true)}
              style={{
                marginHorizontal: 12, marginTop: 8, marginBottom: 12,
                paddingVertical: 10, paddingHorizontal: 12,
                backgroundColor: '#263E3C', borderRadius: 10,
                borderWidth: 1, borderColor: 'rgba(45,212,191,0.35)',
                flexDirection: 'row', alignItems: 'center', gap: 8,
              }}
            >
              <Ionicons name="information-circle-outline" size={18} color="#1FA2A6" style={{ flexShrink: 0 }} />
              <Text style={{ color: '#5EEAD4', fontSize: 12, flex: 1 }} numberOfLines={2}>
                Kadro tahmini yapmadığınız için tahmin yapamazsınız. İzlemek için ilgili sekmelere geçin.
              </Text>
              <Ionicons name="chevron-forward" size={16} color="rgba(94, 234, 212, 0.7)" />
            </TouchableOpacity>
            <Modal visible={showNoSquadInfoModal} transparent animationType="fade">
              <TouchableOpacity activeOpacity={1} style={{ flex: 1, backgroundColor: 'rgba(0,0,0,0.5)', justifyContent: 'center', padding: 24 }} onPress={() => setShowNoSquadInfoModal(false)}>
                <TouchableOpacity activeOpacity={1} onPress={(e) => e.stopPropagation()} style={{ backgroundColor: '#1E3A3A', borderRadius: 16, padding: 20, borderWidth: 1, borderColor: 'rgba(31, 162, 166, 0.3)' }}>
                  <View style={{ flexDirection: 'row', alignItems: 'flex-start', gap: 10, marginBottom: 16 }}>
                    <Ionicons name="eye-outline" size={22} color="#1FA2A6" style={{ flexShrink: 0 }} />
                    <Text style={{ color: '#E2E8F0', fontSize: 14, lineHeight: 22, flex: 1 }}>
                      Kadro tahmini yapmadığınız için tahmin yapamazsınız. İzlemek için aşağıdaki sekmelere geçin:
                    </Text>
                  </View>
                  {onNavigateToTab ? (
                    <View style={{ gap: 10 }}>
                      <View style={{ backgroundColor: 'rgba(31, 162, 166, 0.15)', borderRadius: 10, overflow: 'hidden' }}>
                        <View style={{ flexDirection: 'row', alignItems: 'center', gap: 8, paddingVertical: 8, paddingHorizontal: 12 }}>
                          <Ionicons name="analytics-outline" size={18} color="#5EEAD4" />
                          <Text style={{ color: '#5EEAD4', fontWeight: '600' }}>Tahmin</Text>
                        </View>
                        <TouchableOpacity onPress={() => { onNavigateToTab('prediction', { predictionSubIndex: 1 }); setShowNoSquadInfoModal(false); }} style={{ flexDirection: 'row', alignItems: 'center', gap: 8, paddingVertical: 8, paddingHorizontal: 12, paddingLeft: 38, borderTopWidth: 1, borderTopColor: 'rgba(31, 162, 166, 0.2)' }}>
                          <Ionicons name="people-outline" size={16} color="#99F6E4" />
                          <Text style={{ color: '#E0F2FE', fontSize: 13 }}>Topluluk verileri</Text>
                          <View style={{ flex: 1 }} />
                          <Ionicons name="chevron-forward" size={14} color="rgba(94, 234, 212, 0.7)" />
                        </TouchableOpacity>
                        <TouchableOpacity onPress={() => { onNavigateToTab('prediction', { predictionSubIndex: 2 }); setShowNoSquadInfoModal(false); }} style={{ flexDirection: 'row', alignItems: 'center', gap: 8, paddingVertical: 8, paddingHorizontal: 12, paddingLeft: 38, borderTopWidth: 1, borderTopColor: 'rgba(31, 162, 166, 0.2)' }}>
                          <Ionicons name="football-outline" size={16} color="#99F6E4" />
                          <Text style={{ color: '#E0F2FE', fontSize: 13 }}>Gerçek</Text>
                          <View style={{ flex: 1 }} />
                          <Ionicons name="chevron-forward" size={14} color="rgba(94, 234, 212, 0.7)" />
                        </TouchableOpacity>
                      </View>
                      <TouchableOpacity onPress={() => { onNavigateToTab('live'); setShowNoSquadInfoModal(false); }} style={{ flexDirection: 'row', alignItems: 'center', gap: 8, paddingVertical: 10, paddingHorizontal: 12, backgroundColor: 'rgba(31, 162, 166, 0.2)', borderRadius: 10 }}>
                        <Ionicons name="pulse-outline" size={18} color="#5EEAD4" />
                        <View><Text style={{ color: '#5EEAD4', fontWeight: '600' }}>Canlı</Text><Text style={{ color: 'rgba(94, 234, 212, 0.8)', fontSize: 12 }}>Olaylar</Text></View>
                      </TouchableOpacity>
                      <TouchableOpacity onPress={() => { onNavigateToTab('stats'); setShowNoSquadInfoModal(false); }} style={{ flexDirection: 'row', alignItems: 'center', gap: 8, paddingVertical: 10, paddingHorizontal: 12, backgroundColor: 'rgba(31, 162, 166, 0.2)', borderRadius: 10 }}>
                        <Ionicons name="bar-chart-outline" size={18} color="#5EEAD4" />
                        <View><Text style={{ color: '#5EEAD4', fontWeight: '600' }}>İstatistik</Text><Text style={{ color: 'rgba(94, 234, 212, 0.8)', fontSize: 12 }}>Canlı istatistikler</Text></View>
                      </TouchableOpacity>
                    </View>
                  ) : null}
                  <TouchableOpacity onPress={() => setShowNoSquadInfoModal(false)} style={{ marginTop: 16, paddingVertical: 10, alignItems: 'center', borderRadius: 10, backgroundColor: 'rgba(100, 116, 139, 0.2)' }}>
                    <Text style={{ color: '#94A3B8', fontWeight: '600' }}>Kapat</Text>
                  </TouchableOpacity>
                </TouchableOpacity>
              </TouchableOpacity>
            </Modal>
          </>
        )}

        {/* ✅ Saha altı: Simetrik toolbar [Formasyon] | 🔓 | [Tamamla] – 5px yukarı (Kadro sekmesi konteyner) */}
        {(hasPrediction || !isKadroLocked) && (
          <View style={[styles.bottomBar, { marginTop: 11 }, isLight && { backgroundColor: themeColors.muted, borderColor: themeColors.border }]}>
            <View style={styles.bottomBarLeft}>
              {/* SİMETRİK TOOLBAR: [2 Satırlı Formasyon] | 🔓 | [Tamamla] */}
              <View style={styles.symmetricToolbar}>
                {/* Sol: 2 Satırlı Formasyon Butonu */}
                <View style={[
                    styles.dualLineFormationButton,
                    isLight && { backgroundColor: themeColors.card, borderColor: themeColors.border },
                    isSquadLocked && { opacity: 0.5 } // ✅ Kilitliyken soluk göster
                  ]}>
                  {/* Satır 1: Atak - Tıklanınca sahada göster, kilit açıksa düzenlenebilir */}
                  <TouchableOpacity
                    style={[
                      styles.formationLine,
                      editingMode === 'attack' && styles.formationLineActive
                    ]}
                    onPress={() => {
                      // ✅ Atak formasyonu varsa sahada göster
                      if (attackFormation) {
                        setEditingMode('attack');
                        setFormationType('attack');
                        // Kilit açıksa formasyon değiştirme popup'ı da aç
                        if (!isSquadLocked) {
                          setShowFormationModal(true);
                        }
                      } else if (!isSquadLocked) {
                        // Formasyon yoksa ve kilit açıksa popup aç
                        setFormationType('attack');
                        setShowFormationModal(true);
                      } else {
                        showInfo(t('matchSquad.squadLocked'), t('matchSquad.squadLockedSelectFormation'));
                      }
                    }}
                    activeOpacity={0.7}
                  >
                    <Ionicons name="flash" size={14} color={editingMode === 'attack' ? '#F59E0B' : (isLight ? themeColors.ring : '#1FA2A6')} />
                    <Text style={[
                      styles.formationLineText,
                      isLight && { color: themeColors.mutedForeground },
                      editingMode === 'attack' && (isLight ? styles.formationLineTextActive : styles.formationLineTextActive),
                      editingMode === 'attack' && isLight && { color: themeColors.foreground }
                    ]} numberOfLines={1}>
                      {attackFormation 
                        ? formations.find(f => f.id === attackFormation)?.name || attackFormation
                        : 'Formasyon Seç'}
                    </Text>
                  </TouchableOpacity>
                  {/* Satır 2: Defans - Tıklanınca sahada göster, kilit açıksa düzenlenebilir */}
                  <TouchableOpacity
                    style={[
                      styles.formationLine,
                      editingMode === 'defense' && styles.formationLineActive
                    ]}
                    onPress={() => {
                      // ✅ Önce atak formasyonu olmalı
                      if (!attackFormation) {
                        showInfo(t('matchSquad.firstAttack'), t('matchSquad.selectDefenseFirst'));
                        return;
                      }
                      
                      // ✅ Defans formasyonu varsa sahada göster
                      if (defenseFormation) {
                        setEditingMode('defense');
                        setFormationType('defense');
                        // Kilit açıksa formasyon değiştirme popup'ı da aç
                        if (!isSquadLocked) {
                          setShowFormationModal(true);
                        }
                      } else if (!isSquadLocked) {
                        // Defans formasyonu yoksa ve kilit açıksa popup aç
                        setFormationType('defense');
                        setShowFormationModal(true);
                      } else {
                        showInfo(t('matchSquad.squadLocked'), t('matchSquad.squadLockedSelectFormation'));
                      }
                    }}
                    activeOpacity={0.7}
                  >
                    <Ionicons name="shield" size={14} color={editingMode === 'defense' ? '#3B82F6' : (isLight ? themeColors.ring : '#1FA2A6')} />
                    <Text style={[
                      styles.formationLineText,
                      isLight && { color: themeColors.mutedForeground },
                      editingMode === 'defense' && styles.formationLineTextActive,
                      editingMode === 'defense' && isLight && { color: themeColors.foreground }
                    ]} numberOfLines={1}>
                      {defenseFormation 
                        ? formations.find(f => f.id === defenseFormation)?.name || defenseFormation
                        : ''}
                    </Text>
                  </TouchableOpacity>
                </View>

                {/* Orta: Kilit - Her zaman görünür. Maç başladı/bitince açılamaz (kalıcı kilit). */}
                <TouchableOpacity
                  style={[
                    styles.lockButtonCenter,
                    effectiveSquadLocked ? styles.lockButtonCenterLocked : styles.lockButtonCenterOpen,
                    hasViewedCommunityData && isSquadLocked && { opacity: 0.5 },
                    isKadroLocked && { opacity: 0.9 }
                  ]}
                  onPress={() => {
                    if (squadEditingDisabled) {
                      showInfo('Tahmin kilitli', 'Kadro düzenlemek için önce Tahmin sekmesinde kilidi açın.');
                      return;
                    }
                    if (isKadroLocked) {
                      showInfo('🔒 ' + t('matchSquad.squadLocked'), t('matchSquad.lockCannotOpen'));
                      return;
                    }
                    if (isSquadLocked) {
                      if (hasViewedCommunityData) {
                        showInfo(
                          '🔒 Kalıcı Kilit',
                          'Topluluk verilerini gördüğünüz için kadro kilidi açılamaz. Tahminleriniz kalıcı olarak kilitlidir.'
                        );
                        return;
                      }
                      setIsSquadLocked(false);
                      setHasModifiedSinceUnlock(false);
                    } else {
                      if (isCompleteButtonActive) {
                        handleComplete();
                      } else {
                        showInfo(t('matchSquad.squadLocked'), t('matchSquad.incompleteSquad'));
                      }
                    }
                  }}
                  activeOpacity={effectiveSquadLocked ? 1 : 0.7}
                >
                  <Ionicons 
                    name={effectiveSquadLocked ? "lock-closed" : "lock-open"} 
                    size={20} 
                    color={effectiveSquadLocked ? '#EF4444' : '#10B981'} 
                  />
                </TouchableOpacity>

                {/* Sağ: Tamamla - Her zaman görünür. Maç başladı/bitince veya kilitliyken inaktif */}
                <TouchableOpacity
                  style={[
                    styles.completeButtonSymmetric,
                    isLight && (effectiveSquadLocked || !isCompleteButtonActive) && { backgroundColor: themeColors.border, opacity: 0.8 },
                    isLight && !(effectiveSquadLocked || !isCompleteButtonActive) && { backgroundColor: themeColors.ring || '#1FA2A6' },
                    !isLight && (effectiveSquadLocked || !isCompleteButtonActive) && styles.completeButtonSymmetricDisabled,
                  ]}
                  onPress={handleComplete}
                  disabled={effectiveSquadLocked || !isCompleteButtonActive}
                  activeOpacity={0.8}
                >
                  <Text style={[
                    styles.completeButtonSymmetricText,
                    isLight && { color: (effectiveSquadLocked || !isCompleteButtonActive) ? themeColors.mutedForeground : themeColors.primaryForeground || '#FFFFFF' },
                    (effectiveSquadLocked || !isCompleteButtonActive) && !isLight && { opacity: 0.5 }
                  ]}>Tamamla</Text>
                  <Ionicons 
                    name="checkmark-circle" 
                    size={18} 
                    color={(effectiveSquadLocked || !isCompleteButtonActive) ? (isLight ? themeColors.mutedForeground : 'rgba(255,255,255,0.5)') : (isLight ? themeColors.primaryForeground || '#FFFFFF' : '#FFFFFF')} 
                  />
                </TouchableOpacity>
              </View>
          </View>
        </View>
        )}
      </View>

      {/* Player Selection Modal - Defans: sadece atak 11'i. Canlı maçta atak: sadece mevcut 11 (formasyon/yerleşim değişikliği aynı 11 ile). */}
      <PlayerModal
        visible={showPlayerModal}
        players={editingMode === 'defense' && attackSquadPlayers.length === 11
          ? attackSquadPlayers.filter((p: any) =>
              !Object.entries(defensePlayers).some(([i, pl]) => Number(i) !== selectedSlot && pl?.id === p.id)
            )
          : editingMode === 'attack' && isMatchLive && attackSquadPlayers.length === 11
            ? attackSquadPlayers.filter((p: any) =>
                !Object.entries(attackPlayers).some(([i, pl]) => Number(i) !== selectedSlot && pl?.id === p.id)
              )
            : attackTeamPlayers}
        selectedPlayers={selectedPlayers}
        positionLabel={selectedSlot !== null ? formation?.positions[selectedSlot] : ''}
        onSelect={handlePlayerSelect}
        onClose={() => {
          setShowPlayerModal(false);
          setSelectedSlot(null);
        }}
        isDefenseMode={editingMode === 'defense'}
        isLoadingSquad={isLoadingSquad}
        loadingFromApi={squadLoadingFromApi}
        hasBackendError={!isLoadingSquad && squadPlayers.length === 0 && !lineups?.length && editingMode === 'attack'}
        onRetrySquad={retrySquadFetch}
        communityDataVisible={communityDataVisible}
      />

      {/* ✅ İlk 11 Popup - Maç başladığında gösterilir */}
      <Modal
        visible={showStartingXIPopup}
        animationType="fade"
        transparent={true}
        onRequestClose={handleStartingXIPopupClose}
      >
        <View style={styles.startingXIPopupOverlay}>
          <View style={styles.startingXIPopupModal}>
            <LinearGradient
              colors={['#0F2A24', '#1E3A3A', '#0F2A24']}
              style={styles.startingXIPopupGradient}
            >
              {/* Header */}
              <View style={styles.startingXIPopupHeader}>
                <View style={styles.startingXIPopupTitleRow}>
                  <Ionicons name="football" size={20} color="#1FA2A6" />
                  <Text style={styles.startingXIPopupTitle}>İlk 11 Kadrosu</Text>
                </View>
                <TouchableOpacity onPress={handleStartingXIPopupClose}>
                  <Ionicons name="close-circle" size={24} color="#9CA3AF" />
                </TouchableOpacity>
              </View>
              
              {/* Takım Bilgisi */}
              {userPreferenceStats && (
                <View style={styles.startingXIPopupTeamInfo}>
                  <Text style={styles.startingXIPopupTeamName}>{userPreferenceStats.teamName}</Text>
                  <Text style={styles.startingXIPopupTeamSubtext}>
                    Topluluk tercihleri ile oluşturuldu
                  </Text>
                </View>
              )}
              
              {/* Oyuncu Listesi - Scroll yok, tek ekranda */}
              <View style={styles.startingXIPopupPlayerList}>
                {startingXIPlayers.map((playerItem: any, idx: number) => {
                  const player = playerItem.player;
                  const isGK = isGoalkeeperPlayer(player);
                  const posData = autoGeneratedSquad?.positions.find(p => p.player.id === player.id);
                  const borderStyle = posData ? getPreferenceBorderStyle(posData.preferencePercentage) : null;
                  
                  return (
                    <View 
                      key={player.id || idx} 
                      style={[
                        styles.startingXIPopupPlayerItem,
                        isGK && styles.startingXIPopupPlayerItemGK,
                        borderStyle && { borderLeftColor: borderStyle.color, borderLeftWidth: borderStyle.width }
                      ]}
                    >
                      <View style={styles.startingXIPopupPlayerNumber}>
                        <Text style={styles.startingXIPopupPlayerNumberText}>{player.number || '?'}</Text>
                      </View>
                      <View style={styles.startingXIPopupPlayerInfo}>
                        <Text style={styles.startingXIPopupPlayerName}>
                        {formatPlayerDisplayName({ ...player, name: player.name || 'Bilinmiyor' })}
                      </Text>
                        <Text style={styles.startingXIPopupPlayerPosition}>
                          {posData?.positionLabel || player.pos || player.position || 'Pozisyon'}
                        </Text>
                      </View>
                      {posData && (
                        <View style={[styles.startingXIPopupPlayerPercent, { backgroundColor: borderStyle?.color }]}>
                          <Text style={styles.startingXIPopupPlayerPercentText}>%{posData.preferencePercentage}</Text>
                        </View>
                      )}
                    </View>
                  );
                })}
              </View>
              
              {/* Footer */}
              <View style={styles.startingXIPopupFooter}>
                <Text style={styles.startingXIPopupFooterText}>
                  X tuşuna basarak sahaya geçebilirsiniz
                </Text>
              </View>
            </LinearGradient>
          </View>
        </View>
      </Modal>

      {/* ✅ "Tahminlerinizi Tamamlayın" popup'ı kaldırıldı */}
      {/* Maç başladıktan sonra sayfayı açanlar için normal "İlk 11" popup'ı gösterilir */}
      {/* Kullanıcı popup'ı kapatınca kadro sekmesinde çalışmaya devam edebilir */}

      {/* ✅ Tercih İstatistikleri Popup - Oyuncu pozisyon tercihlerini gösterir */}
      <Modal
        visible={showPreferencePopup !== null}
        animationType="fade"
        transparent={true}
        onRequestClose={() => { setShowPreferencePopup(null); setShowPlayerRatingDropdown(false); }}
      >
        <TouchableOpacity 
          style={styles.preferencePopupOverlay} 
          activeOpacity={1} 
          onPress={() => { setShowPreferencePopup(null); setShowPlayerRatingDropdown(false); }}
        >
          <View style={styles.preferencePopupModal}>
            <LinearGradient
              colors={['#1A2E2A', '#0F2420']}
              style={styles.preferencePopupGradient}
            >
              {/* Header */}
              <View style={styles.preferencePopupHeader}>
                <View style={styles.preferencePopupTitleRow}>
                  <Ionicons name="people" size={20} color="#1FA2A6" />
                  <Text style={styles.preferencePopupTitle}>Topluluk Tercihleri</Text>
                </View>
                <TouchableOpacity onPress={() => { setShowPreferencePopup(null); setShowPlayerRatingDropdown(false); }}>
                  <Ionicons name="close-circle" size={24} color="#9CA3AF" />
                </TouchableOpacity>
              </View>
              
              {/* Pozisyon Bilgisi */}
              <View style={styles.preferencePopupPositionInfo}>
                <Text style={styles.preferencePopupPositionLabel}>{showPreferencePopup?.positionLabel}</Text>
                <Text style={styles.preferencePopupPlayerName}>
                {showPreferencePopup?.player && formatPlayerDisplayName(showPreferencePopup.player)}
              </Text>
                <View style={styles.preferencePopupBadge}>
                  <Text style={styles.preferencePopupBadgeText}>%{showPreferencePopup?.preferencePercentage}</Text>
                </View>
              </View>
              
              {/* Tüm Tercihler */}
              <View style={styles.preferencePopupList}>
                <Text style={styles.preferencePopupListTitle}>Tüm Tercihler</Text>
                {showPreferencePopup?.allPreferences.map((pref, idx) => {
                  const isSelected = pref.playerId === showPreferencePopup?.player.id;
                  const borderStyle = getPreferenceBorderStyle(pref.percentage);
                  return (
                    <View 
                      key={pref.playerId} 
                      style={[
                        styles.preferencePopupItem,
                        isSelected && styles.preferencePopupItemSelected,
                        { borderLeftColor: borderStyle.color, borderLeftWidth: borderStyle.width }
                      ]}
                    >
                      <Text style={styles.preferencePopupItemRank}>#{idx + 1}</Text>
                      <Text style={[styles.preferencePopupItemName, isSelected && styles.preferencePopupItemNameSelected]}>
                        {pref.playerName}
                      </Text>
                      <View style={styles.preferencePopupItemStats}>
                        <Text style={styles.preferencePopupItemCount}>{pref.count.toLocaleString()} oy</Text>
                        <Text style={[styles.preferencePopupItemPercentage, { color: borderStyle.color }]}>
                          %{pref.percentage}
                        </Text>
                      </View>
                    </View>
                  );
                })}
              </View>
              
              {/* Toplam */}
              <View style={styles.preferencePopupTotal}>
                <Ionicons name="stats-chart" size={14} color="#9CA3AF" />
                <Text style={styles.preferencePopupTotalText}>
                  Toplam {showPreferencePopup?.totalPreferences.toLocaleString()} tercih
                </Text>
              </View>
              
              {/* ✅ Oyuncu Detaylı Reyting Dropdown */}
              <TouchableOpacity 
                style={styles.playerRatingDropdownHeader}
                onPress={() => setShowPlayerRatingDropdown(!showPlayerRatingDropdown)}
                activeOpacity={0.7}
              >
                <View style={styles.playerRatingDropdownHeaderLeft}>
                  <Ionicons name="star" size={16} color="#C9A44C" />
                  <Text style={styles.playerRatingDropdownHeaderText}>Topluluk Değerlendirmesi</Text>
                </View>
                <View style={styles.playerRatingDropdownHeaderRight}>
                  <Text style={styles.playerRatingDropdownAvg}>
                    {(() => {
                      // Mock: Oyuncu için ortalama reyting
                      const categories = [7.2, 6.8, 7.5, 6.3, 7.0, 6.9, 7.1, 6.7, 7.3];
                      const avg = categories.reduce((a, b) => a + b, 0) / categories.length;
                      return avg.toFixed(1);
                    })()}
                  </Text>
                  <Ionicons 
                    name={showPlayerRatingDropdown ? "chevron-up" : "chevron-down"} 
                    size={18} 
                    color="#C9A44C" 
                  />
                </View>
              </TouchableOpacity>
              
              {showPlayerRatingDropdown && (
                <View style={styles.playerRatingDropdownContent}>
                  {(() => {
                    const popupPlayer = showPreferencePopup?.player;
                    const isGoalkeeper = showPreferencePopup?.positionLabel?.toUpperCase() === 'GK' || 
                                         showPreferencePopup?.positionLabel?.toLowerCase().includes('kaleci');
                    const stats = popupPlayer?.stats || {};
                    const normalizedRating = normalizeRatingTo100(popupPlayer?.rating ?? null);
                    const allStatsDefault = (
                      (stats.pace ?? 70) === 70 && (stats.shooting ?? 70) === 70 &&
                      (stats.passing ?? 70) === 70 && (stats.dribbling ?? 70) === 70 &&
                      (stats.defending ?? 70) === 70 && (stats.physical ?? 70) === 70
                    );
                    const calculatedStats = allStatsDefault ? {
                      pace: normalizedRating, shooting: normalizedRating, passing: normalizedRating,
                      dribbling: normalizedRating, defending: normalizedRating, physical: normalizedRating,
                    } : {
                      pace: stats.pace ?? 70, shooting: stats.shooting ?? 70, passing: stats.passing ?? 70,
                      dribbling: stats.dribbling ?? 70, defending: stats.defending ?? 70, physical: stats.physical ?? 70,
                    };
                    const goalkeeperItems: { id: string; title: string; value: number }[] = [
                      { id: 'pace', title: 'Plonjon', value: calculatedStats.pace },
                      { id: 'shooting', title: 'Refleks', value: calculatedStats.shooting },
                      { id: 'passing', title: 'Top Tutma', value: calculatedStats.passing },
                      { id: 'dribbling', title: 'Yer Tutuş', value: calculatedStats.dribbling },
                      { id: 'defending', title: 'Hava Topu', value: calculatedStats.defending },
                      { id: 'physical', title: 'Bire Bir', value: calculatedStats.physical },
                    ];
                    const outfieldItems: { id: string; title: string; value: number }[] = [
                      { id: 'pace', title: 'Hız', value: calculatedStats.pace },
                      { id: 'shooting', title: 'Şut', value: calculatedStats.shooting },
                      { id: 'passing', title: 'Pas', value: calculatedStats.passing },
                      { id: 'dribbling', title: 'Dribling', value: calculatedStats.dribbling },
                      { id: 'defending', title: 'Savunma', value: calculatedStats.defending },
                      { id: 'physical', title: 'Fizik', value: calculatedStats.physical },
                    ];
                    const items = isGoalkeeper ? goalkeeperItems : outfieldItems;
                    const getStatColor = (v: number) => v >= 80 ? '#10B981' : v >= 70 ? '#F59E0B' : '#EF4444';
                    return items.map((item) => {
                      const value = Math.max(50, Math.min(99, item.value));
                      const color = getStatColor(value);
                      return (
                        <View key={item.id} style={styles.playerRatingDropdownRow}>
                          <View style={styles.playerRatingDropdownRowLeft}>
                            <Text style={styles.playerRatingDropdownTitle}>{item.title}</Text>
                          </View>
                          <View style={styles.playerRatingDropdownRowRight}>
                            <View style={styles.playerRatingDropdownBar}>
                              <View style={[styles.playerRatingDropdownBarFill, { width: `${value}%`, backgroundColor: color }]} />
                            </View>
                            <Text style={[styles.playerRatingDropdownValue, { color }]}>{value}</Text>
                          </View>
                        </View>
                      );
                    });
                  })()}
                </View>
              )}
            </LinearGradient>
          </View>
        </TouchableOpacity>
      </Modal>

      {/* ✅ Tam Tercih Özeti Popup - Tüm pozisyonlar ve formasyonlar */}
      <Modal
        visible={showFullPreferenceSummary}
        animationType="slide"
        transparent={true}
        onRequestClose={() => setShowFullPreferenceSummary(false)}
      >
        <View style={styles.fullPreferenceSummaryOverlay}>
          <View style={styles.fullPreferenceSummaryModal}>
            <LinearGradient
              colors={['#1A2E2A', '#0F2420']}
              style={styles.fullPreferenceSummaryGradient}
            >
              {/* Header */}
              <View style={styles.fullPreferenceSummaryHeader}>
                <View style={styles.preferencePopupTitleRow}>
                  <Ionicons name="stats-chart" size={20} color="#1FA2A6" />
                  <Text style={styles.preferencePopupTitle}>Topluluk Tercih Özeti</Text>
                </View>
                <TouchableOpacity onPress={() => setShowFullPreferenceSummary(false)}>
                  <Ionicons name="close-circle" size={24} color="#9CA3AF" />
                </TouchableOpacity>
              </View>
              
              {/* Kullanıcı Sayısı */}
              <View style={styles.fullPreferenceSummaryUserCount}>
                <Ionicons name="people" size={16} color="#10B981" />
                <Text style={styles.fullPreferenceSummaryUserCountText}>
                  {userPreferenceStats?.totalUsers.toLocaleString()} kullanıcının tercihleri
                </Text>
              </View>
              
              <ScrollView style={styles.fullPreferenceSummaryScroll} showsVerticalScrollIndicator={false}>
                {/* Formasyon Tercihleri */}
                <View style={styles.fullPreferenceSummarySection}>
                  <Text style={styles.fullPreferenceSummarySectionTitle}>
                    <Ionicons name="grid" size={14} color="#F59E0B" /> Formasyon Tercihleri
                  </Text>
                  
                  {/* Atak Formasyonları - Top 2 Özet */}
                  <TouchableOpacity 
                    style={styles.fullPreferenceFormationGroup}
                    onPress={() => {
                      setShowFullPreferenceSummary(false);
                      setShowFormationDetailPopup('attack');
                      setEditingMode('attack');
                      setViewSource('community');
                    }}
                    activeOpacity={0.7}
                  >
                    <View style={styles.fullPreferenceFormationGroupHeader}>
                      <Text style={styles.fullPreferenceFormationGroupTitle}>
                        <Ionicons name="flash" size={12} color="#F59E0B" /> Atak
                      </Text>
                      <View style={styles.fullPreferenceFormationGroupArrow}>
                        <Text style={styles.fullPreferenceFormationGroupArrowText}>Detay</Text>
                        <Ionicons name="chevron-forward" size={14} color="#F59E0B" />
                      </View>
                    </View>
                    {userPreferenceStats?.attackFormation.stats.slice(0, 2).map((f, idx) => (
                      <View key={f.formation} style={[
                        styles.fullPreferenceFormationItem,
                        idx === 0 && styles.fullPreferenceFormationItemFirst
                      ]}>
                        <Text style={styles.fullPreferenceFormationItemRank}>#{idx + 1}</Text>
                        <Text style={[
                          styles.fullPreferenceFormationItemName,
                          idx === 0 && styles.fullPreferenceFormationItemNameFirst
                        ]}>{f.formation}</Text>
                        <Text style={styles.fullPreferenceFormationItemCount}>{f.count.toLocaleString()}</Text>
                        <Text style={[
                          styles.fullPreferenceFormationItemPercentage,
                          idx === 0 && { color: '#10B981' }
                        ]}>%{f.percentage}</Text>
                      </View>
                    ))}
                  </TouchableOpacity>
                  
                  {/* Defans Formasyonları - Top 2 Özet */}
                  <TouchableOpacity 
                    style={styles.fullPreferenceFormationGroup}
                    onPress={() => {
                      setShowFullPreferenceSummary(false);
                      setShowFormationDetailPopup('defense');
                      setEditingMode('defense');
                      setViewSource('community');
                    }}
                    activeOpacity={0.7}
                  >
                    <View style={styles.fullPreferenceFormationGroupHeader}>
                      <Text style={styles.fullPreferenceFormationGroupTitle}>
                        <Ionicons name="shield" size={12} color="#3B82F6" /> Defans
                      </Text>
                      <View style={styles.fullPreferenceFormationGroupArrow}>
                        <Text style={[styles.fullPreferenceFormationGroupArrowText, { color: '#3B82F6' }]}>Detay</Text>
                        <Ionicons name="chevron-forward" size={14} color="#3B82F6" />
                      </View>
                    </View>
                    {userPreferenceStats?.defenseFormation.stats.slice(0, 2).map((f, idx) => (
                      <View key={f.formation} style={[
                        styles.fullPreferenceFormationItem,
                        idx === 0 && styles.fullPreferenceFormationItemFirst
                      ]}>
                        <Text style={styles.fullPreferenceFormationItemRank}>#{idx + 1}</Text>
                        <Text style={[
                          styles.fullPreferenceFormationItemName,
                          idx === 0 && styles.fullPreferenceFormationItemNameFirst
                        ]}>{f.formation}</Text>
                        <Text style={styles.fullPreferenceFormationItemCount}>{f.count.toLocaleString()}</Text>
                        <Text style={[
                          styles.fullPreferenceFormationItemPercentage,
                          idx === 0 && { color: '#10B981' }
                        ]}>%{f.percentage}</Text>
                      </View>
                    ))}
                  </TouchableOpacity>
                </View>
                
                {/* ✅ Pozisyon tercihleri bu popup'tan KALDIRILDI - detay popup'ta gösterilecek */}
                {/* Bilgi notu */}
                <View style={styles.fullPreferenceSummaryNote}>
                  <Ionicons name="information-circle" size={14} color="#9CA3AF" />
                  <Text style={styles.fullPreferenceSummaryNoteText}>
                    Pozisyon detayları için Atak veya Defans formasyonuna tıklayın
                  </Text>
                </View>
              </ScrollView>
            </LinearGradient>
          </View>
        </View>
      </Modal>

      {/* ✅ Formasyon Detay Popup - Atak veya Defans formasyon detayları */}
      <Modal
        visible={showFormationDetailPopup !== null}
        animationType="slide"
        transparent={true}
        onRequestClose={() => {
          setShowFormationDetailPopup(null);
          // Popup kapatıldığında da topluluk verilerine göre göster
          setViewSource('community');
        }}
      >
        <View style={styles.formationDetailOverlay}>
          <View style={styles.formationDetailModal}>
            <LinearGradient
              colors={['#0F2A24', '#1E3A3A', '#0F2A24']}
              style={styles.formationDetailGradient}
            >
              {/* Header with Tabs */}
              <View style={styles.formationDetailHeader}>
                <Text style={styles.formationDetailTitle}>
                  {showFormationDetailPopup === 'attack' ? '⚡ Atak Formasyonu' : '🛡️ Defans Formasyonu'}
                </Text>
                <TouchableOpacity onPress={() => {
                  setShowFormationDetailPopup(null);
                  // Popup kapatıldığında da topluluk verilerine göre göster
                  setViewSource('community');
                }}>
                  <Ionicons name="close-circle" size={24} color="#9CA3AF" />
                </TouchableOpacity>
              </View>
              
              {/* Tab Buttons */}
              <View style={styles.formationDetailTabs}>
                <TouchableOpacity 
                  style={[
                    styles.formationDetailTab, 
                    showFormationDetailPopup === 'attack' && styles.formationDetailTabActive
                  ]}
                  onPress={() => {
                    setShowFormationDetailPopup('attack');
                    setEditingMode('attack'); // ✅ Sahada atak formasyonunu göster
                    setViewSource('community'); // ✅ Topluluk verilerine göre göster
                  }}
                >
                  <Ionicons name="flash" size={14} color={showFormationDetailPopup === 'attack' ? '#F59E0B' : '#6B7280'} />
                  <Text style={[
                    styles.formationDetailTabText,
                    showFormationDetailPopup === 'attack' && styles.formationDetailTabTextActive
                  ]}>Atak</Text>
                </TouchableOpacity>
                <TouchableOpacity 
                  style={[
                    styles.formationDetailTab, 
                    showFormationDetailPopup === 'defense' && styles.formationDetailTabActive
                  ]}
                  onPress={() => {
                    setShowFormationDetailPopup('defense');
                    setEditingMode('defense'); // ✅ Sahada defans formasyonunu göster
                    setViewSource('community'); // ✅ Topluluk verilerine göre göster
                  }}
                >
                  <Ionicons name="shield" size={14} color={showFormationDetailPopup === 'defense' ? '#3B82F6' : '#6B7280'} />
                  <Text style={[
                    styles.formationDetailTabText,
                    showFormationDetailPopup === 'defense' && styles.formationDetailTabTextActive
                  ]}>Defans</Text>
                </TouchableOpacity>
              </View>
              
              <ScrollView style={styles.formationDetailScroll} showsVerticalScrollIndicator={false}>
                {/* Seçilen Formasyon */}
                <View style={styles.formationDetailSelected}>
                  <Text style={styles.formationDetailSelectedLabel}>Seçilen Formasyon</Text>
                  <Text style={styles.formationDetailSelectedValue}>
                    {showFormationDetailPopup === 'attack' 
                      ? autoGeneratedSquad?.attackFormation.formation 
                      : autoGeneratedSquad?.defenseFormation.formation}
                  </Text>
                  <View style={styles.formationDetailSelectedBadge}>
                    <Text style={styles.formationDetailSelectedBadgeText}>
                      %{showFormationDetailPopup === 'attack' 
                          ? autoGeneratedSquad?.attackFormation.percentage 
                          : autoGeneratedSquad?.defenseFormation.percentage}
                    </Text>
                  </View>
                </View>
                
                {/* Top 3 Formasyonlar */}
                <View style={styles.formationDetailSection}>
                  <Text style={styles.formationDetailSectionTitle}>En Çok Tercih Edilen Formasyonlar</Text>
                  {(showFormationDetailPopup === 'attack' 
                    ? userPreferenceStats?.attackFormation.stats 
                    : userPreferenceStats?.defenseFormation.stats
                  )?.slice(0, 4).map((f, idx) => {
                    const isFirst = idx === 0;
                    return (
                      <View key={f.formation} style={[
                        styles.formationDetailItem,
                        isFirst && styles.formationDetailItemFirst
                      ]}>
                        <Text style={[styles.formationDetailItemRank, isFirst && { color: '#10B981' }]}>#{idx + 1}</Text>
                        <Text style={[styles.formationDetailItemName, isFirst && { color: '#10B981', fontWeight: '700' }]}>
                          {f.formation}
                        </Text>
                        <Text style={styles.formationDetailItemCount}>
                          {f.count.toLocaleString()} oy
                        </Text>
                        <View style={[
                          styles.formationDetailItemPercentBadge,
                          isFirst && { backgroundColor: 'rgba(16, 185, 129, 0.2)' }
                        ]}>
                          <Text style={[
                            styles.formationDetailItemPercent,
                            isFirst && { color: '#10B981' }
                          ]}>%{f.percentage}</Text>
                        </View>
                      </View>
                    );
                  })}
                </View>
                
                {/* Pozisyon Tercihleri - Pozisyon gruplarına göre renklendirme */}
                <View style={styles.formationDetailSection}>
                  <Text style={styles.formationDetailSectionTitle}>Oyuncu Pozisyon Tercihleri (Top 3)</Text>
                  {autoGeneratedSquad?.positions.map((pos) => {
                    const positionColor = getPositionGroupColor(pos.position);
                    return (
                      <View 
                        key={pos.position} 
                        style={[
                          styles.formationDetailPositionGroup,
                          { backgroundColor: positionColor.background, borderColor: `${positionColor.primary}30` }
                        ]}
                      >
                        <View style={styles.formationDetailPositionHeader}>
                          <Text style={[styles.formationDetailPositionLabel, { color: positionColor.text }]}>
                            {pos.positionLabel}
                          </Text>
                          <View style={[styles.formationDetailPositionBadge, { backgroundColor: positionColor.primary }]}>
                            <Text style={styles.formationDetailPositionBadgeText}>%{pos.preferencePercentage}</Text>
                          </View>
                        </View>
                        {pos.allPreferences.slice(0, 3).map((pref, idx) => {
                          const isFirst = idx === 0;
                          return (
                            <View key={pref.playerId} style={[
                              styles.formationDetailPositionItem,
                              isFirst && styles.formationDetailPositionItemFirst,
                              { borderLeftColor: isFirst ? positionColor.primary : `${positionColor.primary}60`, borderLeftWidth: isFirst ? 3 : 2 }
                            ]}>
                              <Text style={[styles.formationDetailPositionItemRank, { color: positionColor.secondary }]}>#{idx + 1}</Text>
                              <Text style={[
                                styles.formationDetailPositionItemName,
                                isFirst && { color: positionColor.text, fontWeight: '700' }
                              ]}>{pref.playerName}</Text>
                              <Text style={styles.formationDetailPositionItemCount}>{pref.count.toLocaleString()}</Text>
                              <Text style={[styles.formationDetailPositionItemPercent, { color: positionColor.secondary }]}>
                                %{pref.percentage}
                              </Text>
                            </View>
                          );
                        })}
                      </View>
                    );
                  })}
                </View>
              </ScrollView>
            </LinearGradient>
          </View>
        </View>
      </Modal>
      {/* ✅ Community Signal Popup - Shows when user initiates player replacement */}
      <CommunitySignalPopup
        visible={showCommunitySignal}
        onClose={() => setShowCommunitySignal(false)}
        onSelectReplacement={(player) => {
          // When user selects a replacement from community suggestions
          if (selectedSlot !== null) {
            handlePlayerSelect(player);
          }
          setShowCommunitySignal(false);
        }}
        matchId={parseInt(matchId, 10) || 0}
        teamId={effectivePredictionTeamId ?? predictionTeamId ?? attackTeamId ?? 0}
        currentPlayer={communitySignalPlayer}
        userLineup={selectedPlayers}
        formationId={selectedFormation || '4-3-3'}
        availablePlayers={editingMode === 'defense' || (isMatchLive && attackSquadPlayers.length === 11) ? attackSquadPlayers : attackTeamPlayers}
      />

      {/* Formation Modal - Defans sekmesi sadece atak formasyonu seçilip 11 oyuncu yerleştirildiyse aktif */}
      <FormationModal
        visible={showFormationModal}
        formations={formations}
        formationType={formationType}
        onSelect={handleFormationSelect}
        onClose={() => {
          setShowFormationModal(false);
          // ✅ Popup kapatıldığında formationType'a göre editingMode ayarla
          setEditingMode(formationType === 'defense' ? 'defense' : 'attack');
        }}
        onTabChange={(type: string) => {
          setFormationType(type);
          // ✅ Popup'taki tab değişince sahada da doğru formasyon gösterilsin
          if (type === 'defense') {
            setEditingMode('defense');
          } else {
            setEditingMode('attack');
          }
        }}
        canSelectDefense={attackFormation != null && attackCount === 11}
        currentAttackFormation={attackFormation}
        currentDefenseFormation={defenseFormation}
        communityDataVisible={communityDataVisible}
      />

      {/* Player Detail Modal */}
      {selectedPlayerForDetail && (
        <PlayerDetailModal
          player={selectedPlayerForDetail}
          onClose={() => setSelectedPlayerForDetail(null)}
          matchId={matchId}
          positionLabel={selectedSlot !== null ? formation?.positions[selectedSlot] : selectedPlayerForDetail?.position}
          communityDataVisible={communityDataVisible}
        />
      )}

      {/* ✅ Defense Confirmation Modal */}
      <Modal
        visible={showDefenseConfirmModal}
        animationType="fade"
        transparent={true}
        onRequestClose={() => setShowDefenseConfirmModal(false)}
      >
        <View style={styles.defenseConfirmOverlay}>
          <View style={styles.defenseConfirmModal}>
            {/* Icon */}
            <View style={styles.defenseConfirmIcon}>
              <Ionicons name="shield-checkmark" size={48} color="#1FA2A6" />
            </View>
            
            {/* Title */}
            <Text style={styles.defenseConfirmTitle}>Defans Formasyonu</Text>
            
            {/* Description */}
            <Text style={styles.defenseConfirmDesc}>
              Atak kadronuz tamamlandı! Defans için farklı bir formasyon kullanmak ister misiniz?
            </Text>
            
            {/* Info Box */}
            <View style={styles.defenseConfirmInfo}>
              <Ionicons name="information-circle" size={18} color="#F59E0B" />
              <Text style={styles.defenseConfirmInfoText}>
                <Text style={{ fontWeight: '700' }}>Evet:</Text> Defans formasyonu seçip sadece kaleci otomatik kalır, diğer 10 oyuncuyu defans pozisyonlarına manuel yerleştirirsiniz.{'\n'}
                <Text style={{ fontWeight: '700' }}>Hayır:</Text> Atak formasyonu ve oyuncuları defans için de aynen kullanılır.
              </Text>
            </View>
            
            {/* Buttons */}
            <View style={styles.defenseConfirmButtons}>
              <TouchableOpacity
                style={styles.defenseConfirmNoBtn}
                onPress={handleDefenseConfirmNo}
                activeOpacity={0.7}
              >
                <Ionicons name="close-circle-outline" size={20} color="#94A3B8" />
                <Text style={styles.defenseConfirmNoText}>Hayır, Devam Et</Text>
              </TouchableOpacity>
              
              <TouchableOpacity
                style={styles.defenseConfirmYesBtn}
                onPress={handleDefenseConfirmYes}
                activeOpacity={0.7}
              >
                <LinearGradient
                  colors={['#3B82F6', '#1D4ED8']}
                  start={{ x: 0, y: 0 }}
                  end={{ x: 1, y: 0 }}
                  style={styles.defenseConfirmYesGradient}
                >
                  <Ionicons name="shield" size={20} color="#FFFFFF" />
                  <Text style={styles.defenseConfirmYesText}>Evet, Seç</Text>
                </LinearGradient>
              </TouchableOpacity>
            </View>
          </View>
        </View>
      </Modal>

      {/* ✅ Kadro Tamamlandı – ekran ortasında standart popup, 5 sn sonra kapanır */}
      <Modal
        visible={showSquadCompletePopup !== null}
        animationType="fade"
        transparent={true}
        onRequestClose={() => setShowSquadCompletePopup(null)}
      >
        <TouchableOpacity
          style={styles.squadCompleteOverlay}
          activeOpacity={1}
          onPress={() => setShowSquadCompletePopup(null)}
        >
          <View style={styles.squadCompleteModal}>
            <LinearGradient colors={['#1A2E2A', '#0F2420']} style={styles.squadCompleteGradient}>
              <View style={styles.squadCompleteIcon}>
                <Ionicons name="football" size={44} color="#1FA2A6" />
              </View>
              <Text style={styles.squadCompleteTitle}>Kadro Tamamlandı! ⚽</Text>
              <Text style={styles.squadCompleteFormation}>
                Atak ve defans için aynı formasyon kullanılacak: {showSquadCompletePopup}
              </Text>
              <View style={styles.squadCompleteInfo}>
                <Ionicons name="information-circle" size={18} color="#F59E0B" />
                <Text style={styles.squadCompleteInfoText}>
                  Bu sayfadan ayrılmadan önce değişiklikleri kaydetmek için "Tamamla" butonuna basmanız gerekiyor. Tamamla'ya basmadan çıkarsanız değişiklikler kaydedilmez!
                </Text>
              </View>
            </LinearGradient>
          </View>
        </TouchableOpacity>
      </Modal>

      {/* Atak formasyonu değişikliği – uygulama içi onay popup */}
      {formationConfirmModal && (
        <ConfirmModal
          visible={true}
          title="Atak formasyonu değişikliği"
          message="Sadece ATAK formasyonu değişiyor – tüm tahmin verileriniz silinecek. Defans formasyonu veya oyuncu değişikliği bu uyarıyı göstermez. Devam?"
          buttons={[
            {
              text: 'İptal',
              style: 'cancel',
              onPress: () => {
                setShowFormationModal(false);
                setFormationConfirmModal(null);
              },
            },
            {
              text: 'Onayla',
              style: 'destructive',
              onPress: () => runFormationChangeConfirm(),
            },
          ]}
          onRequestClose={() => {
            setShowFormationModal(false);
            setFormationConfirmModal(null);
          }}
        />
      )}
      
      {/* ✅ Kaydediliyor... Overlay */}
      {isSaving && (
        <Modal
          visible={true}
          transparent={true}
          animationType="fade"
        >
          <View style={styles.savingOverlay}>
            <View style={styles.savingContainer}>
              <ActivityIndicator size="large" color="#1FA2A6" />
              <Text style={styles.savingText}>Kaydediliyor...</Text>
              <Text style={styles.savingSubtext}>Kadronuz kaydediliyor, lütfen bekleyin</Text>
            </View>
          </View>
        </Modal>
      )}
    </View>
  );
}

// Formation Modal Component - 3 COLUMN GRID
const FormationModal = ({ visible, formations, formationType, onSelect, onClose, onTabChange, canSelectDefense = true, currentAttackFormation, currentDefenseFormation, communityDataVisible = false }: any) => {
  const { t } = useTranslation();
  // ✅ Mevcut sekmede seçili formasyon
  const currentSelectedFormation = formationType === 'defense' ? currentDefenseFormation : currentAttackFormation;
  const [selectedFormationForDetail, setSelectedFormationForDetail] = useState<any>(null);
  const [hoveredFormation, setHoveredFormation] = useState<any>(null); // ✅ Önizleme için
  
  // ✅ Formasyon popülerlik verileri (API'den gelecek, şimdilik mock)
  const [formationPopularity, setFormationPopularity] = React.useState<Record<string, number>>({});
  const [popularityLoading, setPopularityLoading] = React.useState(true);
  
  // ✅ Formasyon popülerlik verilerini yükle
  React.useEffect(() => {
    if (!visible) return;
    
    const loadPopularity = async () => {
      setPopularityLoading(true);
      try {
        // TODO: Gerçek API entegrasyonu
        // const result = await squadPredictionsApi.getFormationPopularity(formationType);
        
        // ✅ Mock data - Toplam %100 olacak şekilde dağıtılmış (formationType'a göre)
        // Atak için popüler formasyonlar
        const attackPopularity: Record<string, number> = {
          '4-3-3': 18,
          '4-2-3-1': 14,
          '4-4-2': 11,
          '3-4-3': 9,
          '4-1-2-3': 8,
          '3-5-2': 7,
          '4-3-3-holding': 6,
          '4-3-3-false9': 5,
          '4-1-4-1': 4,
          '4-3-1-2': 4,
          '3-4-1-2': 3,
          '4-2-2-2': 3,
          '4-4-2-diamond': 2,
          '4-2-4': 2,
          '2-3-5': 1,
          '3-3-3-1': 1,
          '3-3-4': 1,
          '3-4-2-1': 1,
        };
        
        // Defans için popüler formasyonlar
        const defensePopularity: Record<string, number> = {
          '5-4-1': 16,
          '5-3-2': 14,
          '4-5-1': 12,
          '4-4-2': 10,
          '4-1-4-1': 9,
          '3-5-2': 8,
          '4-2-3-1': 6,
          '4-3-3-holding': 5,
          '5-2-3': 4,
          '4-4-1-1': 4,
          '4-3-2-1': 3,
          '4-1-3-2': 3,
          '3-6-1': 2,
          '4-3-3': 2,
          '3-4-3': 1,
          '4-3-1-2': 1,
        };
        
        // Seçilen tipe göre popülerlik dağılımı
        const baseData = formationType === 'defense' ? defensePopularity : attackPopularity;
        const mockData: Record<string, number> = { ...baseData };
        
        // Eksik formasyonlara düşük değer ver (toplam %100 olsun)
        let totalAssigned = Object.values(mockData).reduce((a, b) => a + b, 0);
        const remaining = 100 - totalAssigned;
        const unassignedFormations = formations.filter((f: any) => !mockData[f.id]);
        
        if (unassignedFormations.length > 0 && remaining > 0) {
          const perFormation = Math.floor(remaining / unassignedFormations.length);
          unassignedFormations.forEach((f: any, i: number) => {
            mockData[f.id] = i === unassignedFormations.length - 1 
              ? remaining - (perFormation * (unassignedFormations.length - 1)) // Son formasyona kalanı ver
              : perFormation;
          });
        }
        
        setFormationPopularity(mockData);
      } catch (error) {
        console.warn('Formation popularity load failed', error);
      }
      setPopularityLoading(false);
    };
    
    loadPopularity();
  }, [visible, formationType, formations]);
  
  // ✅ Defans sekmesi sadece atak formasyonu seçilip 11 oyuncu yerleştirildiyse seçilebilir
  React.useEffect(() => {
    if (visible && formationType === 'defense' && !canSelectDefense) {
      onTabChange('attack');
    }
  }, [visible, formationType, canSelectDefense, onTabChange]);
  
  // ✅ Tüm formasyonları göster (filtreleme yok - kullanıcı istediği formasyonu seçebilir)
  const filteredFormations = formations;

  return (
    <>
      <Modal
        visible={visible}
        animationType="slide"
        transparent={true}
        onRequestClose={onClose}
      >
        <View style={styles.modalOverlay}>
          <Animated.View 
            entering={Platform.OS === 'web' ? undefined : SlideInDown.duration(300)}
            exiting={Platform.OS === 'web' ? undefined : SlideOutDown.duration(300)}
            style={styles.modalContent}
          >
            {/* Header */}
            <View style={styles.modalHeader}>
              <View style={styles.modalHeaderContent}>
                <Text style={styles.modalTitle}>
                  {formationType === 'defense' ? 'Defans Formasyonu Seçin' : 'Atak Formasyonu Seçin'}
                </Text>
                <Text style={styles.modalSubtitle}>
                  {formationType === 'defense' ? 'Defans için formasyon seçiniz' : 'Atak için formasyon seçin'}
                </Text>
              </View>
            </View>

            {/* ✅ Atak / Defans sekme butonları */}
            <View style={styles.formationModalTabs}>
              <TouchableOpacity
                style={[styles.formationModalTab, formationType === 'attack' && styles.formationModalTabActive]}
                onPress={() => onTabChange('attack')}
                activeOpacity={0.8}
              >
                <Ionicons name="flash" size={18} color={formationType === 'attack' ? '#FFFFFF' : '#1FA2A6'} />
                <Text style={[styles.formationModalTabText, formationType === 'attack' && styles.formationModalTabTextActive]}>
                  Atak
                </Text>
              </TouchableOpacity>
              <TouchableOpacity
                style={[
                  styles.formationModalTab,
                  formationType === 'defense' && styles.formationModalTabActive,
                  !canSelectDefense && styles.formationModalTabDisabled,
                ]}
                onPress={() => {
                  if (!canSelectDefense) {
                    showInfo(
                      'Önce Atak Kadrosu',
                      'Defans formasyonu seçebilmek için önce atak formasyonunu seçin ve 11 oyuncuyu yerleştirin.'
                    );
                    return;
                  }
                  onTabChange('defense');
                }}
                activeOpacity={0.8}
              >
                <Ionicons name="shield-checkmark" size={18} color={formationType === 'defense' ? '#FFFFFF' : canSelectDefense ? '#3B82F6' : '#6B7280'} />
                <Text style={[styles.formationModalTabText, formationType === 'defense' && styles.formationModalTabTextActive, !canSelectDefense && formationType !== 'defense' && styles.formationModalTabTextDisabled]}>
                  Defans
                </Text>
              </TouchableOpacity>
            </View>

            {/* Close Button - Absolute Position */}
            <TouchableOpacity onPress={onClose} style={styles.modalCloseButtonAbsolute}>
              <Ionicons name="close" size={22} color="#1FA2A6" />
            </TouchableOpacity>

            {/* Formations Grid - 3 Columns */}
            <ScrollView 
              style={styles.modalScroll} 
              contentContainerStyle={styles.formationGridContainer}
              showsVerticalScrollIndicator={false}
            >
              {filteredFormations.map((formation: any) => {
                const isCurrentlySelected = formation.id === currentSelectedFormation;
                return (
                <View key={formation.id} style={styles.formationGridItem}>
                  <TouchableOpacity
                    style={[
                      styles.formationCard,
                      hoveredFormation?.id === formation.id && styles.formationCardSelected,
                      isCurrentlySelected && styles.formationCardCurrentlySelected,
                    ]}
                    onPress={() => setHoveredFormation(formation)}
                    activeOpacity={0.8}
                  >
                    {/* ✅ Seçili Formasyon Tik İşareti */}
                    {isCurrentlySelected && (
                      <View style={styles.formationSelectedBadge}>
                        <Ionicons name="checkmark-circle" size={16} color="#10B981" />
                      </View>
                    )}
                    {/* Formation ID */}
                    <Text style={[
                      styles.formationCardId,
                      hoveredFormation?.id === formation.id && { color: '#1FA2A6' },
                      isCurrentlySelected && { color: '#10B981' },
                    ]}>
                      {formation.id.replace(/-holding|-false9|-diamond|-attack/g, '')}
                    </Text>

                    {/* Formation Subtitle - From parentheses */}
                    <Text style={[
                      styles.formationCardSubtitle,
                      isCurrentlySelected && { color: '#10B981' },
                    ]} numberOfLines={1}>
                      {(() => {
                        const parts = formation.name.split('(');
                        if (parts.length > 1) {
                          return parts[1].replace(')', '').trim();
                        }
                        // If no parentheses, show type or formation style
                        return formation.type === 'attack' ? 'Attack' : 
                               formation.type === 'defense' ? 'Defense' : 'Balanced';
                      })()}
                    </Text>
                  </TouchableOpacity>
                </View>
              );})}
            </ScrollView>
            
          </Animated.View>
        </View>
      </Modal>

      {/* ✅ Formation Preview Modal - Ayrı popup olarak açılıyor */}
      {hoveredFormation && (
        <Modal
          visible={true}
          animationType="fade"
          transparent={true}
          onRequestClose={() => setHoveredFormation(null)}
        >
          <View style={styles.formationPreviewOverlay}>
            <View style={styles.formationPreviewModal}>
              {/* Header */}
              <View style={styles.formationPreviewHeader}>
                <View style={styles.formationPreviewTitleRow}>
                  <Text style={styles.formationPreviewName}>{hoveredFormation.name}</Text>
                  <TouchableOpacity 
                    onPress={() => setHoveredFormation(null)}
                    style={styles.formationPreviewCloseBtn}
                  >
                    <Ionicons name="close" size={20} color="#94A3B8" />
                  </TouchableOpacity>
                </View>
                <View style={[
                  styles.formationPreviewTypeBadge,
                  { backgroundColor: hoveredFormation.type === 'attack' ? 'rgba(239, 68, 68, 0.2)' : 
                    hoveredFormation.type === 'defense' ? 'rgba(59, 130, 246, 0.2)' : 'rgba(245, 158, 11, 0.2)' }
                ]}>
                  <Text style={[
                    styles.formationPreviewTypeText,
                    { color: hoveredFormation.type === 'attack' ? '#EF4444' : 
                      hoveredFormation.type === 'defense' ? '#3B82F6' : '#F59E0B' }
                  ]}>
                    {hoveredFormation.type === 'attack' ? '⚔️ Atak' : 
                     hoveredFormation.type === 'defense' ? '🛡️ Defans' : '⚖️ Dengeli'}
                  </Text>
                </View>
                {formationType === 'defense' && (
                  <Text style={styles.formationPreviewContextHint}>
                    🛡️ Bu formasyon defans dizilişi için seçiliyor
                  </Text>
                )}
              </View>

              {/* Description */}
              <Text style={styles.formationPreviewDesc}>
                {hoveredFormation.description}
              </Text>

              {/* Pros */}
              <View style={styles.formationPreviewSection}>
                <Text style={styles.formationPreviewSectionTitle}>✅ Avantajlar</Text>
                {hoveredFormation.pros?.map((pro: string, index: number) => (
                  <View key={index} style={styles.formationPreviewListItem}>
                    <View style={styles.formationPreviewBullet} />
                    <Text style={styles.formationPreviewListText}>{pro}</Text>
                  </View>
                ))}
              </View>

              {/* Cons */}
              <View style={styles.formationPreviewSection}>
                <Text style={styles.formationPreviewSectionTitle}>❌ Dezavantajlar</Text>
                {hoveredFormation.cons?.map((con: string, index: number) => (
                  <View key={index} style={styles.formationPreviewListItem}>
                    <View style={[styles.formationPreviewBullet, { backgroundColor: '#EF4444' }]} />
                    <Text style={styles.formationPreviewListText}>{con}</Text>
                  </View>
                ))}
              </View>

              {/* Best For */}
              <View style={styles.formationPreviewBestFor}>
                <Ionicons name="trophy" size={16} color="#F59E0B" />
                <Text style={styles.formationPreviewBestForText}>
                  <Text style={styles.formationPreviewBestForLabel}>En İyi: </Text>
                  {hoveredFormation.bestFor}
                </Text>
              </View>
              
              {/* ✅ Topluluk Tercihi - Formasyon Popülerliği - DİKKAT ÇEKİCİ */}
              <View style={styles.formationPopularitySection}>
                <View style={styles.formationPopularityHeader}>
                  <Ionicons name="flame" size={18} color="#F97316" />
                  <Text style={styles.formationPopularitySectionTitle}>🔥 Topluluk Tercihi</Text>
                </View>
                {popularityLoading ? (
                  <Text style={styles.formationPopularityLoading}>Yükleniyor...</Text>
                ) : (
                  <View style={styles.formationPopularityContent}>
                    {communityDataVisible ? (
                      <>
                        <Text style={styles.formationPopularityText}>
                          {t('matchSquad.formationCommunityTooltip', { pct: formationPopularity[hoveredFormation.id] || 0, type: formationType === 'defense' ? t('matchSquad.defense') : t('matchSquad.attack') })}
                        </Text>
                        <View style={styles.formationPopularityBarBg}>
                          <View 
                            style={[
                              styles.formationPopularityBarFill,
                              { 
                                width: `${formationPopularity[hoveredFormation.id] || 0}%`,
                                backgroundColor: (formationPopularity[hoveredFormation.id] || 0) >= 50 ? '#F97316' : 
                                  (formationPopularity[hoveredFormation.id] || 0) >= 25 ? '#FB923C' : '#FDBA74' // Turuncu tonları
                              }
                            ]}
                          />
                        </View>
                      </>
                    ) : (
                      <View style={{ alignItems: 'center', padding: 8 }}>
                        <Text style={[styles.formationPopularityText, { textAlign: 'center' }]}>
                          <Ionicons name="lock-closed" size={14} color="#F97316" /> Kullanıcıların <Text style={styles.formationPopularityHighlight}>??%</Text>'i bu formasyonu tercih ediyor
                        </Text>
                        <Text style={{ color: '#9CA3AF', fontSize: 11, marginTop: 6, textAlign: 'center' }}>
                          Tahminlerinizi kaydedin ve topluluk verilerini görün!
                        </Text>
                      </View>
                    )}
                  </View>
                )}
              </View>
              
              {/* Buttons */}
              <View style={styles.formationPreviewButtons}>
                <TouchableOpacity
                  style={styles.formationPreviewCancelBtn}
                  onPress={() => setHoveredFormation(null)}
                  activeOpacity={0.7}
                >
                  <Text style={styles.formationPreviewCancelText}>İptal</Text>
                </TouchableOpacity>
                
                <TouchableOpacity
                  style={styles.formationPreviewSelectBtn}
                  onPress={() => {
                    onSelect(hoveredFormation.id);
                    setHoveredFormation(null);
                  }}
                  activeOpacity={0.85}
                >
                  <LinearGradient
                    colors={['#1FA2A6', '#0D9488', '#047857']}
                    start={{ x: 0, y: 0 }}
                    end={{ x: 1, y: 0 }}
                    style={styles.formationPreviewSelectGradient}
                  >
                    <Ionicons name="checkmark-circle" size={18} color="#FFFFFF" />
                    <Text style={styles.formationPreviewSelectText}>
                      {formationType === 'defense' ? 'Bu Defans Dizilişini Seç' : 'Bu Dizilişi Seç'}
                    </Text>
                  </LinearGradient>
                </TouchableOpacity>
              </View>
            </View>
          </View>
        </Modal>
      )}
    </>
  );
};

// Player Modal Component - COMPLETE
// Kaleci pozisyonuna yalnızca kaleci, saha pozisyonlarına yalnızca saha oyuncusu atanabilir.
const PlayerModal = ({ visible, players, selectedPlayers, positionLabel, onSelect, onClose, isDefenseMode, isLoadingSquad = false, loadingFromApi = false, hasBackendError = false, onRetrySquad, communityDataVisible = false }: any) => {
  const [previewPlayer, setPreviewPlayer] = useState<any>(null);
  
  // ✅ Preview oyuncusu için topluluk istatistikleri
  const [previewCommunityStats, setPreviewCommunityStats] = React.useState<{
    inStartingXI: number;
    mostAssignedPosition: string; // ✅ Oyuncunun en çok atandığı pozisyon
    mostAssignedPositionPercentage: number; // ✅ Bu pozisyona atanma yüzdesi
    topPlayerForPosition: string; // ✅ Bu pozisyon için en çok tercih edilen oyuncu
    topPlayerPercentage: number; // ✅ Bu oyuncunun yüzdesi
    loading: boolean;
  }>({ inStartingXI: 0, mostAssignedPosition: '', mostAssignedPositionPercentage: 0, topPlayerForPosition: '', topPlayerPercentage: 0, loading: true });
  
  // ✅ Preview oyuncusu değiştiğinde topluluk istatistiklerini yükle
  React.useEffect(() => {
    if (!previewPlayer) {
      setPreviewCommunityStats({ 
        inStartingXI: 0, 
        mostAssignedPosition: '', 
        mostAssignedPositionPercentage: 0, 
        topPlayerForPosition: '', 
        topPlayerPercentage: 0, 
        loading: true 
      });
      return;
    }
    
    const loadStats = async () => {
      setPreviewCommunityStats(prev => ({ ...prev, loading: true }));
      try {
        // TODO: Gerçek API entegrasyonu
        // Mock data - oyuncunun en çok atandığı pozisyonu ve bu pozisyon için en çok tercih edilen oyuncuyu bul
        
        // Rating'e göre ilk 11'e seçilme yüzdesi
        const normRating = normalizeRatingTo100(previewPlayer.rating);
        const baseXI = normRating >= 85 ? 78 : normRating >= 80 ? 62 : normRating >= 75 ? 48 : 32;
        const variance = Math.floor(Math.random() * 12);
        const inStartingXI = Math.min(95, baseXI + variance);
        
        // ✅ Oyuncunun en çok atandığı pozisyonu bul (mock - gerçek API gelince değişecek)
        // Oyuncunun pozisyonuna göre en çok atandığı pozisyonu belirle
        const playerPos = previewPlayer.position?.toUpperCase() || previewPlayer.pos?.toUpperCase() || '';
        let mostAssignedPosition = playerPos;
        let mostAssignedPositionPercentage = 72; // Mock yüzde
        
        // Forvet/kanatlar için alternatif pozisyonlar
        if (['ST', 'CF', 'SS'].includes(playerPos)) {
          mostAssignedPosition = 'ST';
          mostAssignedPositionPercentage = 75 + Math.floor(Math.random() * 15);
        } else if (['LW', 'LM', 'LF'].includes(playerPos)) {
          mostAssignedPosition = 'LW';
          mostAssignedPositionPercentage = 68 + Math.floor(Math.random() * 20);
        } else if (['RW', 'RM', 'RF'].includes(playerPos)) {
          mostAssignedPosition = 'RW';
          mostAssignedPositionPercentage = 68 + Math.floor(Math.random() * 20);
        } else if (['CAM', 'AM', 'ACM'].includes(playerPos)) {
          mostAssignedPosition = 'CAM';
          mostAssignedPositionPercentage = 65 + Math.floor(Math.random() * 20);
        } else if (['CM', 'CDM', 'DM'].includes(playerPos)) {
          mostAssignedPosition = 'CM';
          mostAssignedPositionPercentage = 60 + Math.floor(Math.random() * 25);
        } else if (['CB', 'DC'].includes(playerPos)) {
          mostAssignedPosition = 'CB';
          mostAssignedPositionPercentage = 70 + Math.floor(Math.random() * 20);
        } else if (['LB', 'LWB'].includes(playerPos)) {
          mostAssignedPosition = 'LB';
          mostAssignedPositionPercentage = 72 + Math.floor(Math.random() * 18);
        } else if (['RB', 'RWB'].includes(playerPos)) {
          mostAssignedPosition = 'RB';
          mostAssignedPositionPercentage = 72 + Math.floor(Math.random() * 18);
        } else if (['GK', 'G'].includes(playerPos)) {
          mostAssignedPosition = 'GK';
          mostAssignedPositionPercentage = 95 + Math.floor(Math.random() * 5);
        }
        
        // ✅ Bu pozisyon için en çok tercih edilen oyuncu (mock)
        // Aynı pozisyondaki oyunculardan en yüksek rating'li olanı seç
        const samePositionPlayers = players.filter((p: any) => {
          const pPos = p.position?.toUpperCase() || p.pos?.toUpperCase() || '';
          return pPos === playerPos || 
            (mostAssignedPosition === 'ST' && ['ST', 'CF', 'SS'].includes(pPos)) ||
            (mostAssignedPosition === 'CAM' && ['CAM', 'AM', 'ACM'].includes(pPos));
        });
        
        const topPlayer = samePositionPlayers.length > 0 
          ? samePositionPlayers.reduce((best: any, p: any) => (!best || (p.rating || 0) > (best.rating || 0)) ? p : best, null)
          : null;
        
        const topPlayerForPosition = topPlayer?.name || 'Bilinmiyor';
        const topRating = normalizeRatingTo100(topPlayer?.rating);
        const topPlayerPercentage = topPlayer 
          ? (topRating >= 85 ? 85 : topRating >= 80 ? 72 : topRating >= 75 ? 58 : 45) + Math.floor(Math.random() * 10)
          : 0;
        
        // ✅ Mock data için beklemeden anında göster - sıçramayı önler
        setPreviewCommunityStats({
          inStartingXI,
          mostAssignedPosition,
          mostAssignedPositionPercentage,
          topPlayerForPosition,
          topPlayerPercentage: Math.min(95, topPlayerPercentage),
          loading: false,
        });
      } catch (error) {
        setPreviewCommunityStats({ 
          inStartingXI: 0, 
          mostAssignedPosition: '', 
          mostAssignedPositionPercentage: 0, 
          topPlayerForPosition: '', 
          topPlayerPercentage: 0, 
          loading: false 
        });
      }
    };
    
    loadStats();
  }, [previewPlayer, players]);

  const isGKPosition = positionLabel === 'GK';

  // Check if player can be assigned to current slot (GK slot → only GK, field → only field)
  const isPlayerEligible = (player: any) =>
    isGKPosition ? isGoalkeeperPlayer(player) : !isGoalkeeperPlayer(player);

  // Filter: not already selected AND eligible for this position (sakat/cezalı listede görünsün ama pasif)
  // Dedup: aynı ID'li oyuncuları tek tut (API bazen duplicate döner)
  const seenIds = new Set<number>();
  const dedupedPlayers = players.filter((p: any) => {
    if (seenIds.has(p.id)) return false;
    seenIds.add(p.id);
    return true;
  });
  // Sadece gerçekten kadrodaki (non-null) oyuncuları "seçili" say; çıkarılan oyuncu yedekte görünsün
  const selectedIds = new Set(
    Object.values(selectedPlayers).filter((sp: any) => sp != null && sp.id != null).map((sp: any) => sp.id)
  );
  const eligiblePlayers = dedupedPlayers.filter(
    (p: any) => !selectedIds.has(p.id) && isPlayerEligible(p)
  );
  const canAddToSquad = (p: any) => p.eligible_for_selection !== false && !p.injured && !p.suspended;

  // Forma numarasına göre küçükten büyüğe sırala (yoksa sonda)
  const sortedPlayers = [...eligiblePlayers].sort((a, b) => {
    const na = a.number != null ? Number(a.number) : 999;
    const nb = b.number != null ? Number(b.number) : 999;
    return na - nb;
  });

  const handlePlayerSelect = (player: any) => {
    setPreviewPlayer(null);
    if (player.eligible_for_selection === false || player.injured || player.suspended) {
      showInfo(
        'Kadroya Eklenemez',
        'Bu oyuncu sakat veya cezalı. Kadroya ekleyemezsiniz. Sakatlık/ceza kalkınca tekrar ekleyebilirsiniz.'
      );
      return;
    }
    onSelect(player);
  };

  return (
    <>
      <Modal
        visible={visible}
        animationType="slide"
        transparent={true}
        onRequestClose={onClose}
      >
        <View style={styles.modalOverlay}>
          <Animated.View 
            entering={isWeb ? undefined : SlideInDown.duration(300)}
            exiting={isWeb ? undefined : SlideOutDown.duration(300)}
            style={styles.modalContent}
          >
            {/* Header */}
            <View style={styles.playerModalHeader}>
              <View style={styles.playerModalHeaderText}>
                <Text style={styles.modalTitle}>
                  {isDefenseMode ? '🛡️ Defans Oyuncusu Seç' : '⚡ Atak Oyuncusu Seç'}
                </Text>
                <Text style={styles.modalSubtitle}>Pozisyon: {positionLabel}</Text>
              </View>
              <TouchableOpacity onPress={onClose} style={styles.playerModalCloseBtnTopRight}>
                <Ionicons name="close" size={20} color="#94A3B8" />
              </TouchableOpacity>
            </View>
            
            {/* Mode Info - Atak ve Defans için farklı uyarılar */}
            {isDefenseMode ? (
              <View style={styles.defenseModeInfo}>
                <Ionicons name="information-circle" size={16} color="#3B82F6" />
                <Text style={styles.defenseModeInfoText}>
                  Sadece atak kadronuzdaki 11 oyuncudan seçim yapabilirsiniz.
                </Text>
              </View>
            ) : (
              <View style={styles.attackModeInfo}>
                <Ionicons name="football" size={16} color="#1FA2A6" />
                <Text style={styles.attackModeInfoText}>
                  Takımınızın ilk 11'ini kadrodaki oyuncular arasından belirleyin.
                </Text>
              </View>
            )}

            {/* Players List – sadece uygun oyuncular, forma no küçükten büyüğe */}
            <FlatList
              data={sortedPlayers}
              keyExtractor={(item) => item.id.toString()}
              renderItem={({ item }) => {
                const itemRating = normalizeRatingTo100(item.rating);
                const getBorderColor = () => {
                  if (itemRating >= 85) return '#C9A44C'; // Gold for elite
                  if (isGoalkeeperPlayer(item)) return '#3B82F6'; // Blue for GK
                  return '#4B5563'; // Gray for others
                };
                return (
                  <TouchableOpacity
                    style={[
                      styles.playerItem,
                      { borderLeftWidth: 3, borderLeftColor: getBorderColor() },
                    ]}
                    onPress={() => setPreviewPlayer(item)}
                    activeOpacity={0.7}
                  >
                    <View style={styles.playerItemLeft}>
                      <View style={[
                        styles.playerItemJerseyNumber,
                        { backgroundColor: itemRating >= 85 ? '#C9A44C' : '#1FA2A6' }
                      ]}>
                        <Text style={styles.playerItemJerseyNumberText}>
                          {(item.number ?? item.shirt_number ?? item.jersey_number) != null && (item.number ?? item.shirt_number ?? item.jersey_number) > 0 ? (item.number ?? item.shirt_number ?? item.jersey_number) : '-'}
                        </Text>
                      </View>
                      <View style={styles.playerItemInfo}>
                        <View style={styles.playerItemNameRow}>
                          <Text style={styles.playerItemName}>
                            {formatPlayerDisplayName(item)}
                          </Text>
                          {item.form >= 8 && (
                            <Ionicons name="flame" size={14} color="#F59E0B" style={{ marginLeft: 4 }} />
                          )}
                          {item.injury && (
                            <Ionicons name="warning" size={14} color="#EF4444" style={{ marginLeft: 4 }} />
                          )}
                        </View>
                        <View style={styles.playerItemBottomRow}>
                          <Text style={styles.playerItemRatingBottom}>
                            {formatRatingDisplay(item.rating)}
                          </Text>
                          <Text style={styles.playerItemPosition}>
                            {item.position} • {item.team}
                          </Text>
                        </View>
                      </View>
                    </View>
                    <TouchableOpacity
                      onPress={() => handlePlayerSelect(item)}
                      style={styles.playerItemAddBtn}
                      activeOpacity={0.7}
                    >
                      <Ionicons name="add-circle" size={28} color="#1FA2A6" />
                    </TouchableOpacity>
                  </TouchableOpacity>
                );
              }}
              showsVerticalScrollIndicator={false}
              contentContainerStyle={styles.playersList}
              ListFooterComponent={null}
              ListEmptyComponent={() => {
                const noData = !players || players.length === 0;
                return (
                  <View style={{ alignItems: 'center', padding: 32, marginTop: 40 }}>
                    {isLoadingSquad ? (
                      <>
                        <Ionicons name="hourglass-outline" size={48} color="rgba(31, 162, 166, 0.5)" />
                        <Text style={{ fontSize: 18, fontWeight: '600', color: '#FFFFFF', marginTop: 16 }}>
                          Kadro Yükleniyor...
                        </Text>
                        <Text style={{ fontSize: 14, color: 'rgba(255,255,255,0.7)', textAlign: 'center', marginTop: 8, lineHeight: 20 }}>
                          {loadingFromApi
                            ? 'Takım kadrosu API\'den çekiliyor. Lütfen bekleyin...'
                            : 'Uygulama verisinden yükleniyor...'}
                        </Text>
                      </>
                    ) : (
                      <>
                        <Ionicons name="football-outline" size={48} color="rgba(31, 162, 166, 0.5)" />
                        <Text style={{ fontSize: 18, fontWeight: '600', color: '#FFFFFF', marginTop: 16 }}>
                          {hasBackendError ? 'Backend Bağlantı Hatası' : noData ? 'Kadro Bilgisi Yok' : 'Bu Pozisyon İçin Uygun Oyuncu Yok'}
                        </Text>
                        <Text style={{ fontSize: 14, color: 'rgba(255,255,255,0.7)', textAlign: 'center', marginTop: 8, lineHeight: 20 }}>
                          {hasBackendError
                            ? 'Backend servisi çalışmıyor olabilir. Lütfen backend\'in port 3001\'de çalıştığından emin olun.'
                            : noData
                            ? 'Kadro henüz yüklenmedi. Sistem otomatik yükleyecektir.'
                            : (isGKPosition ? 'Kadroda kaleci yok veya hepsi seçildi. Önce kalecinizi kale pozisyonuna yerleştirin.' : 'Kadroda saha oyuncusu yok veya hepsi seçildi.')}
                        </Text>
                      </>
                    )}
                  </View>
                );
              }}
            />
          </Animated.View>
        </View>
      </Modal>

      {/* Player Preview Modal - FIFA Card Style */}
      {previewPlayer && (
        <Modal
          visible={true}
          animationType="none"
          transparent={true}
          onRequestClose={() => setPreviewPlayer(null)}
        >
          <View style={styles.playerCardOverlay}>
            <Animated.View 
              style={styles.playerCardContainer}
            >
              {/* Card Header with Gradient */}
              <LinearGradient
                colors={normalizeRatingTo100(previewPlayer.rating) >= 85 ? ['#C9A44C', '#A67C00'] : ['#1FA2A6', '#0D7377']}
                start={{ x: 0, y: 0 }}
                end={{ x: 1, y: 1 }}
                style={styles.playerCardHeader}
              >
                {/* Close Button */}
                <TouchableOpacity 
                  style={styles.playerCardCloseBtn}
                  onPress={() => setPreviewPlayer(null)}
                >
                  <Ionicons name="close" size={18} color="#FFFFFF" />
                </TouchableOpacity>

                {/* Jersey Number Circle */}
                <View style={styles.playerCardRatingCircle}>
                  <Text style={styles.playerCardRatingText}>
                    {(previewPlayer.number ?? previewPlayer.shirt_number ?? previewPlayer.jersey_number) != null && (previewPlayer.number ?? previewPlayer.shirt_number ?? previewPlayer.jersey_number) > 0 ? (previewPlayer.number ?? previewPlayer.shirt_number ?? previewPlayer.jersey_number) : '-'}
                  </Text>
                </View>

                {/* Player Name & Position */}
                <Text style={styles.playerCardName}>
                {formatPlayerDisplayName({ ...previewPlayer, name: previewPlayer.name || '' })}
              </Text>
                <View style={styles.playerCardPositionRow}>
                  <View style={styles.playerCardPositionBadge}>
                    <Text style={styles.playerCardPositionText}>{previewPlayer.position}</Text>
                  </View>
                  <Text style={styles.playerCardTeam}>{previewPlayer.team}</Text>
                </View>

                {/* Nationality & Age with Rating */}
                <View style={styles.playerCardInfoRow}>
                  <Text style={styles.playerCardInfoText}>
                    {previewPlayer.nationality || 'Unknown'} • {previewPlayer.age || '?'} yaş
                  </Text>
                  <Text style={styles.playerCardRatingBottom}>
                    {formatRatingDisplay(previewPlayer.rating)}
                  </Text>
                </View>
              </LinearGradient>

              {/* Card Body */}
              <View style={styles.playerCardBody}>
                {/* Status Row */}
                <View style={styles.playerCardStatusRow}>
                  {/* Form */}
                  <View style={[
                    styles.playerCardStatusItem,
                    { borderColor: previewPlayer.form >= 8 ? '#10B981' : previewPlayer.form >= 5 ? '#F59E0B' : '#EF4444' }
                  ]}>
                    <Ionicons 
                      name={previewPlayer.form >= 8 ? "flame" : previewPlayer.form >= 5 ? "remove-circle" : "arrow-down-circle"} 
                      size={20} 
                      color={previewPlayer.form >= 8 ? "#10B981" : previewPlayer.form >= 5 ? "#F59E0B" : "#EF4444"} 
                    />
                    <Text style={styles.playerCardStatusLabel}>Form</Text>
                    <Text style={[
                      styles.playerCardStatusValue,
                      { color: previewPlayer.form >= 8 ? "#10B981" : previewPlayer.form >= 5 ? "#F59E0B" : "#EF4444" }
                    ]}>{previewPlayer.form}/10</Text>
                  </View>

                  {/* Health */}
                  <View style={[
                    styles.playerCardStatusItem,
                    { borderColor: previewPlayer.injury ? '#EF4444' : '#10B981' }
                  ]}>
                    <Ionicons 
                      name={previewPlayer.injury ? "medical" : "heart"} 
                      size={20} 
                      color={previewPlayer.injury ? "#EF4444" : "#10B981"} 
                    />
                    <Text style={styles.playerCardStatusLabel}>Sağlık</Text>
                    <Text style={[
                      styles.playerCardStatusValue,
                      { color: previewPlayer.injury ? "#EF4444" : "#10B981" }
                    ]}>{previewPlayer.injury ? 'Sakat' : 'Fit'}</Text>
                  </View>
                </View>

                {/* Stats Grid - 2x3 */}
                {previewPlayer.stats && (
                  <View style={styles.playerCardStatsGrid}>
                    {(() => {
                      // ✅ Kaleci mi kontrol et
                      const playerPos = (previewPlayer.position || previewPlayer.pos || '').toUpperCase();
                      const isGK = playerPos === 'GK' || playerPos === 'G' || playerPos.toLowerCase().includes('goalkeeper') ||
                                   positionLabel?.toUpperCase() === 'GK';
                      
                      // ✅ Stats'leri al veya rating'den geriye doğru hesapla
                      const stats = previewPlayer.stats || {};
                      const normalizedRating = normalizeRatingTo100(previewPlayer.rating);
                      
                      // ✅ Eğer tüm stats 70 ise (default), rating'den geriye doğru hesapla
                      const allStatsDefault = (
                        (stats.pace ?? 70) === 70 &&
                        (stats.shooting ?? 70) === 70 &&
                        (stats.passing ?? 70) === 70 &&
                        (stats.dribbling ?? 70) === 70 &&
                        (stats.defending ?? 70) === 70 &&
                        (stats.physical ?? 70) === 70
                      );
                      
                      // ✅ Rating'den geriye doğru stats hesapla (eğer default ise)
                      const calculatedStats = allStatsDefault ? {
                        pace: normalizedRating,
                        shooting: normalizedRating,
                        passing: normalizedRating,
                        dribbling: normalizedRating,
                        defending: normalizedRating,
                        physical: normalizedRating,
                      } : {
                        pace: stats.pace ?? 70,
                        shooting: stats.shooting ?? 70,
                        passing: stats.passing ?? 70,
                        dribbling: stats.dribbling ?? 70,
                        defending: stats.defending ?? 70,
                        physical: stats.physical ?? 70,
                      };
                      
                      // ✅ Kaleci özellikleri (Oyuncu İstatistikleri popup ile aynı isimler: Plonjon, Refleks, Top Tutma, Yer Tutuş, Hava Topu, Bire Bir)
                      const goalkeeperStats = [
                        { label: 'Plonjon', value: calculatedStats.pace, icon: 'hand-left' },
                        { label: 'Refleks', value: calculatedStats.shooting, icon: 'flash' },
                        { label: 'Top Tutma', value: calculatedStats.passing, icon: 'hand-right' },
                        { label: 'Yer Tutuş', value: calculatedStats.dribbling, icon: 'locate' },
                        { label: 'Hava Topu', value: calculatedStats.defending, icon: 'arrow-up' },
                        { label: 'Bire Bir', value: calculatedStats.physical, icon: 'body' },
                      ];
                      
                      // ✅ Saha oyuncusu özellikleri
                      const outfieldStats = [
                        { label: 'HIZ', value: calculatedStats.pace, icon: 'flash' },
                        { label: 'ŞUT', value: calculatedStats.shooting, icon: 'football' },
                        { label: 'PAS', value: calculatedStats.passing, icon: 'swap-horizontal' },
                        { label: 'DRİBLİNG', value: calculatedStats.dribbling, icon: 'walk' },
                        { label: 'DEFANS', value: calculatedStats.defending, icon: 'shield' },
                        { label: 'FİZİK', value: calculatedStats.physical, icon: 'fitness' },
                      ];
                      
                      const finalStats = isGK ? goalkeeperStats : outfieldStats;
                      
                      return finalStats.map((stat, index) => (
                        <View key={index} style={styles.playerCardStatItem}>
                          <View style={[
                            styles.playerCardStatCircle,
                            { borderColor: getStatColor(stat.value) }
                          ]}>
                            <Text style={[styles.playerCardStatValue, { color: getStatColor(stat.value) }]}>
                              {stat.value}
                            </Text>
                          </View>
                          <Text style={styles.playerCardStatLabel}>{stat.label}</Text>
                        </View>
                      ));
                    })()}
                  </View>
                )}

                {/* ✅ Topluluk Tercihleri - SADECE communityDataVisible ise göster */}
                <View style={styles.playerPreviewCommunitySection}>
                  <View style={styles.playerPreviewCommunityHeader}>
                    <Ionicons name="people" size={16} color="#F97316" />
                    <Text style={styles.playerPreviewCommunityTitle}>🔥 Topluluk Tercihi</Text>
                  </View>
                  {communityDataVisible ? (
                    previewCommunityStats.loading ? (
                      <Text style={styles.playerPreviewCommunityLoading}>Yükleniyor...</Text>
                    ) : (
                      <View style={styles.playerPreviewCommunityContent}>
                        {/* İlk 11'e seçim */}
                        <View style={styles.playerPreviewCommunityRow}>
                          <Ionicons name="football" size={14} color="#1FA2A6" />
                          <Text style={styles.playerPreviewCommunityText}>
                            {t('matchSquad.playerCommunityTooltip', { pct: previewCommunityStats.inStartingXI })}
                          </Text>
                        </View>
                        {/* ✅ Oyuncunun en çok atandığı pozisyon */}
                        <View style={styles.playerPreviewCommunityRow}>
                          <Ionicons name="star" size={14} color="#F59E0B" />
                          <Text style={styles.playerPreviewCommunityText}>
                            Bu oyuncu en çok <Text style={styles.playerPreviewCommunityHighlight}>{previewCommunityStats.mostAssignedPosition}</Text> pozisyonuna atandı <Text style={styles.playerPreviewCommunityHighlight}>(%{previewCommunityStats.mostAssignedPositionPercentage})</Text>
                          </Text>
                        </View>
                        {/* ✅ Bu pozisyon için en çok tercih edilen oyuncu */}
                        <View style={[styles.playerPreviewCommunityRow, { borderTopWidth: 1, borderTopColor: 'rgba(255,255,255,0.1)', paddingTop: 8, marginTop: 4 }]}>
                          <Ionicons name="trophy" size={14} color="#10B981" />
                          <Text style={styles.playerPreviewCommunityText}>
                            <Text style={styles.playerPreviewCommunityHighlight}>{positionLabel}</Text> için en çok: <Text style={styles.playerPreviewCommunityHighlight}>{previewCommunityStats.topPlayerForPosition}</Text> <Text style={{ color: '#10B981' }}>(%{previewCommunityStats.topPlayerPercentage})</Text>
                          </Text>
                        </View>
                      </View>
                    )
                  ) : (
                    <View style={{ alignItems: 'center', padding: 12 }}>
                      <Ionicons name="lock-closed" size={20} color="#9CA3AF" />
                      <Text style={{ color: '#9CA3AF', fontSize: 12, marginTop: 6, textAlign: 'center' }}>
                        Tahminlerinizi kaydedin ve topluluk verilerini görün!
                      </Text>
                    </View>
                  )}
                </View>

                {/* Position Assignment Info */}
                <View style={styles.playerCardAssignInfo}>
                  <Ionicons name="locate" size={16} color="#1FA2A6" />
                  <Text style={styles.playerCardAssignText}>
                    <Text style={{ color: '#1FA2A6', fontWeight: '700' }}>{positionLabel}</Text> pozisyonuna atanacak
                  </Text>
                </View>

                {/* Action Buttons */}
                <View style={styles.playerCardActions}>
                  <TouchableOpacity
                    style={styles.playerCardCancelBtn}
                    onPress={() => setPreviewPlayer(null)}
                    activeOpacity={0.7}
                  >
                    <Text style={styles.playerCardCancelText}>Vazgeç</Text>
                  </TouchableOpacity>
                  
                  <TouchableOpacity
                    style={styles.playerCardAddBtn}
                    onPress={() => handlePlayerSelect(previewPlayer)}
                    activeOpacity={0.7}
                  >
                    <LinearGradient
                      colors={previewPlayer.injury ? ['#64748B', '#475569'] : ['#10B981', '#059669']}
                      start={{ x: 0, y: 0 }}
                      end={{ x: 1, y: 0 }}
                      style={styles.playerCardAddGradient}
                    >
                      <Ionicons name="person-add" size={18} color="#FFFFFF" />
                      <Text style={styles.playerCardAddText}>Kadroya Al</Text>
                    </LinearGradient>
                  </TouchableOpacity>
                </View>

                {/* Injury Warning */}
                {previewPlayer.injury && (
                  <View style={styles.playerCardWarning}>
                    <Ionicons name="alert-circle" size={16} color="#F59E0B" />
                    <Text style={styles.playerCardWarningText}>
                      Sakatlık nedeniyle performansı düşük olabilir
                    </Text>
                  </View>
                )}
              </View>
            </Animated.View>
          </View>
        </Modal>
      )}
    </>
  );
};

// Player Detail Modal - FULL STATS + Community Stats (tema uyumlu)
const PlayerDetailModal = ({ player, onClose, matchId, positionLabel, communityDataVisible = false }: any) => {
  const { theme } = useTheme();
  const isLight = theme === 'light';
  const themeColors = isLight ? COLORS.light : COLORS.dark;

  // ✅ Kullanıcı istatistikleri state
  const [communityStats, setCommunityStats] = React.useState<{
    inStartingXI: number; // % kaçı ilk 11'de görmek istiyor
    inThisPosition: number; // % kaçı bu pozisyonda görmek istiyor
    loading: boolean;
  }>({ inStartingXI: 0, inThisPosition: 0, loading: true });
  
  // ✅ Mock kullanıcı istatistiklerini yükle (gerçek API gelince değiştirilecek)
  React.useEffect(() => {
    const loadCommunityStats = async () => {
      try {
        // TODO: Gerçek API entegrasyonu yapılacak
        // const result = await squadPredictionsApi.getPlayerCommunityStats(matchId, player.id, positionLabel);
        // Mock data - rating'e göre daha gerçekçi değerler
        const pRating = normalizeRatingTo100(player.rating);
        const baseXI = pRating >= 85 ? 75 : pRating >= 80 ? 60 : pRating >= 75 ? 45 : 30;
        const basePosition = pRating >= 85 ? 65 : pRating >= 80 ? 50 : pRating >= 75 ? 35 : 20;
        const variance = Math.floor(Math.random() * 15);
        
        setCommunityStats({
          inStartingXI: Math.min(95, baseXI + variance),
          inThisPosition: Math.min(90, basePosition + variance),
          loading: false,
        });
      } catch (error) {
        console.warn('Community stats load failed', error);
        setCommunityStats({ inStartingXI: 0, inThisPosition: 0, loading: false });
      }
    };
    
    loadCommunityStats();
  }, [player.id, player.rating, matchId, positionLabel]);
  
  return (
    <Modal
      visible={true}
      animationType="slide"
      transparent={true}
      onRequestClose={onClose}
    >
      <View style={styles.modalOverlay}>
        <Animated.View 
          entering={Platform.OS === 'web' ? undefined : SlideInDown.duration(300)}
          exiting={Platform.OS === 'web' ? undefined : SlideOutDown.duration(300)}
          style={[styles.playerDetailModal, isLight && { backgroundColor: themeColors.popover, borderColor: themeColors.border }]}
        >
          {/* Header - açık temada açık gradient */}
          <LinearGradient
            colors={normalizeRatingTo100(player.rating) >= 85 ? ['#C9A44C', '#8B6914'] : isLight ? [themeColors.muted, themeColors.card] : ['#1E3A3A', '#0F2A24']}
            style={[
              styles.playerDetailHeader,
              normalizeRatingTo100(player.rating) >= 85 && styles.playerDetailHeaderElite,
            ]}
          >
            <TouchableOpacity onPress={onClose} style={styles.modalCloseButton}>
              <Ionicons name="close" size={24} color={isLight ? themeColors.foreground : '#FFFFFF'} />
            </TouchableOpacity>

            <View style={styles.playerDetailHeaderContent}>
              <View style={[
                styles.playerDetailRating,
                { backgroundColor: normalizeRatingTo100(player.rating) >= 85 ? '#C9A44C' : '#1FA2A6' }
              ]}>
                <Text style={styles.playerDetailRatingText}>
                  {formatRatingDisplay(player.rating)}
                </Text>
              </View>

              <View style={styles.playerDetailInfo}>
                <Text style={[styles.playerDetailName, isLight && { color: themeColors.foreground }]}>
                {formatPlayerDisplayName(player)}
              </Text>
                <Text style={[styles.playerDetailMeta, isLight && { color: themeColors.mutedForeground }]}>
                  {player.position} • {player.team} • {player.age} yaş
                </Text>
                {/* Ortalama reyting (sarı) - sağda */}
                <View style={styles.playerDetailInfoRow}>
                  <View />
                  <Text style={styles.playerDetailRatingHighlight}>
                    {formatRatingDisplay(player.rating)}
                  </Text>
                </View>
                <View style={styles.playerDetailBadges}>
                  <View style={[styles.nationalityBadge, isLight && { backgroundColor: 'rgba(15, 42, 36, 0.12)' }]}>
                    <Ionicons name="flag" size={12} color={isLight ? themeColors.foreground : '#FFFFFF'} />
                    <Text style={[styles.nationalityText, isLight && { color: themeColors.foreground }]}>{player.nationality}</Text>
                  </View>
                  {player.form >= 8 && (
                    <View style={styles.formBadgeLarge}>
                      <Ionicons name="flame" size={12} color="#F59E0B" />
                      <Text style={styles.formTextLarge}>Form Yüksek</Text>
                    </View>
                  )}
                  {player.injury && (
                    <View style={styles.injuryBadgeLarge}>
                      <Ionicons name="warning" size={12} color="#EF4444" />
                      <Text style={styles.injuryTextLarge}>Sakatlık</Text>
                    </View>
                  )}
                  {/* Elite badge for 85+ */}
                  {normalizeRatingTo100(player.rating) >= 85 && (
                    <View style={styles.eliteBadgeLarge}>
                      <Ionicons name="star" size={12} color="#C9A44C" />
                      <Text style={styles.eliteTextLarge}>Elit</Text>
                    </View>
                  )}
                </View>
              </View>
            </View>
          </LinearGradient>

          {/* Stats & Info - SCROLL YOK, TEK SAYFADA SIĞMALI */}
          <View style={[styles.playerDetailContentNoScroll, isLight && { borderTopColor: themeColors.border }]}>
            {/* ✅ Oyuncu İstatistikleri - 1 sütun 6 satır dikey layout */}
            <Text style={[styles.playerDetailSectionTitleCompact, isLight && { color: themeColors.foreground }]}>📊 Oyuncu İstatistikleri</Text>
            
            <View style={styles.statsGridCompact}>
              {(() => {
                const playerPos = (player.position || player.pos || '').toUpperCase();
                const isGK = playerPos === 'GK' || playerPos === 'G' || playerPos.toLowerCase().includes('goalkeeper') || 
                             positionLabel?.toUpperCase() === 'GK' || positionLabel?.toLowerCase().includes('kaleci');
                const goalkeeperStatNames: Record<string, string> = {
                  pace: 'Plonjon', shooting: 'Refleks', passing: 'Top Tutma',
                  dribbling: 'Yer Tutuş', defending: 'Hava Topu', physical: 'Bire Bir'
                };
                const outfieldStatNames: Record<string, string> = {
                  pace: 'Hız', shooting: 'Şut', passing: 'Pas',
                  dribbling: 'Dribling', defending: 'Savunma', physical: 'Fizik'
                };
                const statNames = isGK ? goalkeeperStatNames : outfieldStatNames;
                const STAT_KEYS = ['pace', 'shooting', 'passing', 'dribbling', 'defending', 'physical'] as const;
                const stats = player.stats || {};
                const normalizedRating = normalizeRatingTo100(player.rating ?? null);
                const allStatsDefault = STAT_KEYS.every(k => (stats[k] ?? 70) === 70);
                const calculatedStats = allStatsDefault
                  ? { pace: normalizedRating, shooting: normalizedRating, passing: normalizedRating, dribbling: normalizedRating, defending: normalizedRating, physical: normalizedRating }
                  : { pace: stats.pace ?? 70, shooting: stats.shooting ?? 70, passing: stats.passing ?? 70, dribbling: stats.dribbling ?? 70, defending: stats.defending ?? 70, physical: stats.physical ?? 70 };
                return STAT_KEYS.map((key) => {
                  const value = Math.max(50, Math.min(99, calculatedStats[key]));
                  const statColor = value >= 80 ? '#1FA2A6' : value >= 70 ? '#F59E0B' : '#9CA3AF';
                  return (
                    <View key={key} style={[styles.statItemCompact, isLight && { backgroundColor: themeColors.muted, borderColor: themeColors.border }]}>
                      <Text style={[styles.statLabelCompact, isLight && { color: themeColors.mutedForeground }]}>{statNames[key] || key}</Text>
                      <View style={styles.statBarBackgroundCompact}>
                        <View style={[styles.statBarFillCompact, { width: `${value}%`, backgroundColor: statColor }]} />
                      </View>
                      <Text style={[styles.statValueCompact, { color: statColor }]}>{value}</Text>
                    </View>
                  );
                });
              })()}
            </View>

            {/* ✅ Form/Pozisyon - yaş üstte (position • team • age) gösterildiği için altta tekrar yok */}
            <View style={styles.additionalInfoCompact}>
              <View style={[styles.infoChip, isLight && { backgroundColor: 'rgba(31, 162, 166, 0.08)', borderColor: themeColors.border }]}>
                <Ionicons name="fitness" size={14} color="#1FA2A6" />
                <Text style={[styles.infoChipText, isLight && { color: themeColors.foreground }]}>Form: {player.form}/10</Text>
              </View>
              <View style={[styles.infoChip, isLight && { backgroundColor: 'rgba(31, 162, 166, 0.08)', borderColor: themeColors.border }]}>
                <Ionicons name="shirt" size={14} color="#1FA2A6" />
                <Text style={[styles.infoChipText, isLight && { color: themeColors.foreground }]}>{player.position}</Text>
              </View>
            </View>
          </View>

          {/* Close Button */}
          <View style={[styles.playerDetailActions, isLight && { borderTopColor: themeColors.border }]}>
            <TouchableOpacity
              style={styles.playerDetailCloseButton}
              onPress={onClose}
              activeOpacity={0.8}
            >
              <LinearGradient
                colors={['#1FA2A6', '#047857']}
                style={styles.playerDetailCloseButtonGradient}
              >
                <Text style={styles.playerDetailCloseButtonText}>Kapat</Text>
              </LinearGradient>
            </TouchableOpacity>
          </View>
        </Animated.View>
      </View>
    </Modal>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'transparent', // ✅ Grid pattern görünsün - MatchDetail'den geliyor
  },
  
  // ✅ Kullanıcı Tercih İstatistikleri Kartı
  // ✅ Kompakt tercih istatistikleri kartı
  preferenceStatsCardCompact: {
    marginHorizontal: 12,
    marginTop: 4,
    marginBottom: 4,
    backgroundColor: 'rgba(31, 162, 166, 0.1)',
    borderRadius: 8,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.2)',
  },
  preferenceStatsRowCompact: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 10,
    paddingVertical: 6,
    gap: 6,
    flexWrap: 'wrap',
  },
  preferenceStatsTextCompact: {
    color: '#9CA3AF',
    fontSize: 11,
    flex: 1,
  },
  preferenceFormationBadges: {
    flexDirection: 'row',
    gap: 6,
  },
  preferenceFormationBadge: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: 'rgba(255, 255, 255, 0.08)',
    paddingHorizontal: 6,
    paddingVertical: 2,
    borderRadius: 4,
    gap: 3,
  },
  preferenceFormationBadgeText: {
    color: '#FFFFFF',
    fontSize: 10,
    fontWeight: '600',
  },
  
  // ✅ Yeni Tercih Kartı Stilleri (Sol: kullanıcı, Sağ: formasyonlar)
  preferenceStatsCardNew: {
    flexDirection: 'row',
    marginHorizontal: 12,
    marginTop: 4,
    marginBottom: 4,
    backgroundColor: 'rgba(31, 162, 166, 0.08)',
    borderRadius: 10,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.15)',
    overflow: 'hidden',
  },
  preferenceStatsLeftSection: {
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
    paddingVertical: 8,
    paddingHorizontal: 12,
    backgroundColor: 'rgba(31, 162, 166, 0.1)',
    borderRightWidth: 1,
    borderRightColor: 'rgba(255, 255, 255, 0.05)',
  },
  preferenceStatsUserCount: {
    color: '#1FA2A6',
    fontSize: 16,
    fontWeight: '700',
  },
  preferenceStatsUserLabel: {
    color: '#6B7280',
    fontSize: 9,
    marginTop: 1,
  },
  preferenceStatsRightSection: {
    flex: 1,
    paddingVertical: 4,
    paddingHorizontal: 8,
    gap: 2,
  },
  preferenceFormationRowClickable: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 5,
    paddingHorizontal: 6,
    borderRadius: 6,
    backgroundColor: 'rgba(255, 255, 255, 0.03)',
    gap: 4,
  },
  preferenceFormationLabel: {
    color: '#9CA3AF',
    fontSize: 10,
    width: 42,
  },
  preferenceFormationValue: {
    color: '#FFFFFF',
    fontSize: 12,
    fontWeight: '600',
    flex: 1,
  },
  preferenceFormationPercent: {
    color: '#10B981',
    fontSize: 10,
    fontWeight: '700',
  },
  
  // ✅ İlk 11 Popup Stilleri - Design System Uyumlu
  startingXIPopupOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.92)',
    justifyContent: 'center',
    alignItems: 'center',
    paddingHorizontal: 16,
    paddingVertical: 40, // ✅ Üst/alt boşluk - ekran taşmasını önle
  },
  startingXIPopupModal: {
    width: '100%',
    maxWidth: 380, // ✅ STANDART: 380px genişlik
    maxHeight: '90%', // ✅ Ekran yüksekliğinin %90'ı - taşma önlenir
    borderRadius: 20,
    overflow: 'hidden',
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.3)',
  },
  startingXIPopupGradient: {
    paddingVertical: 14,
    paddingHorizontal: 14,
  },
  startingXIPopupHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 16,
    paddingBottom: 12,
    borderBottomWidth: 1,
    borderBottomColor: 'rgba(31, 162, 166, 0.2)',
  },
  startingXIPopupScrollOld: {
    // Kullanılmıyor - artık scroll yok
    maxHeight: 400,
  },
  startingXIPopupItem: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: 'rgba(255, 255, 255, 0.05)',
    borderRadius: 8,
    padding: 12,
    marginBottom: 6,
    gap: 12,
  },
  startingXIPopupItemGK: {
    backgroundColor: 'rgba(59, 130, 246, 0.15)',
    borderWidth: 1,
    borderColor: 'rgba(59, 130, 246, 0.3)',
  },
  startingXIPopupItemNumber: {
    width: 32,
    height: 32,
    borderRadius: 16,
    backgroundColor: 'rgba(31, 162, 166, 0.2)',
    alignItems: 'center',
    justifyContent: 'center',
  },
  startingXIPopupItemNumberText: {
    color: '#1FA2A6',
    fontSize: 14,
    fontWeight: '700',
  },
  startingXIPopupItemName: {
    color: '#FFFFFF',
    fontSize: 14,
    fontWeight: '600',
    flex: 1,
  },
  startingXIPopupItemPosition: {
    color: '#9CA3AF',
    fontSize: 11,
    fontWeight: '500',
    backgroundColor: 'rgba(255, 255, 255, 0.05)',
    paddingHorizontal: 8,
    paddingVertical: 4,
    borderRadius: 6,
  },
  startingXIPopupFooterOld: {
    marginTop: 12,
    paddingTop: 12,
    borderTopWidth: 1,
    borderTopColor: 'rgba(31, 162, 166, 0.2)',
  },
  startingXIPopupFooterTextOld: {
    color: 'rgba(255, 255, 255, 0.4)',
    fontSize: 11,
    textAlign: 'center',
    fontStyle: 'italic',
  },
  
  // ✅ Formasyon Detay Popup Stilleri - STANDART POPUP KURALLARI
  formationDetailOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.8)',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 16,
  },
  formationDetailModal: {
    width: '100%',
    maxWidth: 380, // ✅ STANDART: 380px genişlik
    maxHeight: '90%',
    borderRadius: 16,
    overflow: 'hidden',
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.2)',
    backgroundColor: '#1A2E2A', // ✅ STANDART: Koyu yeşil-mavi
  },
  formationDetailGradient: {
    padding: 20,
    paddingBottom: 30,
  },
  formationDetailHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 12,
  },
  formationDetailTitle: {
    color: '#FFFFFF',
    fontSize: 16,
    fontWeight: '700',
  },
  formationDetailTabs: {
    flexDirection: 'row',
    backgroundColor: 'rgba(255, 255, 255, 0.05)',
    borderRadius: 10,
    padding: 4,
    marginBottom: 16,
  },
  formationDetailTab: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    paddingVertical: 8,
    borderRadius: 8,
    gap: 6,
  },
  formationDetailTabActive: {
    backgroundColor: 'rgba(31, 162, 166, 0.2)',
  },
  formationDetailTabText: {
    color: '#6B7280',
    fontSize: 13,
    fontWeight: '600',
  },
  formationDetailTabTextActive: {
    color: '#FFFFFF',
  },
  formationDetailScroll: {
    maxHeight: 450,
  },
  formationDetailSelected: {
    alignItems: 'center',
    paddingVertical: 16,
    backgroundColor: 'rgba(31, 162, 166, 0.1)',
    borderRadius: 12,
    marginBottom: 16,
  },
  formationDetailSelectedLabel: {
    color: '#9CA3AF',
    fontSize: 11,
    marginBottom: 4,
  },
  formationDetailSelectedValue: {
    color: '#FFFFFF',
    fontSize: 28,
    fontWeight: '700',
  },
  formationDetailSelectedBadge: {
    backgroundColor: '#10B981',
    paddingHorizontal: 12,
    paddingVertical: 4,
    borderRadius: 12,
    marginTop: 8,
  },
  formationDetailSelectedBadgeText: {
    color: '#FFFFFF',
    fontSize: 14,
    fontWeight: '700',
  },
  formationDetailSection: {
    marginBottom: 20,
  },
  formationDetailSectionTitle: {
    color: '#FFFFFF',
    fontSize: 13,
    fontWeight: '700',
    marginBottom: 10,
  },
  formationDetailItem: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 8,
    paddingHorizontal: 10,
    backgroundColor: 'rgba(255, 255, 255, 0.03)',
    borderRadius: 8,
    marginBottom: 6,
  },
  formationDetailItemFirst: {
    backgroundColor: 'rgba(16, 185, 129, 0.1)',
    borderWidth: 1,
    borderColor: 'rgba(16, 185, 129, 0.3)',
  },
  formationDetailItemRank: {
    color: '#6B7280',
    fontSize: 12,
    fontWeight: '600',
    width: 28,
  },
  formationDetailItemName: {
    color: '#FFFFFF',
    fontSize: 14,
    fontWeight: '500',
    flex: 1,
  },
  formationDetailItemCount: {
    color: '#6B7280',
    fontSize: 10,
    marginRight: 8,
  },
  formationDetailItemPercentBadge: {
    backgroundColor: 'rgba(255, 255, 255, 0.1)',
    paddingHorizontal: 8,
    paddingVertical: 3,
    borderRadius: 6,
  },
  formationDetailItemPercent: {
    color: '#FFFFFF',
    fontSize: 12,
    fontWeight: '700',
  },
  formationDetailPositionGroup: {
    borderRadius: 12,
    padding: 12,
    marginBottom: 10,
    borderWidth: 1,
    // ✅ Arka plan ve border pozisyon grubuna göre dinamik uygulanır
  },
  formationDetailPositionHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 8,
  },
  formationDetailPositionLabel: {
    fontSize: 13,
    fontWeight: '700',
    letterSpacing: 0.3,
    // ✅ Renk pozisyon grubuna göre dinamik uygulanır
  },
  formationDetailPositionBadge: {
    paddingHorizontal: 10,
    paddingVertical: 4,
    borderRadius: 10,
  },
  formationDetailPositionBadgeText: {
    color: '#FFFFFF',
    fontSize: 11,
    fontWeight: '700',
  },
  formationDetailPositionItem: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 6,
    paddingHorizontal: 10,
    borderRadius: 8,
    marginBottom: 4,
    backgroundColor: 'rgba(255, 255, 255, 0.04)',
    borderLeftWidth: 2,
    borderLeftColor: '#6B7280',
  },
  formationDetailPositionItemFirst: {
    backgroundColor: 'rgba(255, 255, 255, 0.08)',
  },
  formationDetailPositionItemRank: {
    color: '#6B7280',
    fontSize: 10,
    fontWeight: '600',
    width: 20,
  },
  formationDetailPositionItemName: {
    color: '#FFFFFF',
    fontSize: 11,
    fontWeight: '500',
    flex: 1,
  },
  formationDetailPositionItemCount: {
    color: '#6B7280',
    fontSize: 9,
    marginRight: 6,
  },
  formationDetailPositionItemPercent: {
    fontSize: 11,
    fontWeight: '700',
    width: 32,
    textAlign: 'right',
  },
  
  // ✅ İlk 11 Popup Stilleri
  startingXIPopupOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.85)',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
  },
  startingXIPopupModal: {
    width: '100%',
    maxWidth: 340,
    // ✅ maxHeight kaldırıldı - tüm içerik görünsün
    borderRadius: 14,
    overflow: 'hidden',
  },
  startingXIPopupGradient: {
    paddingVertical: 12,
    paddingHorizontal: 14,
  },
  startingXIPopupTitleRow: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
  },
  startingXIPopupTitle: {
    color: '#FFFFFF',
    fontSize: 18,
    fontWeight: '700',
    letterSpacing: 0.3,
  },
  startingXIPopupTeamInfo: {
    alignItems: 'center',
    marginBottom: 16,
    paddingBottom: 12,
    borderBottomWidth: 1,
    borderBottomColor: 'rgba(31, 162, 166, 0.2)',
  },
  startingXIPopupTeamName: {
    color: '#1FA2A6',
    fontSize: 16,
    fontWeight: '700',
    letterSpacing: 0.5,
  },
  startingXIPopupTeamSubtext: {
    color: 'rgba(255, 255, 255, 0.5)',
    fontSize: 11,
    marginTop: 4,
    fontStyle: 'italic',
  },
  startingXIPopupPlayerList: {
    // ✅ Scroll yok - tüm 11 oyuncu tek ekranda görünsün
  },
  startingXIPopupPlayerItem: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: 'rgba(31, 162, 166, 0.08)',
    borderRadius: 8,
    paddingVertical: 6,
    paddingHorizontal: 8,
    marginBottom: 3,
    borderLeftWidth: 3,
    borderLeftColor: 'rgba(31, 162, 166, 0.4)',
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.1)',
  },
  startingXIPopupPlayerItemGK: {
    backgroundColor: 'rgba(245, 158, 11, 0.12)',
    borderLeftColor: '#F59E0B',
    borderLeftWidth: 4,
    borderColor: 'rgba(245, 158, 11, 0.2)',
  },
  startingXIPopupPlayerNumber: {
    width: 24,
    height: 24,
    borderRadius: 12,
    backgroundColor: 'rgba(31, 162, 166, 0.2)',
    alignItems: 'center',
    justifyContent: 'center',
    marginRight: 6,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.3)',
  },
  startingXIPopupPlayerNumberText: {
    color: '#1FA2A6',
    fontSize: 11,
    fontWeight: '700',
  },
  startingXIPopupPlayerInfo: {
    flex: 1,
  },
  startingXIPopupPlayerName: {
    color: '#FFFFFF',
    fontSize: 12,
    fontWeight: '600',
  },
  startingXIPopupPlayerPosition: {
    color: 'rgba(255, 255, 255, 0.5)',
    fontSize: 9,
    marginTop: 1,
  },
  startingXIPopupPlayerPercent: {
    paddingHorizontal: 10,
    paddingVertical: 5,
    borderRadius: 10,
    minWidth: 48,
    alignItems: 'center',
    justifyContent: 'center',
  },
  startingXIPopupPlayerPercentText: {
    color: '#FFFFFF',
    fontSize: 12,
    fontWeight: '700',
    letterSpacing: 0.3,
  },
  startingXIPopupFooter: {
    alignItems: 'center',
    paddingTop: 14,
    marginTop: 12,
    borderTopWidth: 1,
    borderTopColor: 'rgba(31, 162, 166, 0.2)',
  },
  startingXIPopupFooterText: {
    color: 'rgba(255, 255, 255, 0.4)',
    fontSize: 11,
    fontStyle: 'italic',
    letterSpacing: 0.3,
  },
  
  // ✅ Maç Başlarken Tahmin Tamamlama Popup Stilleri
  completePredictionPopupOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.85)',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
  },
  completePredictionPopupModal: {
    width: '100%',
    maxWidth: 380, // ✅ STANDART: 380px genişlik
    borderRadius: 16,
    overflow: 'hidden',
  },
  completePredictionPopupGradient: {
    padding: 24,
  },
  completePredictionPopupHeader: {
    alignItems: 'center',
    marginBottom: 16,
  },
  completePredictionPopupTitle: {
    color: '#FFFFFF',
    fontSize: 20,
    fontWeight: '700',
    marginTop: 8,
    textAlign: 'center',
  },
  completePredictionPopupMessage: {
    color: '#E5E7EB',
    fontSize: 14,
    lineHeight: 22,
    textAlign: 'center',
    marginBottom: 12,
  },
  completePredictionPopupBold: {
    color: '#1FA2A6',
    fontWeight: '700',
  },
  completePredictionPopupWarning: {
    color: '#F59E0B',
    fontSize: 12,
    textAlign: 'center',
    fontStyle: 'italic',
    marginBottom: 24,
  },
  completePredictionPopupButtons: {
    flexDirection: 'row',
    gap: 12,
  },
  completePredictionPopupButtonSecondary: {
    flex: 1,
    paddingVertical: 12,
    paddingHorizontal: 16,
    borderRadius: 10,
    backgroundColor: 'rgba(255, 255, 255, 0.1)',
    alignItems: 'center',
  },
  completePredictionPopupButtonSecondaryText: {
    color: '#9CA3AF',
    fontSize: 14,
    fontWeight: '600',
  },
  completePredictionPopupButtonPrimary: {
    flex: 2,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    paddingVertical: 12,
    paddingHorizontal: 16,
    borderRadius: 10,
    backgroundColor: '#1FA2A6',
  },
  completePredictionPopupButtonPrimaryText: {
    color: '#FFFFFF',
    fontSize: 14,
    fontWeight: '700',
  },
  
  // ✅ Tam Tercih Özeti Popup Stilleri - STANDART POPUP KURALLARI
  fullPreferenceSummaryOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.8)',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 16,
  },
  fullPreferenceSummaryModal: {
    width: '100%',
    maxWidth: 380, // ✅ STANDART: 380px genişlik
    maxHeight: '90%',
    borderRadius: 16,
    overflow: 'hidden',
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.2)', // ✅ STANDART: Turkuaz border
    backgroundColor: '#1A2E2A', // ✅ STANDART: Koyu yeşil-mavi
  },
  fullPreferenceSummaryGradient: {
    padding: 20,
  },
  fullPreferenceSummaryHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 12,
  },
  fullPreferenceSummaryUserCount: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 6,
    backgroundColor: 'rgba(16, 185, 129, 0.15)',
    paddingVertical: 8,
    paddingHorizontal: 16,
    borderRadius: 20,
    marginBottom: 16,
  },
  fullPreferenceSummaryUserCountText: {
    color: '#10B981',
    fontSize: 13,
    fontWeight: '600',
  },
  fullPreferenceSummaryScroll: {
    maxHeight: 400,
  },
  fullPreferenceSummarySection: {
    marginBottom: 16,
  },
  fullPreferenceSummaryNote: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 6,
    paddingVertical: 12,
    paddingHorizontal: 16,
    backgroundColor: 'rgba(156, 163, 175, 0.1)',
    borderRadius: 8,
    marginTop: 8,
  },
  fullPreferenceSummaryNoteText: {
    color: '#9CA3AF',
    fontSize: 11,
    fontStyle: 'italic',
  },
  fullPreferenceSummarySectionTitle: {
    color: '#FFFFFF',
    fontSize: 14,
    fontWeight: '700',
    marginBottom: 12,
  },
  fullPreferenceFormationGroup: {
    backgroundColor: 'rgba(255, 255, 255, 0.03)',
    borderRadius: 10,
    padding: 10,
    marginBottom: 8,
  },
  fullPreferenceFormationGroupHeader: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginBottom: 6,
  },
  fullPreferenceFormationGroupTitle: {
    color: '#9CA3AF',
    fontSize: 11,
    fontWeight: '600',
    textTransform: 'uppercase',
    letterSpacing: 0.5,
  },
  fullPreferenceFormationGroupArrow: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
  },
  fullPreferenceFormationGroupArrowText: {
    color: '#F59E0B',
    fontSize: 10,
    fontWeight: '600',
  },
  fullPreferenceFormationItem: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 6,
    paddingHorizontal: 8,
    borderRadius: 6,
    marginBottom: 4,
    backgroundColor: 'rgba(255, 255, 255, 0.02)',
  },
  fullPreferenceFormationItemFirst: {
    backgroundColor: 'rgba(16, 185, 129, 0.1)',
  },
  fullPreferenceFormationItemRank: {
    color: '#6B7280',
    fontSize: 11,
    fontWeight: '600',
    width: 24,
  },
  fullPreferenceFormationItemName: {
    color: '#FFFFFF',
    fontSize: 13,
    fontWeight: '500',
    flex: 1,
  },
  fullPreferenceFormationItemNameFirst: {
    color: '#10B981',
    fontWeight: '700',
  },
  fullPreferenceFormationItemCount: {
    color: '#6B7280',
    fontSize: 10,
    marginRight: 8,
  },
  fullPreferenceFormationItemPercentage: {
    color: '#9CA3AF',
    fontSize: 12,
    fontWeight: '700',
    width: 36,
    textAlign: 'right',
  },
  fullPreferencePositionGroup: {
    backgroundColor: 'rgba(255, 255, 255, 0.03)',
    borderRadius: 10,
    padding: 10,
    marginBottom: 8,
  },
  fullPreferencePositionGroupTitle: {
    color: '#1FA2A6',
    fontSize: 11,
    fontWeight: '600',
    marginBottom: 6,
  },
  fullPreferencePositionItem: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 5,
    paddingHorizontal: 8,
    borderRadius: 6,
    marginBottom: 3,
    backgroundColor: 'rgba(255, 255, 255, 0.02)',
    borderLeftWidth: 2,
    borderLeftColor: '#6B7280',
  },
  fullPreferencePositionItemFirst: {
    backgroundColor: 'rgba(31, 162, 166, 0.1)',
  },
  fullPreferencePositionItemRank: {
    color: '#6B7280',
    fontSize: 10,
    fontWeight: '600',
    width: 20,
  },
  fullPreferencePositionItemName: {
    color: '#FFFFFF',
    fontSize: 12,
    fontWeight: '500',
    flex: 1,
  },
  fullPreferencePositionItemNameFirst: {
    color: '#1FA2A6',
    fontWeight: '700',
  },
  fullPreferencePositionItemCount: {
    color: '#6B7280',
    fontSize: 9,
    marginRight: 6,
  },
  fullPreferencePositionItemPercentage: {
    fontSize: 11,
    fontWeight: '700',
    width: 32,
    textAlign: 'right',
  },
  
  // Eski stiller (geriye uyumluluk için)
  preferenceStatsCard: {
    marginHorizontal: 12,
    marginTop: 8,
    marginBottom: 4,
    borderRadius: 12,
    overflow: 'hidden',
  },
  preferenceStatsGradient: {
    padding: 12,
  },
  preferenceStatsHeader: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    marginBottom: 8,
  },
  preferenceStatsTitle: {
    color: '#1FA2A6',
    fontSize: 13,
    fontWeight: '600',
  },
  preferenceStatsContent: {
    gap: 6,
  },
  preferenceStatRow: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  preferenceStatLabel: {
    color: '#9CA3AF',
    fontSize: 12,
  },
  preferenceStatValue: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
  },
  preferenceStatFormation: {
    color: '#FFFFFF',
    fontSize: 13,
    fontWeight: '600',
  },
  preferenceStatPercentage: {
    color: '#10B981',
    fontSize: 12,
    fontWeight: '700',
    backgroundColor: 'rgba(16, 185, 129, 0.2)',
    paddingHorizontal: 6,
    paddingVertical: 2,
    borderRadius: 4,
  },
  preferenceStatsHint: {
    color: '#6B7280',
    fontSize: 10,
    fontStyle: 'italic',
    marginTop: 8,
    textAlign: 'center',
  },
  
  // ✅ Tercih Popup Stilleri - STANDART POPUP KURALLARI
  preferencePopupOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.8)',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 16,
  },
  preferencePopupModal: {
    width: '100%',
    maxWidth: 380, // ✅ STANDART: 380px genişlik
    borderRadius: 16,
    overflow: 'hidden',
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.2)', // ✅ STANDART: Turkuaz border
    backgroundColor: '#1A2E2A', // ✅ STANDART: Koyu yeşil-mavi
  },
  preferencePopupGradient: {
    padding: 20,
  },
  preferencePopupHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 16,
  },
  preferencePopupTitleRow: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
  },
  preferencePopupTitle: {
    color: '#FFFFFF',
    fontSize: 16,
    fontWeight: '700',
  },
  preferencePopupPositionInfo: {
    alignItems: 'center',
    marginBottom: 16,
    paddingBottom: 16,
    borderBottomWidth: 1,
    borderBottomColor: 'rgba(255, 255, 255, 0.1)',
  },
  preferencePopupPositionLabel: {
    color: '#9CA3AF',
    fontSize: 11,
    textTransform: 'uppercase',
    letterSpacing: 1,
  },
  preferencePopupPlayerName: {
    color: '#FFFFFF',
    fontSize: 18,
    fontWeight: '700',
    marginTop: 4,
  },
  preferencePopupBadge: {
    backgroundColor: '#10B981',
    paddingHorizontal: 12,
    paddingVertical: 4,
    borderRadius: 12,
    marginTop: 8,
  },
  preferencePopupBadgeText: {
    color: '#FFFFFF',
    fontSize: 14,
    fontWeight: '700',
  },
  preferencePopupList: {
    marginBottom: 12,
  },
  preferencePopupListTitle: {
    color: '#9CA3AF',
    fontSize: 12,
    marginBottom: 8,
  },
  preferencePopupItem: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: 'rgba(255, 255, 255, 0.05)',
    borderRadius: 8,
    padding: 10,
    marginBottom: 6,
    borderLeftWidth: 2,
    borderLeftColor: '#6B7280',
  },
  preferencePopupItemSelected: {
    backgroundColor: 'rgba(16, 185, 129, 0.15)',
  },
  preferencePopupItemRank: {
    color: '#6B7280',
    fontSize: 12,
    fontWeight: '600',
    width: 24,
  },
  preferencePopupItemName: {
    color: '#FFFFFF',
    fontSize: 13,
    fontWeight: '500',
    flex: 1,
  },
  preferencePopupItemNameSelected: {
    color: '#10B981',
  },
  preferencePopupItemStats: {
    alignItems: 'flex-end',
  },
  preferencePopupItemCount: {
    color: '#9CA3AF',
    fontSize: 10,
  },
  preferencePopupItemPercentage: {
    fontSize: 13,
    fontWeight: '700',
  },
  preferencePopupTotal: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 6,
    paddingTop: 12,
    borderTopWidth: 1,
    borderTopColor: 'rgba(255, 255, 255, 0.1)',
  },
  preferencePopupTotalText: {
    color: '#9CA3AF',
    fontSize: 11,
  },
  
  // ✅ Oyuncu Reyting Dropdown Stilleri
  playerRatingDropdownHeader: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    backgroundColor: 'rgba(201, 164, 76, 0.1)',
    borderWidth: 1,
    borderColor: 'rgba(201, 164, 76, 0.3)',
    borderRadius: 12,
    padding: 12,
    marginTop: 16,
  },
  playerRatingDropdownHeaderLeft: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
  },
  playerRatingDropdownHeaderText: {
    color: '#C9A44C',
    fontSize: 13,
    fontWeight: '600',
  },
  playerRatingDropdownHeaderRight: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
  },
  playerRatingDropdownAvg: {
    color: '#C9A44C',
    fontSize: 16,
    fontWeight: '700',
  },
  playerRatingDropdownContent: {
    backgroundColor: 'rgba(0, 0, 0, 0.3)',
    borderRadius: 12,
    padding: 12,
    marginTop: 8,
    gap: 8,
  },
  playerRatingDropdownRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  playerRatingDropdownRowLeft: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
    width: 100,
  },
  playerRatingDropdownEmoji: {
    fontSize: 14,
  },
  playerRatingDropdownTitle: {
    color: '#9CA3AF',
    fontSize: 11,
    fontWeight: '500',
  },
  playerRatingDropdownRowRight: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    marginLeft: 8,
  },
  playerRatingDropdownBar: {
    flex: 1,
    height: 6,
    backgroundColor: 'rgba(255, 255, 255, 0.1)',
    borderRadius: 3,
    overflow: 'hidden',
  },
  playerRatingDropdownBarFill: {
    height: '100%',
    borderRadius: 3,
  },
  playerRatingDropdownValue: {
    fontSize: 12,
    fontWeight: '700',
    width: 28,
    textAlign: 'right',
  },
  playerRatingDropdownVoters: {
    color: '#6B7280',
    fontSize: 9,
    width: 32,
    textAlign: 'right',
  },
  
  // ✅ Oyuncu Kartı Tercih Badge
  playerPreferenceBadge: {
    position: 'absolute',
    bottom: -4,
    right: -4,
    backgroundColor: '#10B981',
    borderRadius: 8,
    paddingHorizontal: 4,
    paddingVertical: 1,
    zIndex: 10,
    borderWidth: 1,
    borderColor: '#FFFFFF',
  },
  playerPreferenceBadgeText: {
    color: '#FFFFFF',
    fontSize: 8,
    fontWeight: '700',
  },
  
  // Empty State
  emptyStateContainer: {
    flex: 1,
    padding: 16,
    justifyContent: 'center',
  },
  emptyStateContent: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'rgba(255, 255, 255, 0.1)',
    borderRadius: 16,
    padding: 24,
    margin: 40,
  },
  emptyStateBall: {
    marginBottom: 16,
  },
  emptyStateBallEmoji: {
    fontSize: 48,
  },
  emptyStateTitle: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#FFFFFF',
    marginBottom: 8,
    textShadowColor: 'rgba(0,0,0,0.5)',
    textShadowOffset: { width: 0, height: 1 },
    textShadowRadius: 2,
  },
  emptyStateSubtitle: {
    fontSize: 14,
    color: '#FFFFFF',
    textAlign: 'center',
    textShadowColor: 'rgba(0,0,0,0.5)',
    textShadowOffset: { width: 0, height: 1 },
    textShadowRadius: 2,
  },
  selectFormationButton: {
    height: 50,
    borderRadius: 12,
    overflow: 'hidden',
    marginTop: 12, // 4px yukarı (önceki 16)
  },
  
  // Football Field – boyutlar PITCH_LAYOUT (config/constants) tek kaynak
  fieldContainer: {
    width: isWeb ? '100%' : width - PITCH_LAYOUT.H_PADDING,
    maxWidth: isWeb ? PITCH_LAYOUT.WEB_MAX_WIDTH : undefined,
    height: isWeb ? PITCH_LAYOUT.WEB_HEIGHT : (width - PITCH_LAYOUT.H_PADDING) * PITCH_LAYOUT.ASPECT_RATIO,
    alignSelf: 'center',
    borderRadius: 12,
    overflow: 'hidden',
    ...Platform.select({
      ios: {
        shadowColor: '#000',
        shadowOffset: { width: 0, height: 4 },
        shadowOpacity: 0.3,
        shadowRadius: 8,
      },
      android: {
        elevation: 8,
      },
      web: {
        boxShadow: '0px 4px 8px rgba(0, 0, 0, 0.3)',
      },
    }),
  },
  fieldGradient: {
    flex: 1,
  },
  fieldSvg: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
  },
  
  // Main Container – y ekseni boşlukları azaltıldı, saha yüksekliği arttı
  mainContainer: {
    flex: 1,
    flexDirection: 'column',
    paddingHorizontal: 12,
    paddingTop: 8,
    paddingBottom: 8,
  },
  mainField: {
    width: isWeb ? '100%' : width - PITCH_LAYOUT.H_PADDING,
    maxWidth: isWeb ? PITCH_LAYOUT.WEB_MAX_WIDTH : undefined,
    height: isWeb ? PITCH_LAYOUT.WEB_HEIGHT : (width - PITCH_LAYOUT.H_PADDING) * PITCH_LAYOUT.ASPECT_RATIO,
    alignSelf: 'center',
    marginBottom: 0,
  },
  playersContainer: {
    flex: 1,
    position: 'relative',
    overflow: 'visible', // ✅ Kartların kesilmemesi için
  },
  
  // Player Slot - Centered on position
  playerSlot: {
    position: 'absolute',
    transform: [{ translateX: -32 }, { translateY: -38 }], // Half of card size
    zIndex: 5,
    elevation: 5,
  },
  playerCardWrapper: {
    position: 'relative',
    zIndex: 10,
    elevation: 10,
    overflow: 'visible',
  },
  subOutOuter: {
    borderRadius: 10,
  },
  subOutBorderThin: {
    borderWidth: 1,
    borderColor: '#EF4444',
  },
  subOutBorderThick: {
    borderWidth: 2,
    borderColor: '#EF4444',
  },
  subOutBorderPulse: {
    borderWidth: 2,
    borderColor: '#EF4444',
    shadowColor: '#EF4444',
    shadowOffset: { width: 0, height: 0 },
    shadowOpacity: 0.6,
    shadowRadius: 4,
    elevation: 6,
  },
  subOutInfoIcon: {
    position: 'absolute',
    top: 2,
    right: 2,
    zIndex: 12,
    padding: 2,
  },
  // X butonu her zaman oyuncu kartının ÜSTÜNDE (kartın altında kalmamalı)
  
  // Player Card
  playerCard: {
    width: 64,
    height: 76,
    borderRadius: 8,
    overflow: 'hidden',
    borderWidth: 2,
    borderColor: 'rgba(100, 116, 139, 0.3)', // Default gray border
    ...Platform.select({
      ios: {
        shadowColor: '#000',
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.3,
        shadowRadius: 4,
      },
      android: {
        elevation: 4,
      },
      web: {
        boxShadow: '0px 2px 4px rgba(0, 0, 0, 0.3)',
      },
    }),
  },
  playerCardElite: {
    borderColor: '#C9A44C', // Gold border for elite players (85+)
    borderWidth: 2.5,
    ...Platform.select({
      ios: {
        shadowColor: '#C9A44C',
        shadowOffset: { width: 0, height: 0 },
        shadowOpacity: 0.6,
        shadowRadius: 8,
      },
      android: {
        elevation: 8,
      },
      web: {
        boxShadow: '0 0 12px rgba(201, 164, 76, 0.5), 0 2px 4px rgba(0, 0, 0, 0.3)',
      },
    }),
  },
  playerCardGK: {
    borderColor: '#3B82F6', // Blue border for goalkeepers
    borderWidth: 2,
  },
  playerCardGradient: {
    flex: 1,
    padding: 4,
    paddingBottom: 18, // ✅ Alt kısım absolute playerBottomRow için boşluk
    alignItems: 'center',
    justifyContent: 'flex-start',
    gap: 1,
  },
  removeButton: {
    position: 'absolute',
    top: -8,
    right: -8,
    zIndex: 9999,
    elevation: 9999,
    backgroundColor: '#EF4444',
    borderRadius: 13,
    width: 26,
    height: 26,
    alignItems: 'center',
    justifyContent: 'center',
    borderWidth: 2,
    borderColor: '#FFFFFF',
    ...Platform.select({
      ios: {
        shadowColor: '#000',
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.4,
        shadowRadius: 4,
      },
      android: {
        elevation: 999,
      },
      web: {
        boxShadow: '0 2px 8px rgba(239, 68, 68, 0.5)',
      },
    }),
  },
  replaceButton: {
    position: 'absolute',
    top: -8,
    left: -8,
    zIndex: 9999,
    elevation: 9999,
    backgroundColor: '#0F2A24',
    borderRadius: 13,
    width: 26,
    height: 26,
    alignItems: 'center',
    justifyContent: 'center',
    borderWidth: 2,
    borderColor: '#1FA2A6',
    ...Platform.select({
      ios: {
        shadowColor: '#000',
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.3,
        shadowRadius: 4,
      },
      android: {
        elevation: 999,
      },
      web: {
        boxShadow: '0 2px 8px rgba(31, 162, 166, 0.4)',
      },
    }),
  },
  jerseyNumberBadge: {
    width: 22,
    height: 22,
    borderRadius: 6,
    backgroundColor: '#1FA2A6', // ✅ Design System
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 1,
    borderWidth: 1,
    borderColor: 'rgba(255, 255, 255, 0.2)',
  },
  jerseyNumberText: {
    fontSize: 12,
    fontWeight: '900',
    color: '#FFFFFF',
  },
  ratingBadgeSmall: {
    width: 18,
    height: 14,
    borderRadius: 4,
    backgroundColor: 'rgba(201, 164, 76, 0.3)', // Gold tint for rating
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 2,
    borderWidth: 1,
    borderColor: 'rgba(201, 164, 76, 0.5)',
  },
  ratingTextSmall: {
    fontSize: 9,
    fontWeight: '700',
    color: '#C9A44C',
  },
  alertBadge: {
    position: 'absolute',
    top: 4,
    right: 4,
    width: 8,
    height: 8,
    borderRadius: 4,
    backgroundColor: 'rgba(239, 68, 68, 0.2)',
    alignItems: 'center',
    justifyContent: 'center',
    zIndex: 10,
  },
  alertDot: {
    width: 6,
    height: 6,
    borderRadius: 3,
    backgroundColor: '#EF4444',
    zIndex: 50,
  },
  playerName: {
    fontSize: 10,
    fontWeight: '600',
    color: '#FFFFFF',
    textAlign: 'center',
    marginTop: 2,
    paddingHorizontal: 2,
    letterSpacing: 0.3,
    flexShrink: 1,
    flexGrow: 0,
    maxHeight: 22,
  },
  playerPosition: {
    fontSize: 10,
    color: '#9CA3AF',
    textAlign: 'center',
    fontWeight: '600',
    marginTop: 1,
  },
  playerBottomRow: {
    position: 'absolute',
    bottom: 3,
    left: 4,
    right: 4,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  playerRatingBottom: {
    fontSize: 10,
    fontWeight: '700',
    color: '#C9A44C',
  },
  playerPositionBottom: {
    fontSize: 10,
    fontWeight: '600',
    color: '#9CA3AF',
  },
  formGlow: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    backgroundColor: 'rgba(245, 158, 11, 0.1)',
    borderRadius: 12,
  },
  formBadge: {
    position: 'absolute',
    bottom: 2,
    left: 2,
  },
  injuryBadge: {
    position: 'absolute',
    bottom: 2,
    right: 2,
  },
  
  // Empty Slot
  emptySlot: {
    width: 64,
    height: 76,
    borderRadius: 8,
    borderWidth: 2,
    borderColor: 'rgba(255, 255, 255, 0.3)',
    borderStyle: 'dashed',
    backgroundColor: 'rgba(255, 255, 255, 0.05)',
    alignItems: 'center',
    justifyContent: 'center',
  },
  emptySlotContent: {
    alignItems: 'center',
  },
  emptySlotText: {
    fontSize: 8,
    color: 'rgba(255, 255, 255, 0.7)',
    marginTop: 4,
  },
  
  // ✅ Kompakt toolbar - Tahmin sekmesi infoNote ile AYNI boyut
  bottomBar: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'flex-start',
    backgroundColor: '#1E3A3A',
    borderRadius: 12,
    paddingVertical: 8,
    paddingHorizontal: 8,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.3)',
    marginTop: 16, // ✅ Tahmin infoNote ile aynı marginTop
    marginBottom: 0, // ✅ Tahmin infoNote ile aynı
    minHeight: 50, // ✅ Minimum yükseklik, içerik daha fazla yer kaplayabilir
  },
  bottomBarLeft: {
    flex: 1,
    flexDirection: 'column',
    justifyContent: 'center',
  },
  // ✅ Tek satır: Tahminin/Gerçek 11/Topluluk + Atak/Defans (saha boyutu için kompakt)
  unifiedToolbarRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    flexWrap: 'wrap',
    gap: 8,
    paddingVertical: 4,
    marginBottom: 4,
  },
  // ✅ Karşılaştırma sekmeleri (unifiedToolbarRow içinde kullanıldığında kompakt)
  viewSourceRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 6,
    paddingVertical: 4,
    paddingHorizontal: 8,
    backgroundColor: 'rgba(15, 42, 36, 0.6)',
    borderRadius: 10,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.15)',
  },
  comparisonHint: {
    fontSize: 11,
    color: '#94A3B8',
    textAlign: 'center',
    paddingHorizontal: 12,
    paddingBottom: 6,
  },
  comparisonHint: {
    fontSize: 11,
    color: '#94A3B8',
    textAlign: 'center',
    paddingHorizontal: 12,
    paddingBottom: 6,
  },
  viewSourcePill: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
    paddingHorizontal: 10,
    paddingVertical: 5,
    borderRadius: 10,
    backgroundColor: 'rgba(100, 116, 139, 0.15)',
  },
  viewSourcePillActive: {
    backgroundColor: '#1FA2A6',
  },
  viewSourceText: {
    fontSize: 11,
    color: '#9CA3AF',
    fontWeight: '600',
  },
  viewSourceTextActive: {
    color: '#FFFFFF',
  },
  viewToggleRow: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
    marginBottom: 4,
  },
  viewTogglePill: {
    paddingHorizontal: 10,
    paddingVertical: 4,
    borderRadius: 12,
    backgroundColor: 'rgba(31, 162, 166, 0.15)',
  },
  viewTogglePillActive: {
    backgroundColor: '#1FA2A6',
  },
  viewToggleText: {
    fontSize: 12,
    color: '#1FA2A6',
    fontWeight: '500',
  },
  viewToggleTextActive: {
    color: '#FFFFFF',
  },
  changeFormationButton: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
  },
  changeFormationText: {
    fontSize: 12,
    color: '#1FA2A6', // ✅ Design System: Secondary (turkuaz)
    fontWeight: '500',
    flex: 1,
  },
  bottomBarRight: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
  },
  playerCount: {
    fontSize: 14,
    fontWeight: 'bold',
    color: '#E6E6E6', // ✅ Design System: DARK_MODE.foreground
  },
  completeButton: {
    borderRadius: 8,
    overflow: 'hidden',
  },
  completeButtonDisabled: {
    opacity: 0.5,
  },
  completeButtonGradient: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
    paddingHorizontal: 12,
    paddingVertical: 8,
  },
  completeButtonText: {
    fontSize: 13,
    fontWeight: '600',
    color: '#FFFFFF',
  },
  // ✅ Kilit Butonları
  lockButton: {
    width: 36,
    height: 36,
    borderRadius: 8,
    backgroundColor: 'rgba(239, 68, 68, 0.2)',
    alignItems: 'center',
    justifyContent: 'center',
    borderWidth: 1.5,
    borderColor: '#EF4444',
    marginRight: 8,
  },
  lockButtonOpen: {
    width: 36,
    height: 36,
    borderRadius: 8,
    backgroundColor: 'rgba(16, 185, 129, 0.2)',
    alignItems: 'center',
    justifyContent: 'center',
    borderWidth: 1.5,
    borderColor: '#10B981',
    marginRight: 8,
  },
  
  // ✅ Simetrik Toolbar: [Formasyon] | 🔓 | [Tamamla] - Kilit kesinlikle ortada
  symmetricToolbar: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center', // ✅ Ortala
    flex: 1,
    position: 'relative', // ✅ Kilit için absolute container
  },
  // 2 Satırlı Formasyon Butonu - SOL
  dualLineFormationButton: {
    flexDirection: 'column',
    paddingHorizontal: 10,
    paddingVertical: 4,
    borderRadius: 8,
    backgroundColor: 'rgba(31, 162, 166, 0.1)',
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.3)',
    minWidth: 100,
    maxWidth: 130,
    gap: 2,
    position: 'absolute', // ✅ Sol tarafa sabitle
    left: 0,
  },
  formationLine: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
    paddingVertical: 1,
  },
  formationLineActive: {
    // Aktif satır için ek stil (isteğe bağlı)
  },
  formationLineText: {
    fontSize: 11,
    color: '#9CA3AF',
    fontWeight: '500',
    flex: 1,
  },
  formationLineTextActive: {
    color: '#FFFFFF',
    fontWeight: '600',
  },
  // Kilit Butonu - KESİNLİKLE ORTADA
  lockButtonCenter: {
    width: 36,
    height: 36,
    borderRadius: 18,
    alignItems: 'center',
    justifyContent: 'center',
    // Ortada kalması için position yok - normal flow'da
  },
  lockButtonCenterLocked: {
    backgroundColor: 'rgba(239, 68, 68, 0.2)',
    borderWidth: 1.5,
    borderColor: '#EF4444',
  },
  lockButtonCenterOpen: {
    backgroundColor: 'rgba(16, 185, 129, 0.2)',
    borderWidth: 1.5,
    borderColor: '#10B981',
  },
  lockButtonCenterDisabled: {
    backgroundColor: 'rgba(100, 116, 139, 0.15)',
    borderWidth: 1,
    borderColor: 'rgba(100, 116, 139, 0.3)',
  },
  // Tamamla Butonu - SAĞ tarafa sabitle
  completeButtonSymmetric: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 6,
    paddingHorizontal: 10,
    paddingVertical: 8,
    borderRadius: 8,
    backgroundColor: '#1FA2A6',
    minWidth: 100,
    maxWidth: 130,
    position: 'absolute', // ✅ Sağ tarafa sabitle
    right: 0,
  },
  completeButtonSymmetricDisabled: {
    backgroundColor: '#374151',
    opacity: 0.5,
  },
  completeButtonSymmetricText: {
    fontSize: 12,
    fontWeight: '600',
    color: '#FFFFFF',
  },
  
  // Modal - Design System uyumlu
  modalOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.8)',
    justifyContent: 'flex-end',
    alignItems: 'center', // ✅ Yatay ortala
    paddingBottom: 0, // ✅ Alt boşluk yok - zemine yapışık
  },
  modalContent: {
    backgroundColor: '#1A2E2A', // ✅ Design System: Slightly lighter than primary for better contrast
    borderTopLeftRadius: 24,
    borderTopRightRadius: 24,
    borderBottomLeftRadius: 0, // ✅ Alt köşeler düz - zemine oturur
    borderBottomRightRadius: 0,
    maxHeight: height * 0.85, // ✅ Scroll alanı için yeterli alan bırak
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.2)',
    borderBottomWidth: 0, // ✅ Alt border yok - zemine yapışık
    width: '100%', // ✅ Tam genişlik (mobil)
    maxWidth: 380, // ✅ STANDART: 380px genişlik (tüm popup'lar için standart)
    marginBottom: 0, // ✅ Alt margin yok - zemine yapışık
  },
  modalHeader: {
    padding: 12, // ✅ Daha az padding - kompakt header
    paddingLeft: 16,
    paddingRight: 56,
    borderBottomWidth: 1,
    borderBottomColor: 'rgba(31, 162, 166, 0.15)',
  },
  modalHeaderContent: {
    flex: 1,
  },
  modalTitle: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#F8FAFB',
  },
  modalSubtitle: {
    fontSize: 12,
    color: '#64748B',
    marginTop: 4,
  },
  formationModalTabs: {
    flexDirection: 'row',
    paddingHorizontal: 8, // ✅ Daha az yatay padding - geniş görünüm
    paddingVertical: 10,
    gap: 8,
    borderBottomWidth: 1,
    borderBottomColor: 'rgba(31, 162, 166, 0.15)',
  },
  formationModalTab: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    paddingVertical: 10,
    borderRadius: 10,
    backgroundColor: 'rgba(31, 162, 166, 0.1)',
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.25)',
  },
  formationModalTabActive: {
    backgroundColor: '#1FA2A6',
    borderColor: '#1FA2A6',
  },
  formationModalTabDisabled: {
    opacity: 0.6,
  },
  formationModalTabText: {
    fontSize: 14,
    fontWeight: '600',
    color: '#1FA2A6',
  },
  formationModalTabTextActive: {
    color: '#FFFFFF',
  },
  formationModalTabTextDisabled: {
    color: '#6B7280',
  },
  modalCloseButton: {
    width: 36,
    height: 36,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'rgba(71, 85, 105, 0.6)',
    borderRadius: 18,
    borderWidth: 1,
    borderColor: 'rgba(148, 163, 184, 0.3)',
  },
  defenseModeInfo: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    backgroundColor: 'rgba(59, 130, 246, 0.15)',
    paddingHorizontal: 12,
    paddingVertical: 8,
    borderRadius: 8,
    marginBottom: 12,
    borderWidth: 1,
    borderColor: 'rgba(59, 130, 246, 0.3)',
  },
  defenseModeInfoText: {
    fontSize: 12,
    color: '#93C5FD',
    flex: 1,
  },
  // ✅ Atak modu bilgi kutusu
  attackModeInfo: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    backgroundColor: 'rgba(31, 162, 166, 0.15)',
    paddingHorizontal: 12,
    paddingVertical: 8,
    borderRadius: 8,
    marginBottom: 12,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.3)',
  },
  attackModeInfoText: {
    fontSize: 12,
    color: '#5EEAD4',
    flex: 1,
  },
  modalCloseButtonAbsolute: {
    position: 'absolute',
    top: 12,
    right: 12,
    width: 36,
    height: 36,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'rgba(31, 162, 166, 0.15)', // ✅ Design System
    borderRadius: 18,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.3)',
    zIndex: 1000,
  },
  modalTabs: {
    flexDirection: 'row',
    paddingHorizontal: 12,
    paddingVertical: 10,
    gap: 8,
  },
  modalTab: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 6,
    paddingVertical: 10,
    borderRadius: 10,
    backgroundColor: 'rgba(31, 162, 166, 0.08)', // ✅ Design System
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.15)',
  },
  modalTabActive: {
    backgroundColor: 'rgba(31, 162, 166, 0.25)', // ✅ Aktif tab
    borderWidth: 1,
    borderColor: '#1FA2A6',
  },
  modalTabText: {
    fontSize: 14,
    fontWeight: '600',
    color: '#64748B',
  },
  modalTabTextActive: {
    color: '#1FA2A6',
    fontWeight: '700',
  },
  modalScroll: {
    flex: 1,
  },
  
  // Formation Grid - 3 Columns COMPACT - Eşit boşluklar
  formationGridContainer: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    paddingHorizontal: 4, // ✅ Minimal sağ-sol boşluk - daha geniş görünüm
    paddingTop: 4,
    paddingBottom: Platform.OS === 'web' ? 8 : 24, // ✅ Web: minimal, iOS: home indicator için boşluk
    justifyContent: 'space-between', // ✅ Eşit dağılım
  },
  formationGridItem: {
    width: '32%', // 3 columns
    marginBottom: 6, // ✅ Alt boşluk
    marginBottom: 2,
  },
  formationCard: {
    backgroundColor: '#1E3A3A', // ✅ Design System: Standard card background (not blue)
    borderRadius: 10,
    padding: 6,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.3)', // ✅ Turkuaz border
    alignItems: 'center',
    justifyContent: 'center',
    minHeight: 52, // ✅ Daha kompakt
    position: 'relative',
  },
  formationCardId: {
    fontSize: 14,
    fontWeight: 'bold',
    color: '#FFFFFF',
    marginBottom: 2,
  },
  formationCardSubtitle: {
    fontSize: 9,
    color: '#64748B',
    textAlign: 'center',
  },
  formationCardSelected: {
    borderColor: '#1FA2A6',
    borderWidth: 2,
    backgroundColor: 'rgba(31, 162, 166, 0.15)',
  },
  // ✅ Seçili formasyon (mevcut atak/defans formasyonu)
  formationCardCurrentlySelected: {
    borderColor: '#10B981',
    borderWidth: 2,
    backgroundColor: 'rgba(16, 185, 129, 0.15)',
  },
  formationSelectedBadge: {
    position: 'absolute',
    top: -6,
    right: -6,
    backgroundColor: '#0F2A24',
    borderRadius: 10,
    padding: 2,
    zIndex: 10,
  },
  
  // ✅ Formation Preview Modal - Ayrı popup
  formationPreviewOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.85)',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
  },
  formationPreviewModal: {
    backgroundColor: '#1A3D37', // ✅ Daha açık ton - belirgin
    borderRadius: 16,
    padding: 20,
    width: '100%',
    maxWidth: 380,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.4)',
    ...Platform.select({
      ios: {
        shadowColor: '#1FA2A6',
        shadowOffset: { width: 0, height: 4 },
        shadowOpacity: 0.3,
        shadowRadius: 12,
      },
      android: {
        elevation: 10,
      },
      web: {
        boxShadow: '0 4px 24px rgba(31, 162, 166, 0.25)',
      },
    }),
  },
  formationPreviewHeader: {
    marginBottom: 12,
  },
  formationPreviewTitleRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginBottom: 8,
  },
  formationPreviewName: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#FFFFFF',
    flex: 1,
  },
  formationPreviewCloseBtn: {
    width: 32,
    height: 32,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'rgba(148, 163, 184, 0.15)',
    borderRadius: 16,
  },
  formationPreviewTypeBadge: {
    paddingHorizontal: 10,
    paddingVertical: 5,
    borderRadius: 8,
    alignSelf: 'flex-start',
  },
  formationPreviewTypeText: {
    fontSize: 12,
    fontWeight: '600',
  },
  formationPreviewContextHint: {
    fontSize: 12,
    color: '#3B82F6',
    marginTop: 8,
    fontWeight: '500',
  },
  formationPreviewDesc: {
    fontSize: 14,
    color: '#E2E8F0',
    lineHeight: 20,
    marginBottom: 16,
  },
  formationPreviewSection: {
    marginBottom: 12,
  },
  formationPreviewSectionTitle: {
    fontSize: 13,
    fontWeight: '700',
    color: '#FFFFFF',
    marginBottom: 8,
  },
  formationPreviewListItem: {
    flexDirection: 'row',
    alignItems: 'flex-start',
    marginBottom: 4,
    gap: 8,
  },
  formationPreviewBullet: {
    width: 6,
    height: 6,
    borderRadius: 3,
    backgroundColor: '#1FA2A6',
    marginTop: 6,
  },
  formationPreviewListText: {
    fontSize: 12,
    color: '#CBD5E1',
    flex: 1,
    lineHeight: 18,
  },
  formationPreviewBestFor: {
    flexDirection: 'row',
    alignItems: 'flex-start',
    gap: 8,
    backgroundColor: 'rgba(245, 158, 11, 0.12)',
    padding: 12,
    borderRadius: 10,
    marginBottom: 16,
    borderWidth: 1,
    borderColor: 'rgba(245, 158, 11, 0.25)',
  },
  formationPreviewBestForLabel: {
    fontWeight: '700',
    color: '#F59E0B',
  },
  formationPreviewBestForText: {
    fontSize: 12,
    color: '#FDE68A',
    flex: 1,
    lineHeight: 18,
  },
  // ✅ Formasyon Popülerlik Stilleri - DİKKAT ÇEKİCİ TASARIM
  formationPopularitySection: {
    backgroundColor: 'rgba(239, 68, 68, 0.15)', // Kırmızımsı arka plan
    padding: 14,
    borderRadius: 12,
    marginBottom: 16,
    borderWidth: 2,
    borderColor: 'rgba(249, 115, 22, 0.6)', // Turuncu border
    ...Platform.select({
      ios: {
        shadowColor: '#F97316',
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.3,
        shadowRadius: 6,
      },
      android: {
        elevation: 4,
      },
    }),
  },
  formationPopularityHeader: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    marginBottom: 10,
    backgroundColor: 'rgba(249, 115, 22, 0.2)', // Turuncu header arka planı
    padding: 8,
    borderRadius: 8,
    marginHorizontal: -6,
    marginTop: -6,
  },
  formationPopularitySectionTitle: {
    fontSize: 14,
    fontWeight: '800',
    color: '#F97316', // Turuncu başlık
    textTransform: 'uppercase',
    letterSpacing: 0.5,
  },
  formationPopularityLoading: {
    fontSize: 12,
    color: '#9CA3AF',
    textAlign: 'center',
    padding: 8,
  },
  formationPopularityContent: {
    gap: 10,
  },
  formationPopularityText: {
    fontSize: 13,
    color: '#F1F5F9', // Daha parlak text
    lineHeight: 20,
    fontWeight: '500',
  },
  formationPopularityHighlight: {
    fontWeight: '800',
    color: '#F97316', // Turuncu vurgu
    fontSize: 16,
  },
  formationPopularityBarBg: {
    height: 8, // Daha kalın bar
    backgroundColor: 'rgba(100, 116, 139, 0.4)',
    borderRadius: 4,
    overflow: 'hidden',
  },
  formationPopularityBarFill: {
    height: '100%',
    borderRadius: 3,
  },
  formationPreviewButtons: {
    flexDirection: 'row',
    gap: 12,
  },
  formationPreviewCancelBtn: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    paddingVertical: 12,
    borderRadius: 10,
    backgroundColor: 'rgba(100, 116, 139, 0.2)',
    borderWidth: 1,
    borderColor: 'rgba(100, 116, 139, 0.4)',
  },
  formationPreviewCancelText: {
    fontSize: 14,
    fontWeight: '600',
    color: '#94A3B8',
  },
  formationPreviewSelectBtn: {
    flex: 2,
    borderRadius: 10,
    overflow: 'hidden',
    backgroundColor: '#1FA2A6', // ✅ Opak turkuaz – açıklama popup’ı ile aynı, mavi/şeffaf görünmesin
  },
  formationPreviewSelectGradient: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    paddingVertical: 12,
    opacity: 1,
  },
  formationPreviewSelectText: {
    fontSize: 14,
    fontWeight: '700',
    color: '#FFFFFF',
  },
  
  // ✅ Defense Confirmation Modal Styles
  defenseConfirmOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.85)',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 24,
  },
  defenseConfirmModal: {
    backgroundColor: '#1A3D37',
    borderRadius: 20,
    padding: 24,
    width: '100%',
    maxWidth: 380, // ✅ STANDART: 380px genişlik
    alignItems: 'center',
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.3)',
    ...Platform.select({
      ios: {
        shadowColor: '#1FA2A6',
        shadowOffset: { width: 0, height: 8 },
        shadowOpacity: 0.3,
        shadowRadius: 16,
      },
      android: {
        elevation: 12,
      },
      web: {
        boxShadow: '0 8px 32px rgba(31, 162, 166, 0.25)',
      },
    }),
  },
  defenseConfirmIcon: {
    width: 80,
    height: 80,
    borderRadius: 40,
    backgroundColor: 'rgba(31, 162, 166, 0.15)',
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 16,
  },
  defenseConfirmTitle: {
    fontSize: 22,
    fontWeight: 'bold',
    color: '#FFFFFF',
    marginBottom: 12,
    textAlign: 'center',
  },
  defenseConfirmDesc: {
    fontSize: 14,
    color: '#CBD5E1',
    textAlign: 'center',
    lineHeight: 22,
    marginBottom: 16,
  },
  defenseConfirmInfo: {
    flexDirection: 'row',
    alignItems: 'flex-start',
    gap: 10,
    backgroundColor: 'rgba(245, 158, 11, 0.12)',
    padding: 12,
    borderRadius: 10,
    marginBottom: 20,
    borderWidth: 1,
    borderColor: 'rgba(245, 158, 11, 0.25)',
  },
  defenseConfirmInfoText: {
    fontSize: 12,
    color: '#FDE68A',
    flex: 1,
    lineHeight: 18,
  },
  defenseConfirmButtons: {
    flexDirection: 'row',
    gap: 12,
    width: '100%',
  },
  defenseConfirmNoBtn: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 6,
    paddingVertical: 14,
    borderRadius: 12,
    backgroundColor: 'rgba(100, 116, 139, 0.2)',
    borderWidth: 1,
    borderColor: 'rgba(100, 116, 139, 0.4)',
  },
  defenseConfirmNoText: {
    fontSize: 13,
    fontWeight: '600',
    color: '#94A3B8',
  },
  defenseConfirmYesBtn: {
    flex: 1,
    borderRadius: 12,
    overflow: 'hidden',
  },
  defenseConfirmYesGradient: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    paddingVertical: 14,
  },
  defenseConfirmYesText: {
    fontSize: 14,
    fontWeight: '700',
    color: '#FFFFFF',
  },

  // ✅ Kadro Tamamlandı popup (ortada, standart popup stili)
  squadCompleteOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.85)',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 24,
  },
  squadCompleteModal: {
    borderRadius: 20,
    overflow: 'hidden',
    width: '100%',
    maxWidth: 380,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.3)',
    ...Platform.select({
      ios: { shadowColor: '#1FA2A6', shadowOffset: { width: 0, height: 8 }, shadowOpacity: 0.3, shadowRadius: 16 },
      android: { elevation: 12 },
      web: { boxShadow: '0 8px 32px rgba(31, 162, 166, 0.25)' },
    }),
  },
  squadCompleteGradient: {
    paddingVertical: 28,
    paddingHorizontal: 24,
    alignItems: 'center',
  },
  squadCompleteIcon: {
    width: 72,
    height: 72,
    borderRadius: 36,
    backgroundColor: 'rgba(31, 162, 166, 0.15)',
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 14,
  },
  squadCompleteTitle: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#FFFFFF',
    marginBottom: 10,
    textAlign: 'center',
  },
  squadCompleteFormation: {
    fontSize: 15,
    color: '#CBD5E1',
    textAlign: 'center',
    marginBottom: 14,
    fontWeight: '600',
  },
  squadCompleteInfo: {
    flexDirection: 'row',
    alignItems: 'flex-start',
    gap: 10,
    backgroundColor: 'rgba(245, 158, 11, 0.12)',
    padding: 12,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: 'rgba(245, 158, 11, 0.25)',
  },
  squadCompleteInfoText: {
    fontSize: 12,
    color: '#FDE68A',
    flex: 1,
    lineHeight: 18,
  },

  // Formation Detail Modal
  formationDetailOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.8)',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 20,
  },
  
  // Player Item
  playersList: {
    paddingBottom: 20,
  },
  playerItem: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    padding: 16,
    borderBottomWidth: 1,
    borderBottomColor: 'rgba(255, 255, 255, 0.1)',
  },
  playerItemLeft: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
  },
  playerItemRating: {
    width: 40,
    height: 40,
    borderRadius: 8,
    alignItems: 'center',
    justifyContent: 'center',
  },
  playerItemRatingText: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#FFFFFF',
  },
  // ✅ Jersey Number Badge - Circular
  playerItemJerseyNumber: {
    width: 40,
    height: 40,
    borderRadius: 20, // Fully circular
    alignItems: 'center',
    justifyContent: 'center',
    marginRight: 12,
  },
  playerItemJerseyNumberText: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#FFFFFF',
  },
  playerItemInfo: {
    flex: 1,
  },
  playerItemNameRow: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  playerItemName: {
    fontSize: 15,
    fontWeight: '600',
    color: '#FFFFFF',
  },
  playerItemBottomRow: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 2,
    gap: 8,
  },
  playerItemRatingBottom: {
    fontSize: 13,
    fontWeight: '700',
    color: '#C9A44C', // Gold color for rating
  },
  playerItemPosition: {
    fontSize: 13,
    color: '#9CA3AF',
  },
  playerItemAddBtn: {
    padding: 4,
  },
  playerItemLocked: {
    opacity: 0.5,
    backgroundColor: 'rgba(75, 85, 99, 0.1)',
  },
  playerItemNameLocked: {
    color: '#6B7280',
  },
  playerItemPositionLocked: {
    color: '#4B5563',
  },
  playerItemLockedIcon: {
    padding: 4,
    width: 36,
    alignItems: 'center',
    justifyContent: 'center',
  },
  
  // ✅ Player Modal Close Button
  playerModalCloseBtn: {
    width: 36,
    height: 36,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'rgba(148, 163, 184, 0.15)',
    borderRadius: 18,
    borderWidth: 1,
    borderColor: 'rgba(148, 163, 184, 0.3)',
  },
  playerModalHeader: {
    flexDirection: 'row',
    alignItems: 'flex-start',
    justifyContent: 'space-between',
    padding: 16,
    borderBottomWidth: 1,
    borderBottomColor: 'rgba(31, 162, 166, 0.15)',
  },
  playerModalHeaderText: {
    flex: 1,
    marginRight: 12,
  },
  playerModalCloseBtnTopRight: {
    width: 36,
    height: 36,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'rgba(148, 163, 184, 0.15)',
    borderRadius: 18,
    borderWidth: 1,
    borderColor: 'rgba(148, 163, 184, 0.3)',
  },

  // ✅ Player Card Modal - FIFA Style
  playerCardOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.9)',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
  },
  playerCardContainer: {
    width: '100%',
    maxWidth: 340,
    borderRadius: 24,
    overflow: 'hidden',
    backgroundColor: '#0F2027',
    ...Platform.select({
      ios: {
        shadowColor: '#000',
        shadowOffset: { width: 0, height: 10 },
        shadowOpacity: 0.5,
        shadowRadius: 20,
      },
      android: {
        elevation: 20,
      },
    }),
  },
  playerCardHeader: {
    paddingTop: 40,
    paddingBottom: 24,
    paddingHorizontal: 20,
    alignItems: 'center',
    position: 'relative',
  },
  playerCardCloseBtn: {
    position: 'absolute',
    top: 12,
    right: 12,
    width: 32,
    height: 32,
    borderRadius: 16,
    backgroundColor: 'rgba(255, 255, 255, 0.2)',
    alignItems: 'center',
    justifyContent: 'center',
    zIndex: 10,
  },
  playerCardRatingCircle: {
    width: 72,
    height: 72,
    borderRadius: 36,
    backgroundColor: 'rgba(255, 255, 255, 0.25)',
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 12,
    borderWidth: 3,
    borderColor: 'rgba(255, 255, 255, 0.5)',
  },
  playerCardRatingText: {
    fontSize: 32,
    fontWeight: 'bold',
    color: '#FFFFFF',
  },
  playerCardName: {
    fontSize: 22,
    fontWeight: 'bold',
    color: '#FFFFFF',
    textAlign: 'center',
    marginBottom: 8,
    ...Platform.select({
      web: { textShadow: '0px 1px 2px rgba(0,0,0,0.3)' },
      default: {
        textShadowColor: 'rgba(0,0,0,0.3)',
        textShadowOffset: { width: 0, height: 1 },
        textShadowRadius: 2,
      },
    }),
  },
  playerCardPositionRow: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 10,
    marginBottom: 6,
  },
  playerCardPositionBadge: {
    backgroundColor: 'rgba(255, 255, 255, 0.3)',
    paddingHorizontal: 12,
    paddingVertical: 4,
    borderRadius: 12,
  },
  playerCardPositionText: {
    fontSize: 13,
    fontWeight: '700',
    color: '#FFFFFF',
  },
  playerCardTeam: {
    fontSize: 14,
    color: 'rgba(255, 255, 255, 0.9)',
  },
  playerCardInfoRow: {
    marginTop: 4,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    width: '100%',
  },
  playerCardInfoText: {
    fontSize: 12,
    color: 'rgba(255, 255, 255, 0.7)',
  },
  playerCardRatingBottom: {
    fontSize: 14,
    fontWeight: '700',
    color: '#C9A44C', // Gold color for rating
  },
  playerCardBody: {
    backgroundColor: '#0F2027',
    padding: 20,
  },
  playerCardStatusRow: {
    flexDirection: 'row',
    gap: 12,
    marginBottom: 20,
  },
  playerCardStatusItem: {
    flex: 1,
    alignItems: 'center',
    paddingVertical: 12,
    borderRadius: 14,
    backgroundColor: 'rgba(255, 255, 255, 0.05)',
    borderWidth: 2,
  },
  playerCardStatusLabel: {
    fontSize: 10,
    color: '#94A3B8',
    marginTop: 4,
    textTransform: 'uppercase',
  },
  playerCardStatusValue: {
    fontSize: 14,
    fontWeight: '700',
    marginTop: 2,
  },
  playerCardStatsGrid: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'space-between',
    marginBottom: 16,
  },
  playerCardStatItem: {
    width: '30%',
    alignItems: 'center',
    marginBottom: 16,
  },
  playerCardStatCircle: {
    width: 52,
    height: 52,
    borderRadius: 26,
    borderWidth: 3,
    backgroundColor: 'rgba(255, 255, 255, 0.05)',
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 6,
  },
  playerCardStatValue: {
    fontSize: 18,
    fontWeight: 'bold',
  },
  playerCardStatLabel: {
    fontSize: 9,
    color: '#64748B',
    fontWeight: '600',
    textAlign: 'center',
  },
  // ✅ Oyuncu Preview - Topluluk Tercihleri Stilleri
  playerPreviewCommunitySection: {
    backgroundColor: 'rgba(249, 115, 22, 0.12)',
    padding: 12,
    borderRadius: 12,
    marginBottom: 12,
    borderWidth: 1.5,
    minHeight: 130, // ✅ Sabit minimum yükseklik - 3 satır + header için yeterli
    borderColor: 'rgba(249, 115, 22, 0.4)',
  },
  playerPreviewCommunityHeader: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
    marginBottom: 10,
  },
  playerPreviewCommunityTitle: {
    fontSize: 12,
    fontWeight: '700',
    color: '#F97316',
    textTransform: 'uppercase',
    letterSpacing: 0.3,
  },
  playerPreviewCommunityLoading: {
    fontSize: 11,
    color: '#9CA3AF',
    textAlign: 'center',
    paddingVertical: 36, // ✅ Loading durumunda da aynı yüksekliği koru - içerikle eşit
  },
  playerPreviewCommunityContent: {
    gap: 8,
    minHeight: 85, // ✅ Sabit minimum yükseklik - 3 satır için
  },
  playerPreviewCommunityRow: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
  },
  playerPreviewCommunityText: {
    fontSize: 11,
    color: '#E2E8F0',
    flex: 1,
    lineHeight: 16,
  },
  playerPreviewCommunityHighlight: {
    fontWeight: '700',
    color: '#F97316',
    fontSize: 12,
  },
  
  playerCardAssignInfo: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    backgroundColor: 'rgba(31, 162, 166, 0.15)',
    paddingVertical: 10,
    paddingHorizontal: 16,
    borderRadius: 12,
    marginBottom: 16,
  },
  playerCardAssignText: {
    fontSize: 13,
    color: '#94A3B8',
  },
  playerCardActions: {
    flexDirection: 'row',
    gap: 12,
  },
  playerCardCancelBtn: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    paddingVertical: 14,
    borderRadius: 14,
    backgroundColor: 'rgba(100, 116, 139, 0.15)',
    borderWidth: 1,
    borderColor: 'rgba(100, 116, 139, 0.3)',
  },
  playerCardCancelText: {
    fontSize: 14,
    fontWeight: '600',
    color: '#94A3B8',
  },
  playerCardAddBtn: {
    flex: 2,
    borderRadius: 14,
    overflow: 'hidden',
  },
  playerCardAddGradient: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    paddingVertical: 14,
  },
  playerCardAddText: {
    fontSize: 14,
    fontWeight: '700',
    color: '#FFFFFF',
  },
  playerCardWarning: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    backgroundColor: 'rgba(245, 158, 11, 0.1)',
    padding: 12,
    borderRadius: 12,
    marginTop: 12,
    borderWidth: 1,
    borderColor: 'rgba(245, 158, 11, 0.2)',
  },
  playerCardWarningText: {
    fontSize: 11,
    color: '#FDE68A',
    flex: 1,
  },
  
  // Player Detail Modal - STANDART POPUP STİLİ (alttan açılır, zemine yapışık, SCROLL YOK)
  playerDetailModal: {
    backgroundColor: '#1E3A3A',
    borderTopLeftRadius: 24,
    borderTopRightRadius: 24,
    borderBottomLeftRadius: 0,
    borderBottomRightRadius: 0,
    overflow: 'hidden', // ✅ Köşelerin yuvarlaklığını zorla
    width: '92%',
    maxWidth: 380, // ✅ STANDART: 380px genişlik
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.2)',
    borderBottomWidth: 0,
    marginBottom: 0,
    marginHorizontal: '4%',
    paddingBottom: Platform.OS === 'web' ? 16 : 34,
  },
  playerDetailHeader: {
    padding: 20,
    paddingTop: 16,
  },
  playerDetailHeaderContent: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 16,
    marginTop: 8,
  },
  playerDetailRating: {
    width: 60,
    height: 60,
    borderRadius: 12,
    alignItems: 'center',
    justifyContent: 'center',
  },
  playerDetailRatingText: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#FFFFFF',
  },
  playerDetailInfo: {
    flex: 1,
  },
  playerDetailName: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#FFFFFF',
  },
  playerDetailMeta: {
    fontSize: 13,
    color: '#9CA3AF',
    marginTop: 4,
  },
  playerDetailInfoRow: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    width: '100%',
    marginTop: 4,
  },
  playerDetailInfoText: {
    fontSize: 12,
    color: 'rgba(255, 255, 255, 0.7)',
  },
  playerDetailRatingHighlight: {
    fontSize: 14,
    fontWeight: '700',
    color: '#C9A44C',
  },
  playerDetailBadges: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    marginTop: 8,
  },
  nationalityBadge: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
    backgroundColor: 'rgba(5, 150, 105, 0.2)',
    paddingHorizontal: 8,
    paddingVertical: 4,
    borderRadius: 6,
  },
  nationalityText: {
    fontSize: 11,
    color: '#1FA2A6',
    fontWeight: '600',
  },
  formBadgeLarge: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
    backgroundColor: 'rgba(245, 158, 11, 0.2)',
    paddingHorizontal: 8,
    paddingVertical: 4,
    borderRadius: 6,
  },
  formTextLarge: {
    fontSize: 11,
    color: '#F59E0B',
    fontWeight: '600',
  },
  injuryBadgeLarge: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
    backgroundColor: 'rgba(239, 68, 68, 0.2)',
    paddingHorizontal: 8,
    paddingVertical: 4,
    borderRadius: 6,
  },
  injuryTextLarge: {
    fontSize: 11,
    color: '#EF4444',
    fontWeight: '600',
  },
  // ✅ Elit oyuncu badge
  eliteBadgeLarge: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
    backgroundColor: 'rgba(201, 164, 76, 0.2)',
    paddingHorizontal: 8,
    paddingVertical: 4,
    borderRadius: 6,
    borderWidth: 1,
    borderColor: 'rgba(201, 164, 76, 0.4)',
  },
  eliteTextLarge: {
    fontSize: 11,
    color: '#C9A44C',
    fontWeight: '700',
  },
  // ✅ Elit oyuncu header stili
  playerDetailHeaderElite: {
    borderWidth: 2,
    borderColor: '#C9A44C',
    borderTopLeftRadius: 24,
    borderTopRightRadius: 24,
  },
  playerDetailContent: {
    flex: 1,
    padding: 20,
  },
  // ✅ YENI: Scroll olmayan kompakt içerik alanı
  playerDetailContentNoScroll: {
    paddingHorizontal: 16,
    paddingTop: 12,
    paddingBottom: 8,
  },
  playerDetailSectionTitle: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#FFFFFF',
    marginBottom: 16,
  },
  // ✅ YENI: Kompakt başlık
  playerDetailSectionTitleCompact: {
    fontSize: 13,
    fontWeight: '700',
    color: '#FFFFFF',
    marginBottom: 8,
  },
  statsGrid: {
    gap: 12,
  },
  // ✅ YENI: 1 sütun 6 satır dikey layout - scroll yok, tek sayfada
  statsGridCompact: {
    flexDirection: 'column', // ✅ Dikey layout
    gap: 6,
    marginBottom: 12,
  },
  statItemCompact: {
    width: '100%', // ✅ Tam genişlik - tek sütun
    backgroundColor: 'rgba(15, 23, 42, 0.5)',
    paddingHorizontal: 12,
    paddingVertical: 8,
    borderRadius: 8,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.15)',
    flexDirection: 'row', // ✅ Yatay içerik: Label | Bar | Value
    alignItems: 'center',
    gap: 10,
  },
  // statItemCompactHeader artık kullanılmıyor - dikey layout'ta label/bar/value ayrı
  statLabelCompact: {
    fontSize: 11,
    fontWeight: '600',
    color: '#9CA3AF',
    width: 55, // ✅ Sabit genişlik - label için
  },
  statValueCompact: {
    fontSize: 14,
    fontWeight: 'bold',
    width: 28, // ✅ Sabit genişlik - value için
    textAlign: 'right',
  },
  statBarBackgroundCompact: {
    flex: 1, // ✅ Kalan alanı doldur
    height: 6,
    backgroundColor: 'rgba(100, 116, 139, 0.3)',
    borderRadius: 3,
    overflow: 'hidden',
  },
  statBarFillCompact: {
    height: '100%',
    borderRadius: 3,
  },
  statItem: {
    gap: 6,
  },
  statLabel: {
    fontSize: 13,
    fontWeight: '600',
    color: '#FFFFFF',
  },
  statBarContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
  },
  statBarBackground: {
    flex: 1,
    height: 8,
    backgroundColor: 'rgba(100, 116, 139, 0.3)',
    borderRadius: 4,
    overflow: 'hidden',
  },
  statBarFill: {
    height: '100%',
    borderRadius: 4,
  },
  statValue: {
    fontSize: 14,
    fontWeight: 'bold',
    width: 30,
    textAlign: 'right',
  },
  additionalInfo: {
    flexDirection: 'row',
    gap: 12,
    marginTop: 24,
  },
  // ✅ YENI: Kompakt info chips
  additionalInfoCompact: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: 6,
    marginBottom: 12,
  },
  infoChip: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
    backgroundColor: 'rgba(31, 162, 166, 0.1)',
    paddingHorizontal: 8,
    paddingVertical: 4,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.2)',
  },
  infoChipText: {
    fontSize: 11,
    fontWeight: '600',
    color: '#FFFFFF',
  },
  infoCard: {
    flex: 1,
    backgroundColor: 'rgba(15, 23, 42, 0.5)',
    padding: 12,
    borderRadius: 12,
    alignItems: 'center',
    gap: 6,
    borderWidth: 1,
    borderColor: 'rgba(5, 150, 105, 0.3)',
  },
  infoCardLabel: {
    fontSize: 11,
    color: '#9CA3AF',
  },
  infoCardValue: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#FFFFFF',
  },
  // ✅ YENI: Kompakt topluluk istatistikleri
  communityStatsSectionCompact: {
    marginTop: -5,
    paddingTop: 8,
    borderTopWidth: 1,
    borderTopColor: 'rgba(255, 255, 255, 0.1)',
  },
  communityStatsCompactRow: {
    flexDirection: 'row',
    gap: 8,
  },
  communityStatChip: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
    backgroundColor: 'rgba(15, 23, 42, 0.5)',
    padding: 10,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.2)',
  },
  communityStatChipLabel: {
    fontSize: 11,
    color: '#9CA3AF',
    flex: 1,
  },
  communityStatChipValue: {
    fontSize: 14,
    fontWeight: 'bold',
  },
  communityStatsCompactLoading: {
    alignItems: 'center',
    paddingVertical: 12,
  },
  communityStatsCompactLocked: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 6,
    paddingVertical: 10,
    backgroundColor: 'rgba(100, 116, 139, 0.1)',
    borderRadius: 8,
  },
  communityStatsCompactLockedText: {
    fontSize: 11,
    color: '#9CA3AF',
  },
  playerDetailActions: {
    marginTop: -5,
    padding: 16,
    paddingBottom: 8, // ✅ Modal paddingBottom zaten var
    borderTopWidth: 1,
    borderTopColor: 'rgba(255, 255, 255, 0.1)',
  },
  playerDetailCloseButton: {
    height: 50,
    borderRadius: 12,
    overflow: 'hidden',
  },
  playerDetailCloseButtonGradient: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  playerDetailCloseButtonText: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#FFFFFF',
  },
  
  // ✅ Topluluk istatistikleri stilleri
  communityStatsSection: {
    marginTop: 24,
    paddingTop: 16,
    borderTopWidth: 1,
    borderTopColor: 'rgba(255, 255, 255, 0.1)',
  },
  communityStatsContainer: {
    gap: 16,
  },
  communityStatsLoadingPlaceholder: {
    opacity: 0.5,
  },
  communityStatRow: {
    flexDirection: 'row',
    alignItems: 'flex-start',
    gap: 12,
    backgroundColor: 'rgba(15, 23, 42, 0.5)',
    padding: 14,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.2)',
  },
  communityStatIcon: {
    width: 36,
    height: 36,
    borderRadius: 10,
    backgroundColor: 'rgba(31, 162, 166, 0.15)',
    alignItems: 'center',
    justifyContent: 'center',
  },
  communityStatContent: {
    flex: 1,
    gap: 8,
  },
  communityStatLabel: {
    fontSize: 13,
    color: '#CBD5E1',
    lineHeight: 18,
  },
  communityStatHighlight: {
    color: '#1FA2A6',
    fontWeight: '700',
    fontSize: 14,
  },
  communityStatBarBg: {
    height: 6,
    backgroundColor: 'rgba(100, 116, 139, 0.3)',
    borderRadius: 3,
    overflow: 'hidden',
  },
  communityStatBarFill: {
    height: '100%',
    borderRadius: 3,
  },
  
  // Button Gradient
  buttonGradient: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    paddingVertical: 14,
  },
  buttonText: {
    fontSize: 16,
    fontWeight: '600',
    color: '#FFFFFF',
  },
  
  // ✅ Kaydediliyor... Overlay Stilleri
  savingOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.9)',
    justifyContent: 'center',
    alignItems: 'center',
  },
  savingContainer: {
    backgroundColor: 'rgba(15, 42, 36, 0.95)',
    borderRadius: 20,
    padding: 32,
    alignItems: 'center',
    borderWidth: 1,
    borderColor: 'rgba(31, 162, 166, 0.3)',
    ...Platform.select({
      ios: {
        shadowColor: '#1FA2A6',
        shadowOffset: { width: 0, height: 4 },
        shadowOpacity: 0.3,
        shadowRadius: 12,
      },
      android: {
        elevation: 8,
      },
    }),
  },
  savingText: {
    fontSize: 20,
    fontWeight: '700',
    color: '#FFFFFF',
    marginTop: 16,
  },
  savingSubtext: {
    fontSize: 14,
    color: '#9CA3AF',
    marginTop: 8,
    textAlign: 'center',
  },
});
