# Büyük Dosya Parçalama Planı

## DURUM: FAZ 1 TAMAMLANDI
## TARİH: 23 Şubat 2026 (güncellendi)

Bu dosyalar çok büyük ve parçalanması gerekiyor.
Parçalamada KURAL: JSX/style/render kısmına DOKUNMA, sadece logic çıkar.

---

## 1. MatchPrediction.tsx - FAZ 1 TAMAMLANDI

### Önceki: 9,914 satır → Şimdiki: 7,930 satır (1,984 satır azaldı)

### Yapılanlar:
1. ✅ `PlayerPredictionModal` (1203 satır) → `src/components/match/PlayerPredictionModal.tsx` (1763 satır, kendi stilleri dahil)
2. ✅ `SubstituteModal` (200 satır) → SİLİNDİ (hiçbir yerde kullanılmıyordu)
3. ✅ Sabitler → `src/constants/predictionConstants.ts` (32 satır)
   - FOCUS_CATEGORY_MAPPING, getCategoryFocus, PLAYER_RELATED_CATEGORIES, doesFocusIncludePlayerPredictions
4. ✅ Kullanılmayan import'lar temizlendi (FlatList, Animated, SlideInDown/Out)
5. ✅ Kullanılmayan stiller temizlendi (~509 satır SubstituteModal stilleri)
6. ⏸️ `FootballField` → Ertelendi (3 farklı versiyon, JSX değişikliği gerektirir)

### Kalan Yapı (7,930 satır):
- Satır 1-65: Import'lar
- Satır 66-68: Sabitler referansı (constants'tan import)
- Satır 69-145: isWeb, Slider, dimension helpers
- Satır 146-370: Interface + helper fonksiyonlar
- Satır 371-400: FootballField (SVG saha)
- Satır 401-5360: ANA BİLEŞEN
- Satır 5361-7930: StyleSheet (~2569 satır)

---

## 2. MatchSquad.tsx - FAZ 2 TAMAMLANDI

### Önceki: 8,800 satır → Şimdiki: 8,053 satır (-747 satır)

### Yapılanlar:
1. ✅ `formations` + `formationLabels` + `formationPositions` (465 satır) → `src/constants/formations.ts`
   - MatchPrediction.tsx import'ı da güncellendi
2. ✅ `FormationDetailModal` (92 satır) → SİLİNDİ (hiçbir yerde kullanılmıyordu)
3. ✅ Mock `players` + `substitutePlayers` (65 satır) → SİLİNDİ (kullanılmıyordu)
4. ✅ Kullanılmayan formationDetail stiller temizlendi (122 satır)
5. ⏸️ FormationModal (385 satır) → Yerinde kaldı (styles bağımlılığı)
6. ⏸️ PlayerModal/PlayerDetailModal → Ertelendi (paylaşımlı stiller)

---

## 3. ProfileScreen.tsx (5,812 satır)

### Parçalama Planı:
1. Profil ayarları bölümü → ayrı dosya
2. İstatistik kartları → ayrı dosya
3. Rozet/başarı bölümü → ayrı dosya

---

## UYGULAMA SIRASI:
1. Önce en güvenli parçayı çıkar (PlayerPredictionModal)
2. Test et - çalışıyorsa devam
3. SubstituteModal'ı çıkar
4. Test et
5. FootballField'ı çıkar
6. Son olarak sabitleri çıkar

## UYARI:
- Her adımdan sonra uygulamayı test et
- Style referanslarını dikkatli taşı
- Import yollarını kontrol et
- Görsellerde bozulma olursa GERİ AL
