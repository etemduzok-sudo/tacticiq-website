[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# Netlify build settings
# Note: To disable submodule checkout, go to Netlify Dashboard:
# Site settings > Build & deploy > Environment > Build settings
# Uncheck "Install git submodules" option
