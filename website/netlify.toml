[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# Disable submodule checkout to prevent errors
# Netlify will not try to checkout submodules
[build]
  ignore = "git diff --quiet HEAD^ HEAD submodule"
