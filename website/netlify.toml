[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# Disable submodule checkout to prevent errors
[build.processing]
  skip_processing = false

# Ignore submodules
[build.processing.skip]
  skip = false
